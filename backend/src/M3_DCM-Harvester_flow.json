[
    {
        "id": "a4fcd945f4957d68",
        "type": "tab",
        "label": "M3_DCM-Harvester",
        "disabled": false,
        "info": "Backend flows for Asset Crawler + Mapper (Harvester).\n\nMongoDB collections:\n  hv_runs         – harvest run records (config, status, results, embedded logs + provenance)\n  lc_assets       – local catalogue assets (written during harvest, owned by M5_DCM-LocalCatalogue)\n  cr_catalogues   – catalogue registry (read-only, shared)\n\nOperations:\n  - getHarvestCatalogues (reads from cr_catalogues)\n  - startHarvest (creates run, generates assets into lc_assets, stores log + provenance)\n  - listHarvestRuns (find all runs)\n  - getHarvestRunDetail (find one run)\n  - listHarvestLogs (extract logs from runs)\n  - listHarvestProvenance (extract provenance from runs)\n  - pauseHarvest / resumeHarvest / cancelHarvest (update run status)\n\nSRS Requirements covered:\n  FR-ACM-01: Initiating a Harvest\n  FR-ACM-02: Harvest Scope\n  FR-ACM-03: Lifecycle of Imported Assets\n  FR-ACM-04: Linked Assets\n  FR-ACM-05: Crawling References\n\nAll responses are sent back to the Dashboard via link-out → dashboard-linkin.",
        "env": []
    },
    {
        "id": "b991ffcd8f6c360a",
        "type": "mongodb4-client",
        "name": "",
        "protocol": "mongodb",
        "hostname": "mdb.facis.cloud",
        "port": "27017",
        "dbName": "DCM",
        "appName": "",
        "authSource": "",
        "authMechanism": "DEFAULT",
        "tls": true,
        "tlsCAFile": "",
        "tlsCertificateKeyFile": "",
        "tlsInsecure": false,
        "connectTimeoutMS": "30000",
        "socketTimeoutMS": "0",
        "minPoolSize": "0",
        "maxPoolSize": "100",
        "maxIdleTimeMS": "0",
        "uri": "",
        "advanced": "{}",
        "uriTabActive": "tab-uri-simple"
    },
    {
        "id": "23528f022c68a6fb",
        "type": "comment",
        "z": "a4fcd945f4957d68",
        "name": "══ Asset Crawler + Mapper (Harvester) Backend ══",
        "info": "Handles all Harvest operations per SRS FR-ACM-01 through FR-ACM-05.\nResponses go back to Dashboard uibuilder via link-out → dashboard-linkin.",
        "x": 300,
        "y": 40,
        "wires": []
    },
    {
        "id": "570fe356b08d9484",
        "type": "link in",
        "z": "a4fcd945f4957d68",
        "name": "harvest-in",
        "links": [
            "bed452d69e781678"
        ],
        "x": 115,
        "y": 300,
        "wires": [
            [
                "ac03000000000001"
            ]
        ]
    },
    {
        "id": "bde976005c102747",
        "type": "switch",
        "z": "a4fcd945f4957d68",
        "name": "route by msg.type",
        "property": "type",
        "propertyType": "msg",
        "rules": [
            {
                "t": "eq",
                "v": "getHarvestCatalogues",
                "vt": "str"
            },
            {
                "t": "eq",
                "v": "startHarvest",
                "vt": "str"
            },
            {
                "t": "eq",
                "v": "listHarvestRuns",
                "vt": "str"
            },
            {
                "t": "eq",
                "v": "getHarvestRunDetail",
                "vt": "str"
            },
            {
                "t": "eq",
                "v": "listHarvestLogs",
                "vt": "str"
            },
            {
                "t": "eq",
                "v": "listHarvestProvenance",
                "vt": "str"
            },
            {
                "t": "eq",
                "v": "pauseHarvest",
                "vt": "str"
            },
            {
                "t": "eq",
                "v": "resumeHarvest",
                "vt": "str"
            },
            {
                "t": "eq",
                "v": "cancelHarvest",
                "vt": "str"
            },
            {
                "t": "eq",
                "v": "clearHarvestHistory",
                "vt": "str"
            }
        ],
        "checkall": "true",
        "repair": false,
        "outputs": 10,
        "x": 330,
        "y": 300,
        "wires": [
            [
                "f258479ec00a9623"
            ],
            [
                "875eaf801e261b41"
            ],
            [
                "242b5761230f6a22"
            ],
            [
                "f18c93fa59ef3747"
            ],
            [
                "c087575419068b1f"
            ],
            [
                "c834493788a475e4"
            ],
            [
                "ede557980f593bbb"
            ],
            [
                "a73789af29e050c5"
            ],
            [
                "972bdb7c2c30a398"
            ],
            [
                "14cc0a58c709638f"
            ]
        ]
    },
    {
        "id": "dc56327bec3493ee",
        "type": "link out",
        "z": "a4fcd945f4957d68",
        "name": "→ Dashboard (uibuilder response)",
        "mode": "link",
        "links": [
            "4f7511bc7069ea47"
        ],
        "x": 1925,
        "y": 360,
        "wires": []
    },
    {
        "id": "f258479ec00a9623",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Get Harvest Catalogues (from cr_catalogues)",
        "func": "msg.operation = \"find\";\nmsg.collection = \"cr_catalogues\";\nmsg.payload = [\n    {},\n    { projection: { catalog: 1, uniqueId: 1, _id: 0 } }\n];\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 680,
        "y": 100,
        "wires": [
            [
                "964e0afe39aea0b6"
            ]
        ]
    },
    {
        "id": "964e0afe39aea0b6",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "",
        "x": 940,
        "y": 100,
        "wires": [
            [
                "6f260ed4d9d5a674"
            ]
        ]
    },
    {
        "id": "6f260ed4d9d5a674",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "response: getHarvestCatalogues",
        "func": "const docs = Array.isArray(msg.payload) ? msg.payload : [];\n\n// NF-2: the harvester needs the complete protocol configuration, not a summary.\nfunction protoNormalise(value) {\n    var s = String(value == null ? '' : value).trim().toLowerCase();\n    if (!s) return 'query-interface';\n    s = s.replace(/[\\s_]+/g, '-');\n    if (s === 'oai-pmh' || s === 'oaipmh' || s === 'oai') return 'oai-pmh';\n    if (s === 'dcat' || s === 'dcat-ap' || s === 'linked-data') return 'dcat';\n    if (s === 'query-interface' || s === 'query' || s === 'sparql') return 'query-interface';\n    return s;\n}\n\nconst catalogs = docs.map(d => {\n    const c = d.catalog || d;\n    const protocol = protoNormalise(c.protocol);\n    return {\n        uniqueId: d.uniqueId || c.catalogId || d._id,\n        catalogId: c.catalogId || d.uniqueId,\n        catalogName: c.catalogName || c.catalogId || '-',\n        owner: c.owner || '-',\n        strategy: c.strategy || 'none',\n        namespacesToPreserve: c.namespacesToPreserve || [],\n        shaclShapeId: c.shaclShapeId || c.shaclShapeSchemaId || '',\n        enabled: c.enabled !== false,\n        updatedAt: c.updatedAt || '',\n\n        // protocol\n        protocol: protocol,\n        protocolLabel: c.protocol || 'Query interface',\n        baseEndpoint: c.baseEndpoint || '',\n        mimeType: c.mimeType || '',\n\n        // query interface\n        queryEndpoint: c.queryEndpoint || '',\n        queryLanguage: c.queryLanguage || c.queryLanguages || '',\n        queryMethod: c.queryMethod || 'GET',\n        queryText: c.queryText || '',\n        queryParameterName: c.queryParameterName || 'query',\n        requestContentType: c.requestContentType || '',\n        resultMimeType: c.resultMimeType || '',\n        resultMode: c.resultMode || '',\n        resultRootPath: c.resultRootPath || '',\n        resultIdVariable: c.resultIdVariable || '',\n        resultNameVariable: c.resultNameVariable || '',\n        resultTypeVariable: c.resultTypeVariable || '',\n\n        // OAI-PMH\n        metadataPrefix: c.metadataPrefix || '',\n        setSpec: c.setSpec || '',\n        resumptionTokenEnabled: c.resumptionTokenEnabled !== undefined ? c.resumptionTokenEnabled !== false : c.resumptionToken !== false,\n        maxPages: c.maxPages || 50,\n\n        // DCAT\n        dcatCatalogUri: c.dcatCatalogUri || '',\n        linkedDataEndpoint: c.linkedDataEndpoint || '',\n        contentNegotiation: c.contentNegotiation || '',\n\n        // authentication\n        auth: c.auth || 'none',\n        authLoginEndpoint: c.authLoginEndpoint || '',\n        authUsername: c.authUsername || '',\n        authPassword: c.authPassword || '',\n        authPayloadTemplate: c.authPayloadTemplate || '',\n        authTokenPath: c.authTokenPath || 'token',\n        authTokenInjection: c.authTokenInjection || 'body',\n        authTokenFieldName: c.authTokenFieldName || 'token',\n        authTokenPrefix: c.authTokenPrefix || '',\n        authStaticToken: c.authStaticToken || '',\n        authApiKey: c.authApiKey || '',\n        authApiKeyHeader: c.authApiKeyHeader || 'X-API-Key',\n\n        // response mapping\n        responseRootPath: c.responseRootPath || '',\n        responseAssetIdField: c.responseAssetIdField || '',\n        responseAssetNameField: c.responseAssetNameField || '',\n        responseAssetTypeField: c.responseAssetTypeField || '',\n        typeMapping: Array.isArray(c.typeMapping) ? c.typeMapping : [],\n\n        sourceData: c.sourceData || null,\n        sourceFileName: c.sourceFileName || ''\n    };\n});\n\nmsg.response = {\n    action: \"getHarvestCatalogues\",\n    status: \"success\",\n    catalogs: catalogs\n};\ndelete msg.payload;\nreturn msg;\n",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1160,
        "y": 100,
        "wires": [
            [
                "dc56327bec3493ee"
            ]
        ]
    },
    {
        "id": "478e4cbe3927bbd4",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Start Harvest — build run + lc_assets",
        "func": "const chars = 'abcdefghijklmnopqrstuvwxyz0123456789';\nlet rnd = '';\nfor (let i = 0; i < 12; i++) rnd += chars[Math.floor(Math.random() * chars.length)];\nconst runId = 'run-' + Date.now().toString(36) + rnd;\n\n// ── FR-CR-04: JSON Field Mapping helpers ─────────────────────────────────\nfunction fmGetPath(obj, p) {\n    if (!p || obj == null) return undefined;\n\n    // Strategy 1 — exact match: the path *is* a key on this object.\n    if (Object.prototype.hasOwnProperty.call(obj, p)) return obj[p];\n\n    // Strategy 2 — greedy walk that prefers the LONGEST matching key prefix\n    // at every level. Naturally handles keys that themselves contain dots.\n    function walk(o, rem) {\n        if (rem === '' || rem == null) return o;\n        if (o == null || typeof o !== 'object') return undefined;\n        if (Object.prototype.hasOwnProperty.call(o, rem)) return o[rem];\n        const keys = Object.keys(o).slice().sort((a, b) => b.length - a.length);\n        for (const k of keys) {\n            if (rem.startsWith(k + '.')) {\n                const v = walk(o[k], rem.slice(k.length + 1));\n                if (v !== undefined) return v;\n            }\n        }\n        return undefined;\n    }\n    const greedy = walk(obj, p);\n    if (greedy !== undefined) return greedy;\n\n    // Strategy 3 — backward compatibility with the v35 escape-aware syntax.\n    const parts = []; let cur = ''; let esc = false;\n    for (let i = 0; i < p.length; i++) {\n        const ch = p[i];\n        if (esc) { cur += ch; esc = false; continue; }\n        if (ch === '\\\\') { esc = true; continue; }\n        if (ch === '.') { parts.push(cur); cur = ''; continue; }\n        cur += ch;\n    }\n    if (cur) parts.push(cur);\n    let v = obj;\n    for (const k of parts) {\n        if (v == null || typeof v !== 'object') return undefined;\n        v = v[k];\n    }\n    return v;\n}\nfunction fmSetPath(obj, p, value) {\n    if (!p) return;\n    const parts = p.split('.');\n    let cur = obj;\n    for (let i = 0; i < parts.length - 1; i++) {\n        const k = parts[i];\n        if (cur[k] == null || typeof cur[k] !== 'object') cur[k] = {};\n        cur = cur[k];\n    }\n    cur[parts[parts.length - 1]] = value;\n}\nconst FM_TRANSFORMS = {\n    identity: v => v,\n    parseAddress: v => {\n        if (v == null) return null;\n        const s = String(v).trim();\n        let m = s.match(/^(.+?),\\s*(\\d{4,5})\\s+(.+?),\\s*(.+)$/);\n        if (m) return { streetAddress: m[1].trim(), postalCode: m[2].trim(), addressLocality: m[3].trim(), addressCountry: m[4].trim() };\n        m = s.match(/^(.+?),\\s*(\\d{4,5})\\s+(.+)$/);\n        if (m) return { streetAddress: m[1].trim(), postalCode: m[2].trim(), addressLocality: m[3].trim() };\n        return { streetAddress: s };\n    },\n    split:     (v, sep) => v == null ? [] : String(v).split(sep || ','),\n    concat:    (v, sep, ...rest) => [v, ...rest].filter(x => x != null).join(sep || ' '),\n    lower:     v => v == null ? v : String(v).toLowerCase(),\n    upper:     v => v == null ? v : String(v).toUpperCase(),\n    trim:      v => v == null ? v : String(v).trim(),\n    toNumber:  v => v == null ? null : Number(v),\n    toBoolean: v => Boolean(v),\n    nonEmpty:  v => (v != null && v !== '') ? v : undefined\n};\nfunction fmApplyTransform(value, spec) {\n    if (!spec) return value;\n    if (typeof spec === 'string') {\n        const fn = FM_TRANSFORMS[spec];\n        return fn ? fn(value) : value;\n    }\n    if (typeof spec === 'object' && spec.fn) {\n        const fn = FM_TRANSFORMS[spec.fn];\n        return fn ? fn(value, ...(spec.args || [])) : value;\n    }\n    return value;\n}\nfunction fmFindMapping(catId, catName, mappings) {\n    if (!Array.isArray(mappings)) return null;\n    return mappings.find(m =>\n        m && m.transformationStrategy === 'JSON Field Mapping' &&\n        (m.remoteCatalogueId === catId || m.remoteCatalogue === catName)\n    ) || null;\n}\nfunction fmTransform(entry, mapping, lcId) {\n    const out = {};\n    const rows = Array.isArray(mapping.fieldMappings) ? mapping.fieldMappings : [];\n    for (const row of rows) {\n        if (!row || !row.from || !row.to) continue;\n        const raw = fmGetPath(entry, row.from);\n        const v = fmApplyTransform(raw, row.transform);\n        if (v === undefined) continue;\n        fmSetPath(out, row.to, v);\n    }\n    return {\n        '@context': mapping.jsonLdContext || 'https://registry.lab.gaia-x.eu/development/context/development',\n        '@type':    mapping.targetClass   || '',\n        '@id':      'did:web:dcm.local:' + lcId,\n        ...out\n    };\n}\nconst _fieldMappings = Array.isArray(msg._fieldMappings) ? msg._fieldMappings : [];\n// ── /FR-CR-04 helpers ─────────────────────────────────────────────────────\n\n\nconst catalogues = msg.data?.catalogues || [];\nconst catNames = catalogues.map(c => c.catalogName).filter(Boolean);\nconst now = new Date();\nconst nowISO = now.toISOString();\nconst nowShort = nowISO.replace('T', ' ').slice(0, 19);\n// NF-4: run-scoped diagnostic accumulator (created upstream)\nconst _runDiag = msg._runDiag || (msg._runDiag = { errors: [], logs: [], catalogueStatus: {}, counters: { transportErrors: 0, parseErrors: 0, validationErrors: 0, entryErrors: 0, persistenceErrors: 0 } });\n\nfunction resolvePath(obj, path) {\n    if (!path || !obj) return obj;\n    const parts = path.split('.');\n    let cur = obj;\n    for (const p of parts) {\n        if (cur == null || typeof cur !== 'object') return undefined;\n        cur = cur[p];\n    }\n    return cur;\n}\n\nfunction extractEntries(data, rootPath) {\n    if (!data) return [];\n    if (typeof data === 'string' || Buffer.isBuffer(data)) return [{ __rdfRaw: Buffer.isBuffer(data) ? data.toString('utf8') : data, __rdfContentType: '' }];\n    if (data.__rdfRaw !== undefined) return [data];\n    if (typeof data === 'string' || Buffer.isBuffer(data)) return [{ __rdfRaw: Buffer.isBuffer(data) ? data.toString('utf8') : data, __rdfContentType: '' }];\n    if (data.__rdfRaw !== undefined) return [data];\n    if (typeof data === 'string' || Buffer.isBuffer(data)) return [{ __rdfRaw: Buffer.isBuffer(data) ? data.toString('utf8') : data, __rdfContentType: '' }];\n    if (data.__rdfRaw !== undefined) return [data];\n    if (rootPath) {\n        const resolved = resolvePath(data, rootPath);\n        if (Array.isArray(resolved)) return resolved;\n        if (resolved && typeof resolved === 'object') return [resolved];\n    }\n    if (Array.isArray(data)) return data;\n    if (data['dcat:dataset'] && Array.isArray(data['dcat:dataset'])) return data['dcat:dataset'];\n    if (data.dataset && Array.isArray(data.dataset)) return data.dataset;\n    if (data.datasets && Array.isArray(data.datasets)) return data.datasets;\n    if (data.data && Array.isArray(data.data)) return data.data;\n    if (data.results && Array.isArray(data.results)) return data.results;\n    if (data.result && data.result.results && Array.isArray(data.result.results)) return data.result.results;\n    if (data.items && Array.isArray(data.items)) return data.items;\n    if (data.records && Array.isArray(data.records)) return data.records;\n    if (data.entries && Array.isArray(data.entries)) return data.entries;\n    if (data.assets && Array.isArray(data.assets)) return data.assets;\n    if (data.catalogs && Array.isArray(data.catalogs)) return data.catalogs;\n    if (data.title || data.name || data.identifier || data['@type']) return [data];\n    if (data._id || data.requestId != null || data.companyDetails) return [data];\n    const skipKeys = ['auditInfo','approveInfo','decisionHistory','errors','warnings','logs'];\n    for (const key of Object.keys(data)) {\n        if (Array.isArray(data[key]) && data[key].length > 0 && typeof data[key][0] === 'object' && !skipKeys.includes(key)) return data[key];\n    }\n    return null; // NF-4 RC-6: unrecognised (non-RDF) payload shape — do not fabricate an entry\n}\n\nfunction nsList(value) {\n    if (Array.isArray(value)) return value.map(function (v) { return String(v).trim(); }).filter(Boolean);\n    if (typeof value === 'string') return value.split(',').map(function (v) { return v.trim(); }).filter(Boolean);\n    return [];\n}\nfunction extractName(entry, nameField) {\n    if (!entry || typeof entry !== 'object') return 'Unknown Asset';\n    if (nameField) {\n        const v = resolvePath(entry, nameField);\n        if (v && typeof v === 'string' && v.trim()) return v;\n    }\n    const cands = ['title','name','catalogName','label','displayName','companyName','organisationName','organization','fullName','shortName','legalName','tradingName','firma'];\n    for (const f of cands) { if (entry[f] && typeof entry[f] === 'string' && entry[f].trim()) return entry[f]; }\n\n    // companyDetails (legacy)\n    const cd = entry.companyDetails;\n    if (cd && typeof cd === 'object') {\n        if (cd['A.1.1.1'] && typeof cd['A.1.1.1'] === 'string') return cd['A.1.1.1'];\n        if (cd['A.2.1.1'] && typeof cd['A.2.1.1'] === 'string') return cd['A.2.1.1'];\n        for (const f of cands) { if (cd[f] && typeof cd[f] === 'string' && cd[f].trim()) return cd[f]; }\n    }\n\n    // Trusted Cloud-style: A.1.Anbieter.A.1.1.<varies>.A.1.1.1\n    function findFirstByKey(node, targetKey, depth) {\n        if (depth > 6 || node == null || typeof node !== 'object') return null;\n        if (Object.prototype.hasOwnProperty.call(node, targetKey)) {\n            const v = node[targetKey];\n            if (typeof v === 'string' && v.trim()) return v;\n        }\n        for (const k of Object.keys(node)) {\n            const child = node[k];\n            if (child && typeof child === 'object') {\n                const found = findFirstByKey(child, targetKey, depth + 1);\n                if (found) return found;\n            }\n        }\n        return null;\n    }\n    const tcCompany = findFirstByKey(entry, 'A.1.1.1', 0);\n    if (tcCompany) return tcCompany;\n    const tcService = findFirstByKey(entry, 'A.2.1.1', 0);\n    if (tcService) return tcService;\n\n    if (entry.createdBy && entry.createdBy.fullName && typeof entry.createdBy.fullName === 'string') return entry.createdBy.fullName;\n    for (const key of Object.keys(entry)) {\n        const nested = entry[key];\n        if (nested && typeof nested === 'object' && !Array.isArray(nested)) {\n            for (const f of cands) { if (nested[f] && typeof nested[f] === 'string' && nested[f].trim()) return nested[f]; }\n        }\n    }\n    for (const key of Object.keys(entry)) {\n        const val = entry[key];\n        if (typeof val === 'string' && val.length > 2 && val.length < 200 && !/^[\\d\\-:.TZ]+$/.test(val) && !/^[0-9a-f]{24}$/i.test(val) && !key.startsWith('_')) return val;\n    }\n    return 'Unknown Asset';\n}\n\nfunction extractId(entry, idField) {\n    if (idField) { const v = resolvePath(entry, idField); if (v) return String(v); }\n    return String(entry.identifier || entry.id || entry._id || entry.requestId || entry.uuid || ('remote-' + Math.random().toString(36).slice(2, 10)));\n}\n\nfunction extractType(entry, typeField) {\n    if (typeField) { const v = resolvePath(entry, typeField); if (v) return String(v); }\n    const cd = entry.companyDetails;\n    return String(entry['@type'] || entry.type || entry.assetType || (cd && cd['A.1.1.0']) || (cd && cd['A.2.1.4']) || 'dcat:Dataset');\n}\n// NF-4 RC-6: strict identity/name extraction — no invented fallbacks\nfunction extractIdStrict(entry, idField) {\n    if (idField) { const v = resolvePath(entry, idField); if (v) return String(v); }\n    const v = entry.identifier || entry.id || entry._id || entry.requestId || entry.uuid;\n    return v ? String(v) : null;\n}\nfunction extractNameStrict(entry, nameField) {\n    const nm = extractName(entry, nameField);\n    return (nm && nm !== 'Unknown Asset') ? nm : null;\n}\nfunction validateEntry(entry, idField, nameField) {\n    const reasons = [];\n    if (!entry || typeof entry !== 'object' || Array.isArray(entry)) reasons.push('not_an_object');\n    if (entry && typeof entry === 'object' && !Array.isArray(entry) && Object.keys(entry).length === 0) reasons.push('empty_object');\n    const rawId = extractIdStrict(entry, idField);\n    if (!rawId) reasons.push('no_identifier');\n    const name = extractNameStrict(entry, nameField);\n    if (!name) reasons.push('no_title');\n    return { valid: reasons.length === 0, reasons: reasons, rawId: rawId, name: name };\n}\n\n// ── FR-ACM-05: Crawling Reference helpers ─────────────────────────────────────\nconst REFERENCE_FIELDS = [\n  'dcat:qualifiedRelation',\n  'dcat:Relationship',\n  'dcat:source',\n  'prov:wasDerivedFrom',\n  'dct:references',\n  'dct:relation',\n  'qualifiedRelation',\n  'relation',\n  'references',\n  'source'\n];\n\nfunction normalizeRefUri(v) {\n  if (v == null) return null;\n  if (typeof v === 'string') {\n    const s = v.trim();\n    if (!s) return null;\n    if (/^https?:\\/\\//i.test(s) || /^did:[a-z0-9]+:/i.test(s) || /^urn:/i.test(s)) return s;\n    return null;\n  }\n  if (typeof v === 'object') {\n    if (v['@id']) return normalizeRefUri(v['@id']);\n    if (v.href) return normalizeRefUri(v.href);\n    if (v.id && typeof v.id === 'string') return normalizeRefUri(v.id);\n    if (v.hasTarget) return normalizeRefUri(v.hasTarget);\n    if (v.target) return normalizeRefUri(v.target);\n  }\n  return null;\n}\n\nfunction extractReferencesFromEntry(entry) {\n  if (!entry || typeof entry !== 'object') return [];\n  const refs = [];\n  for (const field of REFERENCE_FIELDS) {\n    const v = entry[field];\n    if (v == null) continue;\n    const arr = Array.isArray(v) ? v : [v];\n    for (const item of arr) {\n      const uri = normalizeRefUri(item);\n      if (uri) refs.push({ field: field, uri: uri });\n    }\n  }\n  if (Array.isArray(entry.links)) {\n    for (const l of entry.links) {\n      if (l && typeof l.href === 'string') {\n        const uri = normalizeRefUri(l.href);\n        if (uri) refs.push({ field: 'links', uri: uri, rel: l.rel || '' });\n      }\n    }\n  }\n  return refs;\n}\n\nfunction entryMatchesUri(entry, uri) {\n  if (!entry || typeof entry !== 'object' || !uri) return false;\n  const candidates = [\n    entry['@id'], entry.id, entry.identifier, entry._id,\n    entry.uri, entry.url, entry.did, entry.canonicalUri\n  ].filter(Boolean).map(String);\n  return candidates.indexOf(uri) !== -1;\n}\n// ── /FR-ACM-05 helpers ────────────────────────────────────────────────────────\n\nconst provenance = [];\nconst logs = [];\nconst assetDetails = [];\nconst lcDocs = [];\nlet assetCounter = 0;\n\nfor (const cat of catalogues) {\n    const catName = cat.catalogName || cat.uniqueId || 'Unknown';\n    const catId = cat.uniqueId || cat.catalogId || '';\n    const isCrawlOnly = cat._crawlOnly === true;\n    if (isCrawlOnly) {\n        logs.push({ runId, assetId: '', level: 'info', message: 'Catalogue tagged crawl-only, skipping full import: ' + catName, catalogue: catName, timestamp: nowISO });\n        continue;\n    }\n    const strategy = cat.strategy || 'none';\n    const catProtocol = cat.protocolNormalised || cat.protocol || '';\n    const rootPath = cat.responseRootPath || '';\n    const idField = cat.responseAssetIdField || '';\n    const nameField = cat.responseAssetNameField || '';\n    const typeField = cat.responseAssetTypeField || '';\n    const typeMapping = Array.isArray(cat.typeMapping) ? cat.typeMapping : [];\n    const sourceData = cat.sourceData || null;\n    let entries = [];\n    if (sourceData) {\n        const _ext = extractEntries(sourceData, rootPath);\n        if (_ext === null) {\n            _runDiag.errors.push({ stage:'parse', step:'extract', code:'unrecognised_payload_shape', level:'error', catalogueId: catId, catalogueName: catName, assetId:null, retryable:false, message:'Response for catalogue \"' + catName + '\" parsed but contained no recognisable asset collection. Nothing imported.', detail:{ rawSnippet:(function(){try{return JSON.stringify(sourceData).slice(0,200);}catch(e){return '';}})() }, timestamp: nowISO });\n            _runDiag.logs.push({ assetId:'', level:'error', step:'extract', code:'unrecognised_payload_shape', message:'Unrecognised payload shape for ' + catName, catalogue: catName, timestamp: nowISO });\n            _runDiag.counters.parseErrors++;\n            if (!_runDiag.catalogueStatus[catId]) _runDiag.catalogueStatus[catId] = 'failed';\n            entries = [];\n        } else {\n            entries = _ext;\n            logs.push({ runId, assetId: '', level: 'info', message: 'Extracted ' + entries.length + ' entries from sourceData', catalogue: catName, timestamp: nowISO });\n        }\n    }\n\n    for (let i = 0; i < entries.length; i++) {\n        assetCounter++;\n        const entry = entries[i];\n        if (!entry || typeof entry !== 'object') continue;\n        const rdfWrapped = Object.prototype.hasOwnProperty.call(entry, '__rdfRaw');\n        const sourceValue = rdfWrapped ? entry.__rdfRaw : entry;\n        const sourceContentType = rdfWrapped ? (entry.__rdfContentType || cat.sourceContentType || cat.mimeType || cat.rdfFormat || '') : (cat.sourceContentType || cat.mimeType || cat.rdfFormat || '');\n        const assetId = 'asset-' + runId + '-' + assetCounter;\n        const lcId = 'lc-' + runId + '-' + assetCounter;\n        // NF-4 RC-6: strict validation for non-RDF entries (RDF payloads are validated by the RDF pipeline)\n        if (!rdfWrapped && cat.strictAssetValidation !== false) {\n            const _val = validateEntry(entry, idField, nameField);\n            if (!_val.valid) {\n                assetDetails.push({ assetId: assetId, title: '(rejected)', catalogue: catName, status: 'error', reasons: _val.reasons, sourceIndex: i });\n                _runDiag.errors.push({ stage:'entry', step:'validate', code:'unrecognisable_entry', level:'error', catalogueId: catId, catalogueName: catName, assetId: assetId, retryable:false, message:'Entry #' + i + ' rejected: ' + _val.reasons.join(', ') + '. No asset was created.', detail:{ rawSnippet:(function(){try{return JSON.stringify(entry).slice(0,200);}catch(e){return '';}})() }, timestamp: nowISO });\n                _runDiag.logs.push({ assetId: assetId, level:'error', step:'validate', code:'unrecognisable_entry', message:'Entry #' + i + ' rejected: ' + _val.reasons.join(', '), catalogue: catName, timestamp: nowISO });\n                _runDiag.counters.entryErrors++;\n                continue;\n            }\n        }\n        // NF-6: RDF payloads carry their identity in the graph, not in JSON\n        // fields, so use the subject IRI and dct:title instead of inventing one.\n        const rdfSubject = rdfWrapped ? String(entry.__rdfSubject || '').trim() : '';\n        const rdfTitle = rdfWrapped ? String(entry.__rdfTitle || '').trim() : '';\n        const rdfFallbackId = (cat.protocolProvenance && cat.protocolProvenance.endpoint) || cat.dcatCatalogUri || cat.baseEndpoint || '';\n        const rawId = rdfWrapped ? (rdfSubject || rdfFallbackId || extractId(entry, idField)) : extractId(entry, idField);\n        const assetName = rdfWrapped ? (rdfTitle || rdfSubject || catName) : extractName(entry, nameField);\n        const assetType = extractType(entry, typeField);\n        const mapRow = typeMapping.find(m => m && m.remoteType === assetType);\n        const localTypeId = mapRow && mapRow.localTypeId ? String(mapRow.localTypeId) : '';\n        const domain = entry.domain || entry.theme || entry.category || catName;\n\n        assetDetails.push({ assetId: assetId, title: assetName, catalogue: catName, status: 'success', strategy: strategy, sourceId: rawId, localTypeId: localTypeId });\n        lcDocs.push({\n            uniqueId: lcId,\n            id: lcId,\n            assets: assetName,\n            title: assetName,\n            type: assetType,\n            localTypeId: localTypeId,\n            name: catName,\n            domain: domain,\n            updated: nowShort,\n            integrationStatus: 'Active',\n            sourceCatalogue: catName,\n            sourceCatalogueName: catName,\n            sourceCatalogueId: catId,\n            harvestRunId: runId,\n            transformationStrategy: strategy,\n            remoteAssetId: rawId,\n            originalForm: sourceValue,\n            sourceContentType: sourceContentType,\n            catalogueTransform: {\n                strategy: cat.strategy || 'none',\n                namespacesToPreserve: nsList(cat.namespacesToPreserve),\n                shaclShapeSchemaId: cat.shaclShapeId || cat.shaclShapeSchemaId || ''\n            },\n            transformedForm: (function(){\n                // ── FR-CR-04: apply JSON field mapping if configured ──\n                const _m = fmFindMapping(catId, catName, _fieldMappings);\n                if (!_m) return null;\n                try { return fmTransform(entry, _m, lcId); }\n                catch (_e) {\n                    logs.push({ runId: runId, assetId: assetId, level: 'warn',\n                        message: 'JSON field mapping failed: ' + _e.message,\n                        catalogue: catName, timestamp: nowISO });\n                    return null;\n                }\n            })(),\n            sourceAssetId: rawId,\n            protocol: cat.protocolNormalised || cat.protocol || null,\n            sourceEndpoint: (cat.protocolProvenance && cat.protocolProvenance.endpoint) || cat.baseEndpoint || cat.queryEndpoint || cat.dcatCatalogUri || null,\n            oaiMetadataPrefix: (cat.protocolProvenance && cat.protocolProvenance.metadataPrefix) || null,\n            oaiSetSpec: (cat.protocolProvenance && cat.protocolProvenance.setSpec) || null,\n            queryExecutionRef: (cat.protocolProvenance && cat.protocolProvenance.query) ? { language: cat.protocolProvenance.queryLanguage || null, method: cat.protocolProvenance.queryMethod || null, query: cat.protocolProvenance.query } : null,\n            rawSourceMetadata: (entry && entry.__rawMetadata) || null,\n            harvestedAt: nowISO,\n            createdAt: nowISO\n        });\n        provenance.push({\n            runId: runId,\n            harvestRunId: runId,\n            assetId: assetId,\n            title: assetName,\n            catalogue: catName,\n            sourceCatalogueName: catName,\n            sourceCatalogueId: catId,\n            action: 'imported',\n            strategy: strategy,\n            transformationStrategy: strategy,\n            protocol: catProtocol,\n            timestamp: nowISO,\n            harvestedAt: nowISO,\n            status: 'success',\n            sourceId: rawId\n        });\n        logs.push({ runId: runId, assetId: assetId, level: 'info', message: 'Imported: ' + assetName, catalogue: catName, timestamp: nowISO });\n    }\n\n    logs.push({ runId: runId, assetId: '', level: 'info', message: 'Harvest completed for ' + catName + ': ' + entries.length + ' entries processed', catalogue: catName, timestamp: nowISO });\n}\n\n// ── FR-ACM-05: Crawl references (BFS, max depth 3, cycle detection) ──────────\nconst resolveReferences = !!(msg.data?.scope && msg.data.scope.resolveReferences);\nlet crawlStats = { enabled: resolveReferences, referencesFound: 0, crawledAssets: 0, unresolved: 0, maxDepthReached: 0 };\n\nif (resolveReferences) {\n    const crawlIndex = [];\n    for (const cat of catalogues) {\n        const catName = cat.catalogName || cat.uniqueId || 'Unknown';\n        const catId = cat.uniqueId || cat.catalogId || '';\n        const rootPath = cat.responseRootPath || '';\n        const entries = (cat.sourceData ? extractEntries(cat.sourceData, rootPath) : []) || [];\n        for (const entry of entries) {\n            crawlIndex.push({\n                entry,\n                catName, catId,\n                strategy: cat.strategy || 'none',\n                idField: cat.responseAssetIdField || '',\n                nameField: cat.responseAssetNameField || '',\n                typeField: cat.responseAssetTypeField || '',\n                typeMapping: Array.isArray(cat.typeMapping) ? cat.typeMapping : [],\n                protocol: cat.protocolNormalised || cat.protocol || '',\n                crawlOnly: cat._crawlOnly === true\n            });\n        }\n    }\n\n    const MAX_DEPTH = 3;\n    const visitedUris = new Set();\n    const visitedAssetIds = new Set(lcDocs.map(d => d.id));\n\n    const queue = [];\n    for (const lcDoc of lcDocs) {\n        queue.push({ entry: lcDoc.originalForm, lcId: lcDoc.id, depth: 1 });\n    }\n\n    while (queue.length > 0) {\n        const { entry, lcId, depth } = queue.shift();\n        if (depth > MAX_DEPTH) continue;\n        crawlStats.maxDepthReached = Math.max(crawlStats.maxDepthReached, depth);\n\n        const refs = extractReferencesFromEntry(entry);\n        for (const ref of refs) {\n            crawlStats.referencesFound++;\n            if (visitedUris.has(ref.uri)) continue;\n            visitedUris.add(ref.uri);\n\n            const hit = crawlIndex.find(row => entryMatchesUri(row.entry, ref.uri));\n            if (!hit) {\n                crawlStats.unresolved++;\n                provenance.push({\n                    runId, harvestRunId: runId, assetId: lcId, action: 'crawl-unresolved',\n                    referenceUri: ref.uri, referenceField: ref.field,\n                    timestamp: nowISO, harvestedAt: nowISO, status: 'warning',\n                    message: 'Reference target not found in any supplied catalogue'\n                });\n                logs.push({\n                    runId, assetId: lcId, level: 'warn',\n                    message: 'Unresolved cross-catalogue reference: ' + ref.uri + ' (field=' + ref.field + ')',\n                    catalogue: '', timestamp: nowISO\n                });\n                continue;\n            }\n\n            assetCounter++;\n            const newAssetId = 'asset-' + runId + '-' + assetCounter;\n            const newLcId = 'lc-' + runId + '-' + assetCounter;\n            if (visitedAssetIds.has(newLcId)) continue;\n            visitedAssetIds.add(newLcId);\n\n            const rawId = extractId(hit.entry, hit.idField);\n            const assetName = extractName(hit.entry, hit.nameField);\n            const assetType = extractType(hit.entry, hit.typeField);\n            const mapRow = hit.typeMapping.find(m => m && m.remoteType === assetType);\n            const localTypeId = mapRow && mapRow.localTypeId ? String(mapRow.localTypeId) : '';\n            const domain = hit.entry.domain || hit.entry.theme || hit.entry.category || hit.catName;\n\n            assetDetails.push({ assetId: newAssetId, title: assetName, catalogue: hit.catName, status: 'success', strategy: hit.strategy, sourceId: rawId, localTypeId: localTypeId, crawled: true });\n            const newLcDoc = {\n                uniqueId: newLcId,\n                id: newLcId,\n                assets: assetName,\n                title: assetName,\n                type: assetType,\n                localTypeId: localTypeId,\n                name: hit.catName,\n                domain: domain,\n                updated: nowShort,\n                integrationStatus: 'Active',\n                sourceCatalogue: hit.catName,\n                sourceCatalogueName: hit.catName,\n                sourceCatalogueId: hit.catId,\n                harvestRunId: runId,\n                transformationStrategy: hit.strategy,\n                protocol: hit.protocol || '',\n                remoteAssetId: rawId,\n                originalForm: hit.entry,\n                transformedForm: (function(){\n                    // ── FR-CR-04: apply JSON field mapping if configured (crawl) ──\n                    const _m = fmFindMapping(hit.catId, hit.catName, _fieldMappings);\n                    if (!_m) return null;\n                    try { return fmTransform(hit.entry, _m, newLcId); }\n                    catch (_e) {\n                        logs.push({ runId: runId, assetId: newAssetId, level: 'warn',\n                            message: 'JSON field mapping failed (crawl): ' + _e.message,\n                            catalogue: hit.catName, timestamp: nowISO });\n                        return null;\n                    }\n                })(),\n                sourceAssetId: rawId,\n                harvestedAt: nowISO,\n                createdAt: nowISO,\n                crawledFrom: lcId,\n                crawlDepth: depth,\n                crawlReferenceUri: ref.uri,\n                crawlReferenceField: ref.field\n            };\n            lcDocs.push(newLcDoc);\n            provenance.push({\n                runId: runId,\n                harvestRunId: runId,\n                assetId: newAssetId,\n                title: assetName,\n                catalogue: hit.catName,\n                sourceCatalogueName: hit.catName,\n                sourceCatalogueId: hit.catId,\n                action: 'crawled',\n                strategy: hit.strategy,\n                transformationStrategy: hit.strategy,\n                protocol: hit.protocol || '',\n                timestamp: nowISO,\n                harvestedAt: nowISO,\n                status: 'success',\n                sourceId: rawId,\n                crawledFrom: lcId,\n                crawlDepth: depth\n            });\n            logs.push({ runId, assetId: newAssetId, level: 'info', message: 'Crawled (depth=' + depth + '): ' + assetName + ' from ' + hit.catName + ' (ref=' + ref.uri + ')', catalogue: hit.catName, timestamp: nowISO });\n            crawlStats.crawledAssets++;\n\n            if (depth + 1 <= MAX_DEPTH) {\n                queue.push({ entry: hit.entry, lcId: newLcId, depth: depth + 1 });\n            }\n        }\n    }\n\n    logs.push({ runId, assetId: '', level: 'info', message: 'Crawl complete: ' + crawlStats.crawledAssets + ' assets crawled, ' + crawlStats.unresolved + ' unresolved, max depth ' + crawlStats.maxDepthReached, catalogue: '', timestamp: nowISO });\n}\n// ── /FR-ACM-05 crawl block ───────────────────────────────────────────────────\n\n// NF-4: derive status/result from real signal (never hardcoded)\nconst successCount = assetDetails.filter(a => a.status === 'success').length;\nconst errorCount = _runDiag.errors.filter(e => e.level === 'error').length;\nconst warningCount = _runDiag.errors.filter(e => e.level === 'warn').length;\nconst processedCount = assetDetails.length;\nconst importedCount = lcDocs.length;\nconst attemptedCatalogues = catalogues.filter(c => !c._crawlOnly).length;\nconst failedCatalogues = Object.keys(_runDiag.catalogueStatus).filter(function(k){ var s = _runDiag.catalogueStatus[k]; return s === 'failed' || s === 'invalid'; }).length;\n_runDiag.errors.forEach(function(e){ if (!e.runId) e.runId = runId; });\n_runDiag.logs.forEach(function(l){ if (!l.runId) l.runId = runId; });\n// A protocol warning such as OAI_MAX_PAGES_REACHED means the source was not read\n// to the end, so the run is partial even though every imported asset is valid.\nfunction finalizeHarvestRun(sc, ec, wc, attempted, failed) {\n    let status, result;\n    if (ec === 0 && wc === 0) { status = 'completed'; result = 'Success'; }\n    else if (ec === 0) { status = 'completed_with_warnings'; result = 'Partial'; }\n    else if (sc > 0) { status = 'completed_with_errors'; result = 'Partial'; }\n    else { status = 'failed'; result = 'Failed'; }\n    if (attempted > 0 && failed >= attempted && sc === 0) { status = 'failed'; result = 'Failed'; }\n    return { status: status, result: result };\n}\nconst _fin = finalizeHarvestRun(successCount, errorCount, warningCount, attemptedCatalogues, failedCatalogues);\nconst _completedAt = new Date().toISOString().replace('T', ' ').slice(0, 19);\nconst _durMs = Math.max(0, Date.parse(_completedAt.replace(' ', 'T')) - Date.parse(nowShort.replace(' ', 'T')));\n\nconst run = {\n    uniqueId: runId,\n    catalogueName: catNames.filter(n => !catalogues.find(c => c.catalogName === n && c._crawlOnly)).join(', ') || 'Harvest',\n    scope: msg.data?.scope || {},\n    lifecycle: msg.data?.lifecycle || {},\n    status: _fin.status,\n    startedAt: nowShort,\n    completedAt: _completedAt,\n    duration: (_durMs / 1000).toFixed(1) + 's',\n    totalAssets: importedCount,\n    assetsAdded: importedCount,\n    processedCount: processedCount,\n    importedCount: importedCount,\n    successCount: successCount,\n    errorCount: errorCount,\n    warningCount: warningCount,\n    warnings: _runDiag.errors.filter(function (e) { return e.level === 'warn'; }),\n    partial: _fin.result === 'Partial',\n    errors: _runDiag.errors,\n    catalogueStatus: _runDiag.catalogueStatus,\n    attemptedCatalogues: attemptedCatalogues,\n    failedCatalogues: failedCatalogues,\n    result: _fin.result,\n    tool: 'Harvester',\n    provenance: provenance,\n    logs: (_runDiag.logs || []).concat(logs),\n    assetIds: lcDocs.map(a => a.id),\n    crawlStats: crawlStats,\n    protocolEvidence: catalogues.filter(function (c) { return c && c.protocolEvidence; }).map(function (c) {\n        return Object.assign({ catalogueId: c.uniqueId || c.catalogId || null, catalogueName: c.catalogName || null }, c.protocolEvidence);\n    })\n};\n\nconst msg1 = {\n    _harvestRun: run,\n    _lcDocs: lcDocs,\n    _runDiag: _runDiag,\n    // Carry-forward prefetched lookups so the post-import AI hook (ai04m3000020) can use them.\n    _fieldMappings: msg._fieldMappings,\n    _aiPrompts: msg._aiPrompts,\n    _aiProviders: msg._aiProviders,\n    _apiMappings: msg._apiMappings,\n    _localSchemas: msg._localSchemas,\n    operation: 'insertOne',\n    collection: 'hv_runs',\n    payload: run,\n    _msgid: msg._msgid,\n    _uib: msg._uib,\n    _socketId: msg._socketId\n};\nreturn msg1;\n",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 680,
        "y": 200,
        "wires": [
            [
                "825fe14de37c47ab"
            ]
        ]
    },
    {
        "id": "825fe14de37c47ab",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "insert run",
        "x": 940,
        "y": 200,
        "wires": [
            [
                "04a7388b0a0ea4b0"
            ]
        ]
    },
    {
        "id": "04a7388b0a0ea4b0",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Insert lc_assets",
        "func": "const lcDocs = msg._lcDocs || [];\nif (lcDocs.length === 0) {\n    if (msg._runDiag) {\n        var _why = (msg._runDiag.errors || []).some(function(e){ return e.stage === 'validation' || e.stage === 'transport' || e.stage === 'parse'; }) ? 'all catalogues failed or returned no usable data' : '0 valid entries after validation';\n        msg._runDiag.logs.push({ assetId:'', level:'info', step:'insert', code:'no_insert', message:'No assets inserted (' + _why + ').', catalogue:'', timestamp:new Date().toISOString() });\n    }\n    return [null, msg];\n}\nmsg.operation = 'insertMany';\nmsg.collection = 'lc_assets';\nmsg.payload = [lcDocs];\nreturn [msg, null];",
        "outputs": 2,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1140,
        "y": 200,
        "wires": [
            [
                "bf3b4e4071038a0b"
            ],
            [
                "c16a4c309f28dee4"
            ]
        ]
    },
    {
        "id": "bf3b4e4071038a0b",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "insert lc_assets",
        "x": 1337.5,
        "y": 60,
        "wires": [
            [
                "a1b2c3d4e5f60001"
            ]
        ]
    },
    {
        "id": "b142eeaaaa987f87",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Build transformation audit records",
        "func": "const run = msg._harvestRun || {};\nconst docs = Array.isArray(msg._lcDocs) ? msg._lcDocs : [];\nconst results = Array.isArray(msg._transformResults) ? msg._transformResults : [];\nconst now = new Date().toISOString();\nconst operationId = run.uniqueId || ('run-' + Date.now().toString(36));\nconst harvestRunId = run.uniqueId || operationId;\nconst actor = msg._callerUsername || (msg.auth && msg.auth.username) || null;\n\nfunction clean(v) {\n    if (v === undefined || v === null) return null;\n    var s = String(v).trim();\n    return s === '' ? null : s;\n}\n\nconst auditRows = docs.map(function (doc) {\n    const row = results.find(function (r) { return r && r.lcId === doc.id; }) || {\n        strategy: 'none', status: 'skipped', error: 'No transformation result was produced.'\n    };\n    const errors = (row.shaclResult && Array.isArray(row.shaclResult.errors)) ? row.shaclResult.errors : [];\n\n    // NF-8: never coerce a missing identifier to an empty string.\n    const catalogueId  = clean(doc.sourceCatalogueId);\n    const remoteAssetId = clean(doc.remoteAssetId || doc.sourceAssetId);\n    const localAssetId  = clean(doc.id);\n\n    let status = row.status || 'failed';\n    let errorCode = row.errorCode || null;\n    let errorMessage = row.error || null;\n    const validationStatus = row.shaclResult ? (row.shaclResult.valid ? 'pass' : 'fail') : 'not-configured';\n\n    // NF-8: a record may only be 'success' when every required identifier is\n    // present AND validation did not fail. Otherwise it is recorded as failed\n    // with an explicit reason — never relabelled as success.\n    if (status === 'success') {\n        const missing = [];\n        if (!catalogueId)   missing.push('catalogueId');\n        if (!localAssetId)  missing.push('localAssetId');\n        if (!remoteAssetId) missing.push('remoteAssetId');\n        if (!operationId)   missing.push('operationId');\n\n        if (missing.length) {\n            status = 'failed';\n            errorCode = !catalogueId ? 'AUDIT_CATALOGUE_ID_REQUIRED'\n                      : !operationId ? 'AUDIT_OPERATION_ID_REQUIRED'\n                      : 'AUDIT_IDENTIFIER_REQUIRED';\n            errorMessage = 'Audit record rejected as success: missing required identifier(s): ' + missing.join(', ') + '.';\n        } else if (validationStatus === 'fail') {\n            status = 'failed';\n            errorCode = errorCode || 'AUDIT_VALIDATION_FAILED';\n            errorMessage = errorMessage || 'Validation reported a blocking failure for this asset.';\n        }\n    }\n\n    const startedAt = row.startedAt || run.startedAt || now;\n    const durationMs = (row.durationMs != null) ? row.durationMs\n        : (function () { try { return Math.max(0, new Date(now) - new Date(startedAt)); } catch (e) { return null; } })();\n\n    return {\n        // ── canonical (hashed) fields ──\n        id: 'ta-' + Date.now().toString(36) + Math.random().toString(36).slice(2, 8),\n        sequenceNumber: null,               // assigned atomically downstream\n        timestamp: now,\n        eventType: 'transformation',\n        operationId: operationId,\n        harvestRunId: harvestRunId,\n        userId: actor,\n        catalogueId: catalogueId,\n        catalogueNameSnapshot: clean(doc.sourceCatalogue),\n        remoteAssetId: remoteAssetId,\n        localAssetId: localAssetId,\n        promptVersionId: clean(row.promptVersionId || row.promptId),\n        strategy: row.strategy || 'none',\n        status: status,\n        validationStatus: validationStatus,\n        durationMs: durationMs,\n        errorCode: errorCode,\n        errorMessage: errorMessage,\n        hashAlgorithm: 'SHA-256',\n        chainVersion: 1,\n        previousHash: null,                 // assigned atomically downstream\n        recordHash: null,                   // assigned atomically downstream\n\n        // ── contextual (non-canonical) fields kept for existing filters/UI ──\n        runId: run.uniqueId || '',\n        catalogueName: clean(doc.sourceCatalogue),\n        assetIdRemote: remoteAssetId,\n        assetIdLocal: localAssetId,\n        mappingId: row.mappingId || null,\n        promptId: row.promptId || null,\n        providerId: row.providerId || null,\n        namespaces: row.namespaces || [],\n        fallbackTriggered: !!row.fallbackTriggered,\n        fallbackReason: row.fallbackReason || null,\n        validationResult: validationStatus,\n        shaclResult: row.shaclResult || null,\n        error: errorMessage,\n        errorCount: errors.length + (errorMessage ? 1 : 0),\n        startedAt: startedAt,\n        completedAt: now\n    };\n});\n\nif (auditRows.length > 0) {\n    msg._auditPendingRows = auditRows;\n    msg._auditExpectedCount = auditRows.length;\n    msg._auditCasAttempt = 0;\n    return [msg, null];\n}\nreturn [null, msg];",
        "outputs": 2,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1280,
        "y": 60,
        "wires": [
            [
                "nf8ch00000000001"
            ],
            [
                "9d3da29eadaf1d1e"
            ]
        ]
    },
    {
        "id": "6b85ed9cbde579a9",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "DB: Insert Transformation Audit",
        "x": 2040,
        "y": 200,
        "wires": [
            [
                "nf8ch00000000006"
            ]
        ]
    },
    {
        "id": "9d3da29eadaf1d1e",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "response: startHarvest",
        "func": "const run = msg._harvestRun || {};\nconst lcDocs = msg._lcDocs || [];\nconst transformResults = Array.isArray(msg._transformResults) ? msg._transformResults : [];\nconst auditError = msg._auditWriteError || null;\nconst auditRowsWritten = msg._auditRowsWritten || 0;\ndelete msg._harvestRun;\ndelete msg._lcDocs;\ndelete msg._runDiag;\ndelete msg._transformResults;\ndelete msg._auditWriteError;\ndelete msg._auditRowsWritten;\ndelete msg._auditChainFrom;\ndelete msg._auditChainTo;\ndelete msg._auditChainLastHash;\n\n// NF-6: a required transformation that failed, was skipped, or produced no\n// transformed form must never be reported as a successful harvest.\nconst failedTransforms = transformResults.filter(function (r) {\n    if (!r) return false;\n    if (r.status === 'failed') return true;\n    if (r.status === 'success' && (r.transformedForm === null || r.transformedForm === undefined)) return true;\n    return r.status === 'skipped' && String(r.strategy || 'none').toLowerCase() !== 'none';\n});\nconst transformedOk = transformResults.filter(function (r) {\n    return r && r.status === 'success' && r.transformedForm;\n}).length;\n\nconst errorCount = run.errorCount || 0;\nconst warningCount = run.warningCount || 0;\nconst hasErrors = errorCount > 0 || failedTransforms.length > 0;\n// A run that stopped early — for example on the OAI-PMH page limit — is partial\n// and must not be announced as a plain success.\nconst isPartial = run.result === 'Partial' || (!hasErrors && warningCount > 0);\nlet status = hasErrors ? (run.result === 'Failed' ? 'error' : 'warning') : (isPartial ? 'warning' : 'success');\nlet ok = run.result !== 'Failed';\nlet message = hasErrors\n    ? ('Harvest completed with ' + errorCount + ' error(s)' + (warningCount > 0 ? ' and ' + warningCount + ' warning(s)' : '') + '.')\n    : (isPartial\n        ? ('Harvest partially completed: ' + warningCount + ' protocol warning(s); ' + (run.importedCount || run.totalAssets || 0) + ' asset(s) imported.')\n        : 'Harvest completed successfully.');\n\nif (failedTransforms.length > 0) {\n    ok = transformedOk > 0;\n    status = transformedOk > 0 ? 'warning' : 'error';\n    message = transformedOk > 0\n        ? ('Harvest completed with ' + failedTransforms.length + ' failed transformation(s) of ' + transformResults.length + '.')\n        : ('Harvest failed: no asset could be transformed (' + failedTransforms.length + ' failure(s)).');\n}\n\n// NF-8: a harvest is only complete once its audit evidence is durable.\nif (auditError) {\n    status = 'error';\n    ok = false;\n    message = 'Harvest data was imported but the audit trail write failed: ' + auditError.message;\n}\n\nmsg.response = {\n    action: 'startHarvest',\n    status: status,\n    ok: ok,\n    run: run,\n    allAssets: lcDocs,\n    allProvenance: run.provenance || [],\n    totalAssets: run.totalAssets || 0,\n    errorCount: errorCount,\n    warningCount: warningCount,\n    warnings: run.warnings || [],\n    partial: isPartial,\n    result: run.result || '',\n    runStatus: run.status || '',\n    errors: run.errors || [],\n    transformationsTotal: transformResults.length,\n    transformationsSucceeded: transformedOk,\n    transformationsFailed: failedTransforms.length,\n    auditRowsWritten: auditRowsWritten,\n    auditError: auditError ? auditError.message : null,\n    errorCode: auditError ? auditError.code : null,\n    message: message,\n    totalRuns: 1\n};\ndelete msg.payload;\nreturn msg;\n",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1777.5,
        "y": 60,
        "wires": [
            [
                "dc56327bec3493ee"
            ]
        ]
    },
    {
        "id": "242b5761230f6a22",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "List Harvest Runs",
        "func": "msg.operation = 'find';\nmsg.collection = 'hv_runs';\nmsg.payload = [{}];\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 650,
        "y": 300,
        "wires": [
            [
                "b88fcf5c9a2a1d53"
            ]
        ]
    },
    {
        "id": "b88fcf5c9a2a1d53",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "",
        "x": 940,
        "y": 300,
        "wires": [
            [
                "1e499bc6e1334cac"
            ]
        ]
    },
    {
        "id": "1e499bc6e1334cac",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "response: listHarvestRuns",
        "func": "const docs = Array.isArray(msg.payload) ? msg.payload : [];\nconst runs = docs.map(d => {\n    const r = Object.assign({}, d);\n    delete r.provenance;\n    delete r.logs;\n    delete r.assets;\n    return r;\n});\nruns.sort((a, b) => (b.startedAt || '').localeCompare(a.startedAt || ''));\nmsg.response = {\n    action: 'listHarvestRuns',\n    status: 'success',\n    runs: runs\n};\ndelete msg.payload;\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1160,
        "y": 300,
        "wires": [
            [
                "dc56327bec3493ee"
            ]
        ]
    },
    {
        "id": "f18c93fa59ef3747",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Get Harvest Run Detail",
        "func": "msg.operation = 'findOne';\nmsg.collection = 'hv_runs';\nmsg.payload = { uniqueId: msg.data.runId };\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 670,
        "y": 400,
        "wires": [
            [
                "6afa7edc22a83295"
            ]
        ]
    },
    {
        "id": "6afa7edc22a83295",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "",
        "x": 940,
        "y": 400,
        "wires": [
            [
                "4462e669cc89ebc8"
            ]
        ]
    },
    {
        "id": "4462e669cc89ebc8",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Stash run + fetch lc_assets",
        "func": "const run = msg.payload || null;\n\nif (!run) {\n    msg.response = {\n        action: 'getHarvestRunDetail',\n        status: 'success',\n        run: null,\n        logs: [],\n        provenance: []\n    };\n    delete msg.payload;\n    return [null, msg];\n}\n\n// Stash for the response builder downstream\nmsg._hvRunDoc = run;\n\n// Fetch the lc_assets imported in this run, ordered by creation\nmsg.collection = 'lc_assets';\nmsg.operation  = 'find';\nmsg.payload    = [{ harvestRunId: run.uniqueId }, { sort: { createdAt: 1 }, projection: {\n    _id: 0,\n    id: 1,\n    uniqueId: 1,\n    title: 1,\n    assets: 1,\n    type: 1,\n    sourceCatalogue: 1,\n    sourceCatalogueId: 1,\n    remoteAssetId: 1,\n    harvestedAt: 1,\n    transformedForm: 1,\n    transformedByMappingId: 1,\n    transformedByPromptId: 1,\n    transformedByProviderId: 1\n} }];\nreturn [msg, null];",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1170,
        "y": 400,
        "wires": [
            [
                "d6e453dd40e806d1"
            ]
        ]
    },
    {
        "id": "c087575419068b1f",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "List Harvest Logs",
        "func": "msg.operation = 'find';\nmsg.collection = 'hv_runs';\nmsg.payload = [{}];\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 650,
        "y": 500,
        "wires": [
            [
                "0934f7b8a60cea6b"
            ]
        ]
    },
    {
        "id": "0934f7b8a60cea6b",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "",
        "x": 940,
        "y": 500,
        "wires": [
            [
                "6fa2ed6353a308b3"
            ]
        ]
    },
    {
        "id": "6fa2ed6353a308b3",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "response: listHarvestLogs — extract from runs",
        "func": "const docs = Array.isArray(msg.payload) ? msg.payload : [];\nconst logs = [];\nfor (const d of docs) {\n    if (Array.isArray(d.logs)) {\n        for (const l of d.logs) logs.push(l);\n    }\n}\nmsg.response = {\n    action: 'listHarvestLogs',\n    status: 'success',\n    logs: logs\n};\ndelete msg.payload;\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1640,
        "y": 460,
        "wires": [
            [
                "dc56327bec3493ee"
            ]
        ]
    },
    {
        "id": "c834493788a475e4",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "List Harvest Provenance",
        "func": "msg.operation = 'find';\nmsg.collection = 'hv_runs';\nmsg.payload = [{}];\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 670,
        "y": 600,
        "wires": [
            [
                "473d590aac2f0865"
            ]
        ]
    },
    {
        "id": "473d590aac2f0865",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "",
        "x": 940,
        "y": 600,
        "wires": [
            [
                "edc50167a4715051"
            ]
        ]
    },
    {
        "id": "edc50167a4715051",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "response: listHarvestProvenance — extract from runs",
        "func": "const docs = Array.isArray(msg.payload) ? msg.payload : [];\n\n// One field mapping for every provenance view. Older rows only carry runId,\n// timestamp and catalogue, so the canonical names are filled in from the run.\nfunction provenanceRow(p, run) {\n    const catName = p.sourceCatalogueName || p.catalogue || p.remoteCatalogueName || '';\n    const catId = p.sourceCatalogueId || p.catalogueId || p.remoteCatalogueId || '';\n    const runId = p.harvestRunId || p.runId || (run && run.uniqueId) || '';\n    const at = p.harvestedAt || p.timestamp || p.harvestDate || (run && run.startedAt) || '';\n    const strategy = p.transformationStrategy || p.strategy || '';\n    const evidence = (run && Array.isArray(run.protocolEvidence)) ? run.protocolEvidence : [];\n    const ev = evidence.find(e => e && ((catId && e.catalogueId === catId) || (catName && e.catalogueName === catName)));\n    return Object.assign({}, p, {\n        runId: runId,\n        harvestRunId: runId,\n        catalogue: catName,\n        catalogueName: catName,\n        sourceCatalogueName: catName,\n        remoteCatalogueName: catName,\n        sourceCatalogueId: catId,\n        remoteCatalogueId: catId,\n        harvestedAt: at,\n        harvestDate: at,\n        timestamp: at,\n        protocol: p.protocol || (ev && ev.protocol) || '',\n        strategy: strategy,\n        transformationStrategy: strategy\n    });\n}\n\nconst provenance = [];\nfor (const d of docs) {\n    if (Array.isArray(d.provenance)) {\n        for (const p of d.provenance) provenance.push(provenanceRow(p, d));\n    }\n}\nmsg.response = {\n    action: 'listHarvestProvenance',\n    status: 'success',\n    provenance: provenance\n};\ndelete msg.payload;\nreturn msg;\n",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1220,
        "y": 600,
        "wires": [
            [
                "dc56327bec3493ee"
            ]
        ]
    },
    {
        "id": "ede557980f593bbb",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Pause Harvest",
        "func": "msg.operation = 'updateOne';\nmsg.collection = 'hv_runs';\nmsg.payload = [\n    { uniqueId: msg.data.runId },\n    { $set: { status: 'paused' } }\n];\nmsg._action = 'pauseHarvest';\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 640,
        "y": 700,
        "wires": [
            [
                "8c2a80e95b1e44b5"
            ]
        ]
    },
    {
        "id": "a73789af29e050c5",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Resume Harvest",
        "func": "msg.operation = 'updateOne';\nmsg.collection = 'hv_runs';\nmsg.payload = [\n    { uniqueId: msg.data.runId },\n    { $set: { status: 'running' } }\n];\nmsg._action = 'resumeHarvest';\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 650,
        "y": 760,
        "wires": [
            [
                "8c2a80e95b1e44b5"
            ]
        ]
    },
    {
        "id": "972bdb7c2c30a398",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Cancel Harvest",
        "func": "msg.operation = 'updateOne';\nmsg.collection = 'hv_runs';\nmsg.payload = [\n    { uniqueId: msg.data.runId },\n    { $set: { status: 'cancelled' } }\n];\nmsg._action = 'cancelHarvest';\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 497.5,
        "y": 1020,
        "wires": [
            [
                "8c2a80e95b1e44b5"
            ]
        ]
    },
    {
        "id": "8c2a80e95b1e44b5",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "",
        "x": 940,
        "y": 760,
        "wires": [
            [
                "372be7bea6082ea6"
            ]
        ]
    },
    {
        "id": "372be7bea6082ea6",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "response: pause/resume/cancel",
        "func": "msg.response = {\n    action: msg._action || 'harvestControl',\n    status: 'success'\n};\ndelete msg._action;\ndelete msg.payload;\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1170,
        "y": 760,
        "wires": [
            [
                "dc56327bec3493ee"
            ]
        ]
    },
    {
        "id": "9736dc335563a8e8",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Resolve API sources",
        "func": "// FR-CR-01 / NF-2: protocol-aware request planning for query-interface, dcat\n// and oai-pmh. FR-CR-03 catalogue API mappings still apply to REST harvests.\nvar catalogues = (msg.data && msg.data.catalogues) ? msg.data.catalogues : [];\nvar queue = [];\n\n// ── NF-2 protocol library (shared, must stay identical in every node) ──────\n// Normalised protocol values: 'query-interface' | 'dcat' | 'oai-pmh'.\nfunction protoNormalise(value) {\n    var s = String(value == null ? '' : value).trim().toLowerCase();\n    if (!s) return 'query-interface';\n    s = s.replace(/[\\s_]+/g, '-');\n    if (s === 'oai-pmh' || s === 'oaipmh' || s === 'oai') return 'oai-pmh';\n    if (s === 'dcat' || s === 'dcat-ap' || s === 'linked-data') return 'dcat';\n    if (s === 'query-interface' || s === 'query' || s === 'sparql' || s === 'query-interfaces') return 'query-interface';\n    return s;\n}\nfunction protoIsSupported(p) {\n    return p === 'query-interface' || p === 'dcat' || p === 'oai-pmh';\n}\nfunction protoText(v) {\n    if (v === undefined || v === null) return '';\n    return String(v).trim();\n}\n// 123 and 91 are the character codes of the JSON object and array openers.\nfunction looksLikeJsonText(s) {\n    var code = String(s == null ? '' : s).trim().charCodeAt(0);\n    return code === 123 || code === 91;\n}\nfunction protoIsHttpUrl(u) {\n    return typeof u === 'string' && /^https?:\\/\\/\\S+$/i.test(u.trim());\n}\nfunction protoQueryLanguage(cat) {\n    var q = protoText(cat.queryLanguage || cat.queryLanguages).toLowerCase();\n    if (!q) return 'rest-json';\n    if (/sparql/.test(q)) return 'sparql';\n    if (/graphql/.test(q)) return 'graphql';\n    if (/rest|json|search|custom$/.test(q)) return 'rest-json';\n    return q;\n}\n// A query language that actually executes a query text, as opposed to a plain\n// REST endpoint that is simply fetched.\nfunction protoIsExecutableQuery(lang) {\n    return lang === 'sparql' || lang === 'graphql' || lang === 'custom-query';\n}\nfunction protoMaxPages(cat) {\n    var n = parseInt(cat.maxPages, 10);\n    if (!isFinite(n) || n <= 0) return 50;\n    return Math.min(n, 1000);\n}\nfunction protoRdfMime(ct) {\n    return /ld\\+json|turtle|n-quads|nquads|n-triples|ntriples|trig|rdf\\+xml/i.test(ct || '');\n}\n// A REST/JSON catalogue keeps the MIME type configured on the catalogue itself.\n// The SPARQL result MIME the registration form pre-fills for query languages\n// must never override it.\nfunction protoRestMime(cat) {\n    var configured = protoText(cat.mimeType);\n    if (configured) return configured;\n    var result = protoText(cat.resultMimeType);\n    if (result && !/sparql-results/i.test(result)) return result;\n    return 'application/json';\n}\nfunction protoXmlMime(ct) {\n    return /xml/i.test(ct || '');\n}\nfunction protoAddQuery(url, params) {\n    var parts = [];\n    Object.keys(params).forEach(function (k) {\n        var v = params[k];\n        if (v === undefined || v === null || v === '') return;\n        parts.push(encodeURIComponent(k) + '=' + encodeURIComponent(String(v)));\n    });\n    if (!parts.length) return url;\n    return url + (url.indexOf('?') >= 0 ? '&' : '?') + parts.join('&');\n}\n// Builds the OAI-PMH request URL. A resumption token is exclusive: the spec\n// forbids sending metadataPrefix or set alongside it.\nfunction protoOaiUrl(base, opts) {\n    if (opts && opts.resumptionToken) {\n        return protoAddQuery(base, { verb: 'ListRecords', resumptionToken: opts.resumptionToken });\n    }\n    return protoAddQuery(base, {\n        verb: 'ListRecords',\n        metadataPrefix: (opts && opts.metadataPrefix) || 'oai_dc',\n        set: (opts && opts.setSpec) || ''\n    });\n}\n// Namespace-tolerant XML helpers. Node-RED function nodes have no XML parser\n// available, so the OAI envelope is read with anchored tag scans.\nfunction protoTag(name) {\n    return '(?:[A-Za-z0-9_.-]+:)?' + name;\n}\nfunction protoFindBlocks(xml, name) {\n    var out = [];\n    var re = new RegExp('<' + protoTag(name) + '(\\\\s[^>]*)?>([\\\\s\\\\S]*?)<\\\\/' + protoTag(name) + '>', 'g');\n    var m;\n    while ((m = re.exec(xml)) !== null) out.push({ attrs: m[1] || '', inner: m[2], whole: m[0] });\n    return out;\n}\nfunction protoFirstBlock(xml, name) {\n    var blocks = protoFindBlocks(xml, name);\n    return blocks.length ? blocks[0] : null;\n}\nfunction protoTagText(xml, name) {\n    var b = protoFirstBlock(xml, name);\n    if (!b) return '';\n    return protoDecodeXml(b.inner.replace(/<[^>]*>/g, '')).trim();\n}\nfunction protoSelfClosing(xml, name) {\n    var re = new RegExp('<' + protoTag(name) + '(\\\\s[^>]*)?\\\\/>');\n    var m = re.exec(xml);\n    return m ? { attrs: m[1] || '', inner: '', whole: m[0] } : null;\n}\nfunction protoAttr(attrs, name) {\n    var m = new RegExp(name + '\\\\s*=\\\\s*\"([^\"]*)\"').exec(attrs || '');\n    if (m) return m[1];\n    m = new RegExp(name + \"\\\\s*=\\\\s*'([^']*)'\").exec(attrs || '');\n    return m ? m[1] : '';\n}\nfunction protoDecodeXml(s) {\n    return String(s == null ? '' : s)\n        .replace(/&lt;/g, '<').replace(/&gt;/g, '>')\n        .replace(/&quot;/g, '\"').replace(/&apos;/g, \"'\")\n        .replace(/&#(\\d+);/g, function (_, d) { return String.fromCharCode(parseInt(d, 10)); })\n        .replace(/&amp;/g, '&');\n}\n\n// NF-4 RC-5: run-scoped diagnostic accumulator + config validation\nif (!msg._runDiag) { msg._runDiag = { errors: [], logs: [], catalogueStatus: {}, counters: { transportErrors: 0, parseErrors: 0, validationErrors: 0, entryErrors: 0, persistenceErrors: 0 } }; }\nvar _rd = msg._runDiag;\nfunction _nowISO() { return new Date().toISOString(); }\nfunction _redact(u){ try{ if(!u) return ''; var s=String(u); s=s.replace(/\\/\\/[^/@]*@/, '//'); var q=s.indexOf('?'); if(q>=0) s=s.slice(0,q)+'?[redacted]'; return s; }catch(e){ return ''; } }\nfunction _rejectCat(cat, catId, code, message) {\n    _rd.errors.push({ stage: 'validation', step: 'resolve API sources', code: code, level: 'error', catalogueId: catId, catalogueName: cat.catalogName || '', endpoint: _redact(cat.baseEndpoint || cat.queryEndpoint || ''), assetId: null, retryable: false, message: message, detail: { protocol: cat.protocolNormalised || '' }, timestamp: _nowISO() });\n    _rd.logs.push({ assetId: '', level: 'error', step: 'resolve API sources', code: code, message: message, catalogue: cat.catalogName || '', timestamp: _nowISO() });\n    _rd.catalogueStatus[catId] = 'invalid';\n    _rd.counters.validationErrors++;\n}\n\nfor (var i = 0; i < catalogues.length; i++) {\n    var cat = catalogues[i];\n    if (cat.sourceData) continue;\n\n    var _catId = cat.uniqueId || cat.catalogId || cat.id || ('cat-' + i);\n    var protocol = protoNormalise(cat.protocol);\n    cat.protocolNormalised = protocol;\n\n    if (!protoIsSupported(protocol)) {\n        _rejectCat(cat, _catId, 'PROTOCOL_UNSUPPORTED', 'Catalogue \"' + (cat.catalogName || _catId) + '\" uses unsupported protocol \"' + protoText(cat.protocol) + '\". Supported: query-interface, dcat, oai-pmh.');\n        continue;\n    }\n\n    var queryLang = protoQueryLanguage(cat);\n    var executable = protocol === 'query-interface' && protoIsExecutableQuery(queryLang);\n\n    // Protocol-specific endpoint selection.\n    var endpoint = '';\n    if (protocol === 'oai-pmh') endpoint = protoText(cat.baseEndpoint);\n    else if (protocol === 'dcat') endpoint = protoText(cat.dcatCatalogUri) || protoText(cat.baseEndpoint) || protoText(cat.linkedDataEndpoint);\n    else endpoint = protoText(cat.queryEndpoint) || protoText(cat.baseEndpoint);\n\n    if (!endpoint) { _rejectCat(cat, _catId, 'CATALOGUE_CONFIG_INVALID', 'Catalogue \"' + (cat.catalogName || _catId) + '\" has no endpoint configured for protocol ' + protocol + '; nothing to harvest.'); continue; }\n    if (!protoIsHttpUrl(endpoint)) { _rejectCat(cat, _catId, 'CATALOGUE_CONFIG_INVALID', 'Catalogue \"' + (cat.catalogName || _catId) + '\" endpoint \"' + endpoint + '\" is not a valid absolute http(s) URL.'); continue; }\n\n    var _a = cat.auth || 'none';\n    if (_a === 'token-login' && (!cat.authLoginEndpoint || !cat.authUsername || !cat.authPassword)) { _rejectCat(cat, _catId, 'CATALOGUE_CONFIG_INVALID', 'Catalogue \"' + (cat.catalogName || _catId) + '\" token-login auth is missing login endpoint, username or password.'); continue; }\n    if (_a === 'static-token' && !cat.authStaticToken) { _rejectCat(cat, _catId, 'CATALOGUE_CONFIG_INVALID', 'Catalogue \"' + (cat.catalogName || _catId) + '\" static-token auth is missing the token.'); continue; }\n    if (_a === 'api-key' && !cat.authApiKey) { _rejectCat(cat, _catId, 'CATALOGUE_CONFIG_INVALID', 'Catalogue \"' + (cat.catalogName || _catId) + '\" api-key auth is missing the API key.'); continue; }\n\n    if (protocol === 'oai-pmh' && !protoText(cat.metadataPrefix)) {\n        _rejectCat(cat, _catId, 'OAI_METADATA_PREFIX_REQUIRED', 'Catalogue \"' + (cat.catalogName || _catId) + '\" is OAI-PMH but has no metadataPrefix configured.');\n        continue;\n    }\n    if (executable) {\n        var qProblems = [];\n        if (!protoText(cat.queryText)) qProblems.push('no query text');\n        var method = protoText(cat.queryMethod).toUpperCase() || 'GET';\n        if (method !== 'GET' && method !== 'POST') qProblems.push('unsupported method \"' + method + '\"');\n        if (method === 'GET' && !protoText(cat.queryParameterName)) qProblems.push('no query parameter name for GET');\n        if (method === 'POST' && protoText(cat.requestContentType).toLowerCase().indexOf('x-www-form-urlencoded') >= 0 && !protoText(cat.queryParameterName)) qProblems.push('no query parameter name for form POST');\n        if (!protoText(cat.resultMimeType)) qProblems.push('no expected result MIME type');\n        if (qProblems.length) {\n            _rejectCat(cat, _catId, 'QUERY_CONFIGURATION_INVALID', 'Catalogue \"' + (cat.catalogName || _catId) + '\" query configuration is incomplete: ' + qProblems.join(', ') + '.');\n            continue;\n        }\n    }\n\n    var authType = cat.auth || 'none';\n    if (authType === 'token-login' && cat.authLoginEndpoint) {\n        var body = (cat.authPayloadTemplate || '{\"email\":\"{{username}}\",\"password\":\"{{password}}\"}').replace('{{username}}', cat.authUsername || '').replace('{{password}}', cat.authPassword || '');\n        queue.push({ step: 'login', catIdx: i, url: cat.authLoginEndpoint, hdrs: { 'Content-Type': 'application/json', 'Accept': 'application/json' }, body: body, tokenPath: cat.authTokenPath || 'token', tokenPrefix: cat.authTokenPrefix || 'Bearer' });\n    }\n\n    var authHeaders = function (base) {\n        var h = Object.assign({}, base);\n        if (authType === 'static-token' && cat.authStaticToken) h['Authorization'] = (cat.authTokenPrefix || 'Bearer') + ' ' + cat.authStaticToken;\n        else if (authType === 'api-key' && cat.authApiKey) h[cat.authApiKeyHeader || 'X-API-Key'] = cat.authApiKey;\n        return h;\n    };\n\n    // Runtime evidence, filled in by the response handler.\n    cat.protocolEvidence = {\n        protocol: protocol,\n        endpoint: _redact(endpoint),\n        pages: 0,\n        remoteRecords: 0,\n        errors: []\n    };\n\n    if (protocol === 'oai-pmh') {\n        var oai = {\n            metadataPrefix: protoText(cat.metadataPrefix),\n            setSpec: protoText(cat.setSpec),\n            resumptionTokenEnabled: cat.resumptionTokenEnabled !== false && cat.resumptionToken !== false,\n            maxPages: protoMaxPages(cat)\n        };\n        cat.protocolEvidence.metadataPrefix = oai.metadataPrefix;\n        cat.protocolEvidence.setSpec = oai.setSpec;\n        cat.protocolEvidence.resumptionTokensFollowed = false;\n        cat.protocolEvidence.deletedRecords = 0;\n        queue.push({\n            step: 'data', protocol: protocol, catIdx: i, catName: cat.catalogName || '',\n            url: protoOaiUrl(endpoint, { metadataPrefix: oai.metadataPrefix, setSpec: oai.setSpec }),\n            method: 'GET', body: '',\n            hdrs: authHeaders({ 'Accept': 'text/xml, application/xml' }),\n            requestedMime: 'text/xml',\n            oai: { base: endpoint, page: 1, seenTokens: [], config: oai }\n        });\n    } else if (protocol === 'dcat') {\n        var dcatMime = protoText(cat.mimeType) || protoText(cat.contentNegotiation) ||\n            'application/ld+json, text/turtle, application/n-quads, application/n-triples, application/trig, application/rdf+xml';\n        queue.push({\n            step: 'data', protocol: protocol, catIdx: i, catName: cat.catalogName || '',\n            url: endpoint, method: 'GET', body: '',\n            hdrs: authHeaders({ 'Accept': dcatMime }),\n            requestedMime: dcatMime\n        });\n    } else if (executable) {\n        var qMethod = protoText(cat.queryMethod).toUpperCase() || 'GET';\n        var accept = protoText(cat.resultMimeType);\n        var qText = protoText(cat.queryText);\n        var paramName = protoText(cat.queryParameterName) || 'query';\n        var item = {\n            step: 'data', protocol: protocol, catIdx: i, catName: cat.catalogName || '',\n            method: qMethod, requestedMime: accept,\n            query: {\n                language: queryLang, method: qMethod, text: qText, parameterName: paramName,\n                resultMode: protoText(cat.resultMode) || 'sparql-select',\n                rootPath: protoText(cat.resultRootPath),\n                idVar: protoText(cat.resultIdVariable),\n                nameVar: protoText(cat.resultNameVariable),\n                typeVar: protoText(cat.resultTypeVariable),\n                endpoint: endpoint\n            }\n        };\n        cat.protocolEvidence.queryLanguage = queryLang;\n        cat.protocolEvidence.queryMethod = qMethod;\n        cat.protocolEvidence.query = qText;\n        cat.protocolEvidence.resultMimeType = accept;\n        if (qMethod === 'GET') {\n            var params = {};\n            params[paramName] = qText;\n            item.url = protoAddQuery(endpoint, params);\n            item.body = '';\n            item.hdrs = authHeaders({ 'Accept': accept });\n        } else {\n            var ct = protoText(cat.requestContentType) || 'application/sparql-query';\n            item.url = endpoint;\n            item.hdrs = authHeaders({ 'Accept': accept, 'Content-Type': ct });\n            if (ct.toLowerCase().indexOf('x-www-form-urlencoded') >= 0) {\n                item.body = encodeURIComponent(paramName) + '=' + encodeURIComponent(qText);\n            } else {\n                item.body = qText;\n            }\n        }\n        queue.push(item);\n    } else {\n        // Plain REST/JSON query interface: the historical FACIS behaviour, kept\n        // intact so existing catalogues continue to harvest.\n        var requestedMime = protoRestMime(cat);\n        var hdrs = authHeaders({ 'Accept': requestedMime, 'Content-Type': 'application/json' });\n        var scope = (msg.data && msg.data.scope) || {};\n        var typeIds = Array.isArray(scope.types) ? scope.types : [];\n        var mappings = Array.isArray(msg._apiMappings) ? msg._apiMappings : [];\n        var catId = cat.uniqueId || cat.catalogId || cat.id || '';\n        var mapping = null;\n        if (typeIds.length > 0 && mappings.length > 0 && catId) {\n            for (var ti = 0; ti < typeIds.length && !mapping; ti++) {\n                mapping = mappings.find(function (m) { return m.catalogueId === catId && m.localTypeId === typeIds[ti] && (m.status === 'active' || !m.status); });\n            }\n        }\n        cat.protocolEvidence.queryLanguage = 'rest-json';\n        var restItem;\n        if (mapping && mapping.apiRequest) {\n            var apiReq = mapping.apiRequest || {};\n            var base = endpoint.replace(/\\/$/, '');\n            var path = apiReq.pathTemplate || '';\n            var qparams = apiReq.queryParams || {};\n            var qsParts = [];\n            Object.keys(qparams).forEach(function (k) { qsParts.push(encodeURIComponent(k) + '=' + encodeURIComponent(qparams[k])); });\n            var qs = qsParts.join('&');\n            var mappedUrl = base + path + (qs ? (path.indexOf('?') >= 0 ? '&' : '?') + qs : '');\n            restItem = { step: 'data', protocol: protocol, catIdx: i, url: mappedUrl, hdrs: Object.assign({}, hdrs, apiReq.headers || {}), body: apiReq.bodyTemplate ? (typeof apiReq.bodyTemplate === 'string' ? apiReq.bodyTemplate : JSON.stringify(apiReq.bodyTemplate)) : '', method: apiReq.method || 'GET', catName: cat.catalogName || '', apiMappingId: mapping.uniqueId, requestedMime: requestedMime };\n        } else {\n            restItem = { step: 'data', protocol: protocol, catIdx: i, url: endpoint, hdrs: hdrs, body: '', method: 'GET', catName: cat.catalogName || '', requestedMime: requestedMime };\n        }\n        queue.push(restItem);\n        cat.protocolEvidence.queryMethod = restItem.method;\n        cat.protocolEvidence.endpoint = _redact(restItem.url);\n        cat.protocolEvidence.resultMimeType = requestedMime;\n        cat.protocolEvidence.apiMappingId = restItem.apiMappingId || '';\n        cat.protocolEvidence.responseMapping = {\n            rootPath: protoText(cat.responseRootPath),\n            idField: protoText(cat.responseAssetIdField),\n            nameField: protoText(cat.responseAssetNameField),\n            typeField: protoText(cat.responseAssetTypeField)\n        };\n    }\n}\n\nif (queue.length === 0) {\n    return [null, msg];\n}\n\nmsg._fetchQueue = queue;\nmsg._fetchIdx = 0;\nvar first = queue[0];\nmsg.url = first.url;\nmsg.method = first.method || 'GET';\nmsg.headers = first.hdrs;\nmsg.payload = first.body || '';\nmsg.requestTimeout = 15000; // NF-4: explicit fetch timeout\nnode.warn('[harvest] Fetching ' + (first.protocol || 'login') + ': ' + first.url);\nreturn [msg, null];\n",
        "outputs": 2,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 680,
        "y": 150,
        "wires": [
            [
                "2d6d4f7aa11eeeaa"
            ],
            [
                "478e4cbe3927bbd4"
            ]
        ]
    },
    {
        "id": "2d6d4f7aa11eeeaa",
        "type": "http request",
        "z": "a4fcd945f4957d68",
        "name": "Fetch Catalogue API",
        "method": "use",
        "ret": "txt",
        "paytoqs": "ignore",
        "url": "",
        "tls": "",
        "persist": false,
        "proxy": "",
        "insecureHTTPParser": false,
        "authType": "",
        "senderr": true,
        "headers": [],
        "x": 797.5,
        "y": 280,
        "wires": [
            [
                "5e5b280d414c2c59"
            ]
        ]
    },
    {
        "id": "5e5b280d414c2c59",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Handle API response + loop",
        "func": "var queue = msg._fetchQueue || [];\nvar idx = msg._fetchIdx || 0;\nvar current = queue[idx];\nvar catalogues = (msg.data && msg.data.catalogues) ? msg.data.catalogues : [];\n\n// ── NF-2 protocol library (shared, must stay identical in every node) ──────\n// Normalised protocol values: 'query-interface' | 'dcat' | 'oai-pmh'.\nfunction protoNormalise(value) {\n    var s = String(value == null ? '' : value).trim().toLowerCase();\n    if (!s) return 'query-interface';\n    s = s.replace(/[\\s_]+/g, '-');\n    if (s === 'oai-pmh' || s === 'oaipmh' || s === 'oai') return 'oai-pmh';\n    if (s === 'dcat' || s === 'dcat-ap' || s === 'linked-data') return 'dcat';\n    if (s === 'query-interface' || s === 'query' || s === 'sparql' || s === 'query-interfaces') return 'query-interface';\n    return s;\n}\nfunction protoIsSupported(p) {\n    return p === 'query-interface' || p === 'dcat' || p === 'oai-pmh';\n}\nfunction protoText(v) {\n    if (v === undefined || v === null) return '';\n    return String(v).trim();\n}\n// 123 and 91 are the character codes of the JSON object and array openers.\nfunction looksLikeJsonText(s) {\n    var code = String(s == null ? '' : s).trim().charCodeAt(0);\n    return code === 123 || code === 91;\n}\nfunction protoIsHttpUrl(u) {\n    return typeof u === 'string' && /^https?:\\/\\/\\S+$/i.test(u.trim());\n}\nfunction protoQueryLanguage(cat) {\n    var q = protoText(cat.queryLanguage || cat.queryLanguages).toLowerCase();\n    if (!q) return 'rest-json';\n    if (/sparql/.test(q)) return 'sparql';\n    if (/graphql/.test(q)) return 'graphql';\n    if (/rest|json|search|custom$/.test(q)) return 'rest-json';\n    return q;\n}\n// A query language that actually executes a query text, as opposed to a plain\n// REST endpoint that is simply fetched.\nfunction protoIsExecutableQuery(lang) {\n    return lang === 'sparql' || lang === 'graphql' || lang === 'custom-query';\n}\nfunction protoMaxPages(cat) {\n    var n = parseInt(cat.maxPages, 10);\n    if (!isFinite(n) || n <= 0) return 50;\n    return Math.min(n, 1000);\n}\nfunction protoRdfMime(ct) {\n    return /ld\\+json|turtle|n-quads|nquads|n-triples|ntriples|trig|rdf\\+xml/i.test(ct || '');\n}\nvar RDF_CANONICAL_TYPES = {\n    'text/turtle': 'text/turtle', 'application/x-turtle': 'text/turtle',\n    'turtle': 'text/turtle', 'ttl': 'text/turtle',\n    'application/n-triples': 'application/n-triples', 'ntriples': 'application/n-triples', 'nt': 'application/n-triples',\n    'application/n-quads': 'application/n-quads', 'nquads': 'application/n-quads', 'nq': 'application/n-quads',\n    'application/trig': 'application/trig', 'trig': 'application/trig',\n    'application/ld+json': 'application/ld+json', 'jsonld': 'application/ld+json', 'json-ld': 'application/ld+json',\n    'application/rdf+xml': 'application/rdf+xml', 'rdfxml': 'application/rdf+xml', 'rdf/xml': 'application/rdf+xml'\n};\nvar RDF_TITLE_PREDICATES = [\n    'http://purl.org/dc/terms/title',\n    'http://purl.org/dc/elements/1.1/title',\n    'http://www.w3.org/2000/01/rdf-schema#label',\n    'http://schema.org/name',\n    'http://xmlns.com/foaf/0.1/name'\n];\nvar RDF_PRIMARY_TYPES = [\n    'http://www.w3.org/ns/dcat#Catalog',\n    'http://www.w3.org/ns/dcat#Dataset',\n    'http://www.w3.org/ns/dcat#DataService',\n    'http://www.w3.org/ns/dcat#Distribution'\n];\nfunction rdfCanonicalType(ct) {\n    var key = String(ct == null ? '' : ct).split(';')[0].trim().toLowerCase();\n    return RDF_CANONICAL_TYPES[key] || '';\n}\nfunction rdfIsHtml(text, ct) {\n    if (/text\\/html/i.test(ct || '')) return true;\n    return /^\\s*<(?:!doctype|html)[\\s>]/i.test(String(text == null ? '' : text));\n}\nfunction rdfSniffType(text) {\n    var s = String(text == null ? '' : text).trim();\n    if (!s) return '';\n    if (/^<\\?xml/i.test(s) || /<rdf:RDF[\\s>]/i.test(s)) return 'application/rdf+xml';\n    var first = s.charAt(0);\n    if (first === '[' || first === String.fromCharCode(123)) return 'application/ld+json';\n    if (/@prefix\\s|@base\\s/i.test(s) || /^\\s*PREFIX\\s/im.test(s)) return 'text/turtle';\n    if (/^\\s*(?:<[^>\\s]*>|_:\\S+)\\s+<[^>\\s]*>\\s/m.test(s)) return 'application/n-triples';\n    return '';\n}\nfunction rdfJsonLdStatements(text) {\n    var doc = JSON.parse(text);\n    var nodes = Array.isArray(doc) ? doc : (doc && Array.isArray(doc['@graph']) ? doc['@graph'] : [doc]);\n    var linked = !Array.isArray(doc) && doc && doc['@context'] !== undefined;\n    var count = 0;\n    var subject = '';\n    var title = '';\n    for (var i = 0; i < nodes.length; i++) {\n        var node = nodes[i];\n        if (!node || typeof node !== 'object') continue;\n        var keys = Object.keys(node);\n        for (var k = 0; k < keys.length; k++) {\n            var key = keys[k];\n            if (key === '@context') continue;\n            if (key.charAt(0) === '@') {\n                linked = true;\n                if (key === '@id' && !subject) subject = String(node[key] || '');\n                continue;\n            }\n            count++;\n            if (!title && /(^|[:#\\/])(title|name|label)$/i.test(key) && typeof node[key] === 'string') {\n                title = node[key].trim();\n            }\n        }\n    }\n    if (!linked) throw new Error('JSON document is not JSON-LD: no @context, @id or @type.');\n    return { statements: count, subject: subject, title: title, quads: null };\n}\nfunction rdfXmlStatements(text) {\n    if (!/<rdf:RDF[\\s>]/i.test(text)) throw new Error('No rdf:RDF root element.');\n    var subjects = text.match(/rdf:(?:about|nodeID|ID)\\s*=\\s*\"([^\"]*)\"/gi) || [];\n    var first = /rdf:about\\s*=\\s*\"([^\"]*)\"/i.exec(text);\n    var titleMatch = /<(?:dct|dcterms|dc):title[^>]*>([^<]*)</i.exec(text);\n    return {\n        statements: subjects.length,\n        subject: first ? first[1] : '',\n        title: titleMatch ? titleMatch[1].trim() : '',\n        quads: null\n    };\n}\nfunction rdfPrimaryResource(quads) {\n    var out = { subject: '', title: '' };\n    if (!quads || !quads.length) return out;\n    var typed = '';\n    var firstNamed = '';\n    for (var i = 0; i < quads.length; i++) {\n        var q = quads[i];\n        if (!q.subject || q.subject.termType !== 'NamedNode') continue;\n        if (!firstNamed) firstNamed = q.subject.value;\n        if (!typed && q.predicate.value === 'http://www.w3.org/1999/02/22-rdf-syntax-ns#type' &&\n            q.object && RDF_PRIMARY_TYPES.indexOf(q.object.value) >= 0) typed = q.subject.value;\n    }\n    out.subject = typed || firstNamed;\n    if (!out.subject) return out;\n    for (var p = 0; p < RDF_TITLE_PREDICATES.length; p++) {\n        for (var j = 0; j < quads.length; j++) {\n            var t = quads[j];\n            if (t.subject.value !== out.subject) continue;\n            if (t.predicate.value !== RDF_TITLE_PREDICATES[p]) continue;\n            if (t.object && t.object.termType === 'Literal' && String(t.object.value).trim()) {\n                out.title = String(t.object.value).trim();\n                return out;\n            }\n        }\n    }\n    return out;\n}\nfunction rdfParseAs(text, type) {\n    if (type === 'application/ld+json') return rdfJsonLdStatements(text);\n    if (type === 'application/rdf+xml') return rdfXmlStatements(text);\n    // A document that parses cleanly is RDF even when it carries no triples;\n    // the harvest then simply yields no assets.\n    var quads = new N3.Parser({ format: type, baseIRI: 'urn:facis:source:' }).parse(text) || [];\n    var primary = rdfPrimaryResource(quads);\n    return { statements: quads.length, subject: primary.subject, title: primary.title, quads: quads };\n}\nfunction rdfAccept(text, responseType, requestedType, configuredType) {\n    var body = String(text == null ? '' : text);\n    if (!body.trim()) return { ok: false, reason: 'the endpoint returned an empty body' };\n    if (rdfIsHtml(body, responseType)) return { ok: false, reason: 'the endpoint returned an HTML page' };\n    var candidates = [];\n    var sources = [responseType, requestedType, configuredType];\n    for (var s = 0; s < sources.length; s++) {\n        var parts = String(sources[s] == null ? '' : sources[s]).split(',');\n        for (var p = 0; p < parts.length; p++) {\n            var canon = rdfCanonicalType(parts[p]);\n            if (canon && candidates.indexOf(canon) < 0) candidates.push(canon);\n        }\n    }\n    var sniffed = rdfSniffType(body);\n    if (sniffed && candidates.indexOf(sniffed) < 0) candidates.push(sniffed);\n    if (candidates.indexOf('text/turtle') < 0) candidates.push('text/turtle');\n    var lastError = '';\n    for (var c = 0; c < candidates.length; c++) {\n        try {\n            var parsed = rdfParseAs(body, candidates[c]);\n            return {\n                ok: true, type: candidates[c], statements: parsed.statements,\n                subject: parsed.subject || '', title: parsed.title || '', quads: parsed.quads || null\n            };\n        } catch (e) {\n            if (!lastError) lastError = e.message;\n        }\n    }\n    return { ok: false, reason: 'the body does not parse as RDF (' + (lastError || 'unknown parse error') + ')' };\n}\nfunction protoXmlMime(ct) {\n    return /xml/i.test(ct || '');\n}\nfunction protoAddQuery(url, params) {\n    var parts = [];\n    Object.keys(params).forEach(function (k) {\n        var v = params[k];\n        if (v === undefined || v === null || v === '') return;\n        parts.push(encodeURIComponent(k) + '=' + encodeURIComponent(String(v)));\n    });\n    if (!parts.length) return url;\n    return url + (url.indexOf('?') >= 0 ? '&' : '?') + parts.join('&');\n}\n// Builds the OAI-PMH request URL. A resumption token is exclusive: the spec\n// forbids sending metadataPrefix or set alongside it.\nfunction protoOaiUrl(base, opts) {\n    if (opts && opts.resumptionToken) {\n        return protoAddQuery(base, { verb: 'ListRecords', resumptionToken: opts.resumptionToken });\n    }\n    return protoAddQuery(base, {\n        verb: 'ListRecords',\n        metadataPrefix: (opts && opts.metadataPrefix) || 'oai_dc',\n        set: (opts && opts.setSpec) || ''\n    });\n}\n// Namespace-tolerant XML helpers. Node-RED function nodes have no XML parser\n// available, so the OAI envelope is read with anchored tag scans.\nfunction protoTag(name) {\n    return '(?:[A-Za-z0-9_.-]+:)?' + name;\n}\nfunction protoFindBlocks(xml, name) {\n    var out = [];\n    var re = new RegExp('<' + protoTag(name) + '(\\\\s[^>]*)?>([\\\\s\\\\S]*?)<\\\\/' + protoTag(name) + '>', 'g');\n    var m;\n    while ((m = re.exec(xml)) !== null) out.push({ attrs: m[1] || '', inner: m[2], whole: m[0] });\n    return out;\n}\nfunction protoFirstBlock(xml, name) {\n    var blocks = protoFindBlocks(xml, name);\n    return blocks.length ? blocks[0] : null;\n}\nfunction protoTagText(xml, name) {\n    var b = protoFirstBlock(xml, name);\n    if (!b) return '';\n    return protoDecodeXml(b.inner.replace(/<[^>]*>/g, '')).trim();\n}\nfunction protoSelfClosing(xml, name) {\n    var re = new RegExp('<' + protoTag(name) + '(\\\\s[^>]*)?\\\\/>');\n    var m = re.exec(xml);\n    return m ? { attrs: m[1] || '', inner: '', whole: m[0] } : null;\n}\nfunction protoAttr(attrs, name) {\n    var m = new RegExp(name + '\\\\s*=\\\\s*\"([^\"]*)\"').exec(attrs || '');\n    if (m) return m[1];\n    m = new RegExp(name + \"\\\\s*=\\\\s*'([^']*)'\").exec(attrs || '');\n    return m ? m[1] : '';\n}\nfunction protoDecodeXml(s) {\n    return String(s == null ? '' : s)\n        .replace(/&lt;/g, '<').replace(/&gt;/g, '>')\n        .replace(/&quot;/g, '\"').replace(/&apos;/g, \"'\")\n        .replace(/&#(\\d+);/g, function (_, d) { return String.fromCharCode(parseInt(d, 10)); })\n        .replace(/&amp;/g, '&');\n}\n\n// NF-4: run-scoped diagnostics + helpers\nvar _rd = msg._runDiag || (msg._runDiag = { errors: [], logs: [], catalogueStatus: {}, counters: { transportErrors: 0, parseErrors: 0, validationErrors: 0, entryErrors: 0, persistenceErrors: 0 } });\nfunction _nowISO() { return new Date().toISOString(); }\nfunction _catIdOf(ci) { var c = catalogues[ci]; return (c && (c.uniqueId || c.catalogId || c.id)) || ('cat-' + ci); }\nfunction _redact(u){ try{ if(!u) return ''; var s=String(u); s=s.replace(/\\/\\/[^/@]*@/, '//'); var q=s.indexOf('?'); if(q>=0) s=s.slice(0,q)+'?[redacted]'; return s; }catch(e){ return ''; } }\nfunction _isRdfType(ct){ return protoRdfMime(ct) || /application\\/xml|text\\/xml/i.test(ct || ''); }\nfunction createHarvestError(o){ o.level = o.level || 'error'; o.timestamp = _nowISO(); return o; }\nfunction markCatalogueFailure(catId, err, code){ _rd.errors.push(err); _rd.logs.push({ assetId:'', level: err.level || 'error', step: err.step, code: code, message: err.message, catalogue: err.catalogueName || '', timestamp: _nowISO() }); if (err.level !== 'warn') _rd.catalogueStatus[catId] = 'failed'; }\n\nfunction resolvePath(obj, path) {\n    if (!path || !obj) return obj;\n    var parts = path.split('.');\n    var cur = obj;\n    for (var pi = 0; pi < parts.length; pi++) {\n        if (cur == null || typeof cur !== 'object') return undefined;\n        cur = cur[parts[pi]];\n    }\n    return cur;\n}\nfunction responseContentType(headers, fallback) {\n    headers = headers || {};\n    return String(headers['content-type'] || headers['Content-Type'] || fallback || '').split(';')[0].trim();\n}\n\nfunction oaiParse(xml) {\n    var out = { ok: false, errorCode: '', errorMessage: '', records: [], token: '', tokenAttrs: {}, deleted: 0 };\n    if (!/<(?:[A-Za-z0-9_.-]+:)?OAI-PMH[\\s>]/.test(xml)) {\n        out.errorMessage = 'Response is not an OAI-PMH envelope.';\n        return out;\n    }\n    var err = protoFirstBlock(xml, 'error') || protoSelfClosing(xml, 'error');\n    if (err) {\n        out.errorCode = protoAttr(err.attrs, 'code') || 'unknown';\n        out.errorMessage = protoDecodeXml(String(err.inner || '').replace(/<[^>]*>/g, '')).trim() || ('OAI-PMH error: ' + out.errorCode);\n        return out;\n    }\n    var list = protoFirstBlock(xml, 'ListRecords');\n    if (!list) { out.ok = true; return out; }\n    var blocks = protoFindBlocks(list.inner, 'record');\n    for (var i = 0; i < blocks.length; i++) {\n        var rec = blocks[i].inner;\n        var header = protoFirstBlock(rec, 'header');\n        var headerXml = header ? header.inner : rec;\n        var st = header ? String(protoAttr(header.attrs, 'status')).toLowerCase() : '';\n        var metadata = protoFirstBlock(rec, 'metadata');\n        var sets = protoFindBlocks(headerXml, 'setSpec').map(function (b) { return protoDecodeXml(b.inner.replace(/<[^>]*>/g, '')).trim(); });\n        out.records.push({\n            identifier: protoTagText(headerXml, 'identifier'),\n            datestamp: protoTagText(headerXml, 'datestamp'),\n            setSpec: sets,\n            deleted: st === 'deleted',\n            metadataXml: metadata ? metadata.inner : '',\n            recordXml: blocks[i].whole\n        });\n        if (st === 'deleted') out.deleted++;\n    }\n    var tokenBlock = protoFirstBlock(xml, 'resumptionToken') || protoSelfClosing(xml, 'resumptionToken');\n    if (tokenBlock) {\n        out.token = protoDecodeXml(String(tokenBlock.inner || '').replace(/<[^>]*>/g, '')).trim();\n        out.tokenAttrs = {\n            cursor: protoAttr(tokenBlock.attrs, 'cursor'),\n            completeListSize: protoAttr(tokenBlock.attrs, 'completeListSize'),\n            expirationDate: protoAttr(tokenBlock.attrs, 'expirationDate')\n        };\n    }\n    out.ok = true;\n    return out;\n}\n\n// Flattens an OAI record for the asset builder while keeping the raw XML.\nfunction oaiEntry(rec, prefix, endpoint) {\n    var meta = rec.metadataXml || '';\n    return {\n        identifier: rec.identifier,\n        title: protoTagText(meta, 'title') || rec.identifier,\n        type: protoTagText(meta, 'type') || '',\n        datestamp: rec.datestamp,\n        setSpec: rec.setSpec.join(' '),\n        metadataPrefix: prefix,\n        sourceEndpoint: endpoint,\n        description: protoTagText(meta, 'description'),\n        publisher: protoTagText(meta, 'publisher'),\n        __rawMetadata: rec.recordXml\n    };\n}\n\nfunction sparqlBindingValue(b) {\n    if (!b || typeof b !== 'object') return '';\n    if (b.type === 'bnode') return '_:' + (b.value || '');\n    return b.value === undefined ? '' : String(b.value);\n}\nfunction sparqlEntries(json, q) {\n    var vars = (json.head && Array.isArray(json.head.vars)) ? json.head.vars : [];\n    var bindings = (json.results && Array.isArray(json.results.bindings)) ? json.results.bindings : [];\n    var idVar = q.idVar || vars[0] || '';\n    var entries = [];\n    var rejected = [];\n    for (var i = 0; i < bindings.length; i++) {\n        var row = bindings[i];\n        var flat = {};\n        for (var v = 0; v < vars.length; v++) {\n            var name = vars[v];\n            var cell = row[name];\n            flat[name] = sparqlBindingValue(cell);\n            if (cell && cell.datatype) flat[name + '__datatype'] = cell.datatype;\n            if (cell && cell['xml:lang']) flat[name + '__lang'] = cell['xml:lang'];\n            if (cell && cell.type) flat[name + '__type'] = cell.type;\n        }\n        var id = idVar ? flat[idVar] : '';\n        if (!id) { rejected.push({ index: i, reason: 'no value for identifier variable ' + (idVar || '(none configured)') }); continue; }\n        entries.push({\n            identifier: id,\n            title: (q.nameVar && flat[q.nameVar]) || id,\n            type: (q.typeVar && flat[q.typeVar]) || '',\n            bindings: flat,\n            __rawMetadata: JSON.stringify(row)\n        });\n    }\n    return { entries: entries, rejected: rejected, vars: vars, bindingCount: bindings.length };\n}\n\n// Protocol entries reach the asset builder through the existing sourceData contract.\nfunction applyEntries(cat, entries, fields) {\n    var existing = (cat.sourceData && Array.isArray(cat.sourceData.records)) ? cat.sourceData.records : [];\n    cat.sourceData = { records: existing.concat(entries) };\n    cat.responseRootPath = 'records';\n    cat.responseAssetIdField = fields.id;\n    cat.responseAssetNameField = fields.name;\n    cat.responseAssetTypeField = fields.type;\n    cat.strictAssetValidation = false;\n}\n\n// Processes one OAI-PMH page and, when a resumption token is returned, appends\n// the next page to the live fetch queue.\nfunction handleOaiPage(c) {\n    var oaiCfg = (c.current.oai && c.current.oai.config) || {};\n    var parsed = oaiParse(c.rawStr);\n    c.ev.pages = (c.ev.pages || 0) + 1;\n\n    if (!parsed.ok && parsed.errorCode) {\n        var oaiCode = parsed.errorCode === 'badResumptionToken' ? 'OAI_BAD_RESUMPTION_TOKEN' : 'OAI_PROTOCOL_ERROR';\n        var oaiLevel = parsed.errorCode === 'noRecordsMatch' ? 'warn' : 'error';\n        markCatalogueFailure(c.cid, createHarvestError({ stage:'protocol', step:'oai', code:oaiCode, level:oaiLevel, catalogueId:c.cid, catalogueName:c.cname, endpoint:c.ep, assetId:null, retryable:false, message:'OAI-PMH error ' + parsed.errorCode + ' from ' + c.ep + ': ' + parsed.errorMessage, detail:{ oaiErrorCode: parsed.errorCode, page: c.ev.pages } }), oaiCode);\n        if (oaiLevel === 'error') _rd.counters.parseErrors++;\n        c.ev.errors.push({ code: oaiCode, oaiErrorCode: parsed.errorCode, message: parsed.errorMessage });\n        return;\n    }\n    if (!parsed.ok) {\n        markCatalogueFailure(c.cid, createHarvestError({ stage:'parse', step:'oai', code:'OAI_INVALID_RESPONSE', catalogueId:c.cid, catalogueName:c.cname, endpoint:c.ep, assetId:null, retryable:false, message:'Response from ' + c.ep + ' is not a valid OAI-PMH document: ' + (parsed.errorMessage || 'unrecognised payload') + '.', detail:{ contentType: c.contentType, page: c.ev.pages } }), 'OAI_INVALID_RESPONSE');\n        _rd.counters.parseErrors++;\n        c.ev.errors.push({ code: 'OAI_INVALID_RESPONSE', message: parsed.errorMessage });\n        return;\n    }\n\n    var live = [];\n    for (var r = 0; r < parsed.records.length; r++) {\n        if (parsed.records[r].deleted) continue;\n        live.push(oaiEntry(parsed.records[r], oaiCfg.metadataPrefix || '', c.current.oai.base));\n    }\n    c.ev.remoteRecords = (c.ev.remoteRecords || 0) + parsed.records.length;\n    c.ev.deletedRecords = (c.ev.deletedRecords || 0) + parsed.deleted;\n    if (parsed.tokenAttrs.cursor) c.ev.cursor = parsed.tokenAttrs.cursor;\n    if (parsed.tokenAttrs.completeListSize) c.ev.completeListSize = parsed.tokenAttrs.completeListSize;\n    if (parsed.tokenAttrs.expirationDate) c.ev.expirationDate = parsed.tokenAttrs.expirationDate;\n\n    applyEntries(c.cat, live, { id: 'identifier', name: 'title', type: 'type' });\n    c.cat.protocolProvenance = { protocol: 'oai-pmh', endpoint: c.current.oai.base, metadataPrefix: oaiCfg.metadataPrefix || '', setSpec: oaiCfg.setSpec || '' };\n    if (_rd.catalogueStatus[c.cid] !== 'failed') _rd.catalogueStatus[c.cid] = 'ok';\n    _rd.logs.push({ assetId:'', level:'info', step:'oai', code:'OAI_PAGE_OK', message:'OAI-PMH page ' + c.ev.pages + ' for ' + (c.cname || c.cid) + ': ' + parsed.records.length + ' record(s), ' + parsed.deleted + ' deleted.', catalogue:c.cname, timestamp:_nowISO() });\n    if (parsed.deleted > 0) {\n        _rd.logs.push({ assetId:'', level:'warn', step:'oai', code:'OAI_RECORD_DELETED', message:parsed.deleted + ' deleted OAI record(s) on page ' + c.ev.pages + ' were not imported.', catalogue:c.cname, timestamp:_nowISO() });\n    }\n\n    var tok = parsed.token;\n    if (!tok) return;\n    var seen = (c.current.oai && c.current.oai.seenTokens) || [];\n    if (oaiCfg.resumptionTokenEnabled === false) {\n        c.ev.partial = true;\n        c.ev.partialReason = 'OAI_RESUMPTION_DISABLED';\n        markCatalogueFailure(c.cid, createHarvestError({ stage:'protocol', step:'oai', code:'OAI_RESUMPTION_DISABLED', level:'warn', catalogueId:c.cid, catalogueName:c.cname, endpoint:c.ep, assetId:null, retryable:false, message:'A resumption token was returned but pagination is disabled for ' + (c.cname || c.cid) + '; the harvest is partial.', detail:{ page: c.ev.pages } }), 'OAI_RESUMPTION_DISABLED');\n        c.ev.errors.push({ code: 'OAI_RESUMPTION_DISABLED', message: 'pagination disabled' });\n        return;\n    }\n    if (seen.indexOf(tok) >= 0) {\n        markCatalogueFailure(c.cid, createHarvestError({ stage:'protocol', step:'oai', code:'OAI_RESUMPTION_TOKEN_LOOP', catalogueId:c.cid, catalogueName:c.cname, endpoint:c.ep, assetId:null, retryable:false, message:'OAI-PMH resumption token repeated for ' + (c.cname || c.cid) + '; pagination stopped to avoid a loop.', detail:{ page: c.ev.pages } }), 'OAI_RESUMPTION_TOKEN_LOOP');\n        c.ev.errors.push({ code: 'OAI_RESUMPTION_TOKEN_LOOP', message: 'repeated resumption token' });\n        return;\n    }\n    if (c.ev.pages >= (oaiCfg.maxPages || 50)) {\n        markCatalogueFailure(c.cid, createHarvestError({ stage:'protocol', step:'oai', code:'OAI_MAX_PAGES_REACHED', level:'warn', catalogueId:c.cid, catalogueName:c.cname, endpoint:c.ep, assetId:null, retryable:false, message:'OAI-PMH page limit (' + (oaiCfg.maxPages || 50) + ') reached for ' + (c.cname || c.cid) + '; the harvest is partial.', detail:{ page: c.ev.pages } }), 'OAI_MAX_PAGES_REACHED');\n        c.ev.errors.push({ code: 'OAI_MAX_PAGES_REACHED', message: 'page limit reached' });\n        c.ev.partial = true;\n        c.ev.partialReason = 'OAI_MAX_PAGES_REACHED';\n        return;\n    }\n    c.ev.resumptionTokensFollowed = true;\n    c.ev.currentToken = tok.length > 24 ? (tok.slice(0, 24) + '...') : tok;\n    c.queue.push({\n        step: 'data', protocol: 'oai-pmh', catIdx: c.current.catIdx, catName: c.current.catName,\n        url: protoOaiUrl(c.current.oai.base, { resumptionToken: tok }),\n        method: 'GET', body: '', hdrs: c.current.hdrs, requestedMime: c.current.requestedMime,\n        oai: { base: c.current.oai.base, page: (c.current.oai.page || 1) + 1, seenTokens: seen.concat([tok]), config: oaiCfg }\n    });\n}\n\n// Processes a query-interface (SPARQL or custom query) result set.\nfunction handleQueryResult(c) {\n    var q = c.current.query;\n    var mode = q.resultMode || 'sparql-select';\n    c.ev.pages = (c.ev.pages || 0) + 1;\n\n    if (protoRdfMime(c.contentType) || mode === 'rdf') {\n        // CONSTRUCT / DESCRIBE results go through the RDF/DCAT parser.\n        c.cat.sourceData = { __rdfRaw: c.rawStr, __rdfContentType: c.contentType || q.resultMime || '' };\n        c.cat.sourceContentType = c.contentType;\n        c.cat.protocolProvenance = { protocol: 'query-interface', endpoint: q.endpoint, queryLanguage: q.language, queryMethod: q.method, query: q.text };\n        if (_rd.catalogueStatus[c.cid] !== 'failed') _rd.catalogueStatus[c.cid] = 'ok';\n        _rd.logs.push({ assetId:'', level:'info', step:'query', code:'QUERY_RDF_OK', message:'Query returned RDF (' + c.contentType + ') for ' + (c.cname || c.cid) + '.', catalogue:c.cname, timestamp:_nowISO() });\n        return;\n    }\n    if (!c.parsedJson) {\n        markCatalogueFailure(c.cid, createHarvestError({ stage:'parse', step:'query', code:'QUERY_RESULT_INVALID', catalogueId:c.cid, catalogueName:c.cname, endpoint:c.ep, assetId:null, retryable:false, message:'Query result from ' + c.ep + ' is neither valid JSON nor RDF (content type ' + (c.contentType || 'unknown') + ').', detail:{ contentType: c.contentType } }), 'QUERY_RESULT_INVALID');\n        _rd.counters.parseErrors++;\n        c.ev.errors.push({ code: 'QUERY_RESULT_INVALID', message: 'unparseable query result' });\n        return;\n    }\n    if (mode === 'custom-json') {\n        var root = q.rootPath ? resolvePath(c.data, q.rootPath) : c.data;\n        if (!root) {\n            markCatalogueFailure(c.cid, createHarvestError({ stage:'parse', step:'query', code:'QUERY_RESULT_INVALID', catalogueId:c.cid, catalogueName:c.cname, endpoint:c.ep, assetId:null, retryable:false, message:'Query result from ' + c.ep + ' has no data at the configured result root ' + (q.rootPath || '(root)') + '.', detail:{ rootPath: q.rootPath } }), 'QUERY_RESULT_INVALID');\n            _rd.counters.parseErrors++;\n            return;\n        }\n        c.cat.sourceData = c.data;\n        c.cat.responseRootPath = q.rootPath || c.cat.responseRootPath || '';\n        c.cat.protocolProvenance = { protocol: 'query-interface', endpoint: q.endpoint, queryLanguage: q.language, queryMethod: q.method, query: q.text };\n        c.ev.resultCount = Array.isArray(root) ? root.length : 1;\n        if (_rd.catalogueStatus[c.cid] !== 'failed') _rd.catalogueStatus[c.cid] = 'ok';\n        return;\n    }\n    if (c.data && typeof c.data.boolean === 'boolean') {\n        // ASK is recorded as query evidence and never produces assets.\n        c.ev.askResult = c.data.boolean;\n        c.ev.resultCount = 0;\n        c.cat.sourceData = { records: [] };\n        c.cat.responseRootPath = 'records';\n        c.cat.protocolProvenance = { protocol: 'query-interface', endpoint: q.endpoint, queryLanguage: q.language, queryMethod: q.method, query: q.text };\n        if (_rd.catalogueStatus[c.cid] !== 'failed') _rd.catalogueStatus[c.cid] = 'ok';\n        _rd.logs.push({ assetId:'', level:'info', step:'query', code:'SPARQL_ASK_OK', message:'SPARQL ASK returned ' + c.data.boolean + ' for ' + (c.cname || c.cid) + '; ASK queries do not create assets.', catalogue:c.cname, timestamp:_nowISO() });\n        return;\n    }\n    if (c.data && c.data.head && c.data.results && Array.isArray(c.data.results.bindings)) {\n        var sel = sparqlEntries(c.data, q);\n        c.ev.bindingCount = (c.ev.bindingCount || 0) + sel.bindingCount;\n        c.ev.variables = sel.vars;\n        c.ev.resultCount = (c.ev.resultCount || 0) + sel.entries.length;\n        for (var rj = 0; rj < sel.rejected.length; rj++) {\n            _rd.errors.push(createHarvestError({ stage:'entry', step:'query', code:'QUERY_RESULT_INVALID', catalogueId:c.cid, catalogueName:c.cname, endpoint:c.ep, assetId:null, retryable:false, message:'SPARQL binding ' + sel.rejected[rj].index + ' skipped: ' + sel.rejected[rj].reason + '.', detail:{} }));\n            _rd.counters.entryErrors++;\n        }\n        applyEntries(c.cat, sel.entries, { id: 'identifier', name: 'title', type: 'type' });\n        c.cat.protocolProvenance = { protocol: 'query-interface', endpoint: q.endpoint, queryLanguage: q.language, queryMethod: q.method, query: q.text };\n        if (_rd.catalogueStatus[c.cid] !== 'failed') _rd.catalogueStatus[c.cid] = 'ok';\n        _rd.logs.push({ assetId:'', level:'info', step:'query', code:'SPARQL_SELECT_OK', message:'SPARQL SELECT returned ' + sel.bindingCount + ' binding(s); ' + sel.entries.length + ' asset(s) derived for ' + (c.cname || c.cid) + '.', catalogue:c.cname, timestamp:_nowISO() });\n        return;\n    }\n    markCatalogueFailure(c.cid, createHarvestError({ stage:'parse', step:'query', code:'SPARQL_RESULT_INVALID', catalogueId:c.cid, catalogueName:c.cname, endpoint:c.ep, assetId:null, retryable:false, message:'Query result from ' + c.ep + ' is JSON but not a SPARQL result set: no head/results.bindings and no boolean.', detail:{ contentType: c.contentType } }), 'SPARQL_RESULT_INVALID');\n    _rd.counters.parseErrors++;\n    c.ev.errors.push({ code: 'SPARQL_RESULT_INVALID', message: 'not a SPARQL result set' });\n}\n\n// DCAT must be genuine RDF. The acceptance rule is shared verbatim with Test\n// Connection, so a response accepted there cannot be rejected here. A generic\n// content type such as text/plain is accepted only when the body really parses.\nfunction handleDcatResult(c) {\n    c.ev.pages = (c.ev.pages || 0) + 1;\n    var configuredMime = c.cat.mimeType || c.cat.contentNegotiation || '';\n    var verdict = rdfAccept(c.rawStr, c.contentType, c.current.requestedMime, configuredMime);\n\n    if (!verdict.ok) {\n        markCatalogueFailure(c.cid, createHarvestError({ stage:'parse', step:'dcat', code:'DCAT_RESPONSE_INVALID', catalogueId:c.cid, catalogueName:c.cname, endpoint:c.ep, assetId:null, retryable:false, message:'Response from ' + c.ep + ' is not recognisable RDF/DCAT: ' + verdict.reason + ' (content type ' + (c.contentType || 'unknown') + ').', detail:{ contentType: c.contentType || '', requestedMime: c.current.requestedMime || '' } }), 'DCAT_RESPONSE_INVALID');\n        _rd.counters.parseErrors++;\n        c.ev.errors.push({ code: 'DCAT_RESPONSE_INVALID', message: verdict.reason });\n        return;\n    }\n\n    // The canonical format detected here is what the NF-6 transform pipeline\n    // parses, so text/plain never reaches the RDF parser as the source format.\n    c.cat.sourceData = {\n        __rdfRaw: c.rawStr,\n        __rdfContentType: verdict.type,\n        __rdfSubject: verdict.subject || '',\n        __rdfTitle: verdict.title || ''\n    };\n    c.cat.sourceContentType = verdict.type;\n    c.cat.protocolProvenance = {\n        protocol: 'dcat',\n        endpoint: c.current.url,\n        mimeType: verdict.type,\n        responseContentType: c.contentType || '',\n        statements: verdict.statements\n    };\n    c.ev.remoteRecords = (c.ev.remoteRecords || 0) + 1;\n    c.ev.rdfFormat = verdict.type;\n    c.ev.rdfStatements = verdict.statements;\n    _rd.catalogueStatus[c.cid] = 'ok';\n    _rd.logs.push({ assetId:'', level:'info', step:'dcat', code:'DCAT_OK', message:'Fetched DCAT data for ' + (c.cname || c.cid) + ': ' + verdict.statements + ' statement(s) parsed as ' + verdict.type + ' (server sent ' + (c.contentType || 'no content type') + ').', catalogue:c.cname, timestamp:_nowISO() });\n}\n\n// Plain REST/JSON query interfaces: the historical FACIS behaviour.\nfunction handleRestResult(c) {\n    var looksHtml = /^\\s*<(!doctype|html)[\\s>]/i.test(c.rawStr) || /text\\/html/i.test(c.contentType);\n    var isEmpty = (c.data === null || c.data === undefined || c.data === '' || (typeof c.raw === 'string' && !c.raw.trim()));\n    var looksJson = (typeof c.raw === 'string') && (/json/i.test(c.contentType) || looksLikeJsonText(c.raw));\n\n    if (looksHtml) {\n        markCatalogueFailure(c.cid, createHarvestError({ stage:'parse', step:'parse', code:'unsupported_content_type', catalogueId:c.cid, catalogueName:c.cname, endpoint:c.ep, assetId:null, retryable:false, message:'Endpoint ' + c.ep + ' returned HTML, not machine-readable data, for catalogue ' + (c.cname || c.cid) + '.', detail:{ contentType: c.contentType } }), 'unsupported_content_type');\n        _rd.counters.parseErrors++;\n        return;\n    }\n    if (isEmpty) {\n        markCatalogueFailure(c.cid, createHarvestError({ stage:'parse', step:'parse', code:'empty_response', catalogueId:c.cid, catalogueName:c.cname, endpoint:c.ep, assetId:null, retryable:false, message:'Endpoint ' + c.ep + ' returned an empty response for catalogue ' + (c.cname || c.cid) + '.', detail:{ contentType: c.contentType } }), 'empty_response');\n        _rd.counters.parseErrors++;\n        return;\n    }\n    if (looksJson && !c.parsedJson) {\n        markCatalogueFailure(c.cid, createHarvestError({ stage:'parse', step:'parse', code:'unparseable_response', catalogueId:c.cid, catalogueName:c.cname, endpoint:c.ep, assetId:null, retryable:false, message:'Response from ' + c.ep + ' for catalogue ' + (c.cname || c.cid) + ' is not valid JSON.', detail:{ contentType: c.contentType } }), 'unparseable_response');\n        _rd.counters.parseErrors++;\n        return;\n    }\n    if (!c.parsedJson && !_isRdfType(c.contentType)) {\n        markCatalogueFailure(c.cid, createHarvestError({ stage:'parse', step:'parse', code:'unsupported_content_type', catalogueId:c.cid, catalogueName:c.cname, endpoint:c.ep, assetId:null, retryable:false, message:'Response from ' + c.ep + ' for catalogue ' + (c.cname || c.cid) + ' is neither JSON nor a recognised RDF media type.', detail:{ contentType: c.contentType } }), 'unsupported_content_type');\n        _rd.counters.parseErrors++;\n        return;\n    }\n    c.cat.sourceData = c.parsedJson ? c.data : { __rdfRaw: c.rawStr, __rdfContentType: c.contentType || c.current.requestedMime || '' };\n    c.cat.sourceContentType = c.contentType || c.current.requestedMime || '';\n    var restMapping = c.ev.responseMapping || {\n        rootPath: c.cat.responseRootPath || '',\n        idField: c.cat.responseAssetIdField || '',\n        nameField: c.cat.responseAssetNameField || '',\n        typeField: c.cat.responseAssetTypeField || ''\n    };\n    var restRoot = restMapping.rootPath ? resolvePath(c.data, restMapping.rootPath) : c.data;\n    var restCount = Array.isArray(restRoot) ? restRoot.length : (restRoot ? 1 : 0);\n    c.cat.protocolProvenance = {\n        protocol: 'query-interface',\n        endpoint: c.current.url,\n        queryLanguage: 'rest-json',\n        queryMethod: c.current.method || 'GET',\n        mimeType: c.current.requestedMime || '',\n        responseContentType: c.contentType || ''\n    };\n    c.ev.queryLanguage = c.ev.queryLanguage || 'rest-json';\n    c.ev.queryMethod = c.ev.queryMethod || c.current.method || 'GET';\n    c.ev.resultMimeType = c.ev.resultMimeType || c.current.requestedMime || '';\n    c.ev.responseContentType = c.contentType || '';\n    c.ev.responseMapping = restMapping;\n    c.ev.resultCount = (c.ev.resultCount || 0) + restCount;\n    c.ev.remoteRecords = (c.ev.remoteRecords || 0) + restCount;\n    c.ev.pages = (c.ev.pages || 0) + 1;\n    _rd.catalogueStatus[c.cid] = 'ok';\n    _rd.logs.push({ assetId:'', level:'info', step:'fetch', code:'fetch_ok', message:'Fetched API data for ' + (c.cname || c.cid) + ' (HTTP ' + c.status + ', ' + (c.contentType || 'unknown content type') + ')', catalogue:c.cname, timestamp:_nowISO() });\n}\n\nfunction handleLoginStep(c) {\n    if (c.parsedJson && c.data && c.status >= 200 && c.status < 300) {\n        var token = resolvePath(c.data, c.current.tokenPath || 'token');\n        if (token) {\n            for (var j = c.idx + 1; j < c.queue.length; j++) {\n                if (c.queue[j].catIdx === c.current.catIdx && c.queue[j].step === 'data') {\n                    c.queue[j].hdrs['Authorization'] = (c.current.tokenPrefix || 'Bearer') + ' ' + String(token);\n                    break;\n                }\n            }\n            return;\n        }\n        markCatalogueFailure(c.cid, createHarvestError({ stage:'protocol', step:'login', code:'auth_no_token', catalogueId:c.cid, catalogueName:c.cname, endpoint:c.ep, assetId:null, retryable:false, message:'Login to ' + (c.cname || c.cid) + ' returned no token; the data fetch would be unauthenticated.', detail:{ tokenPath: c.current.tokenPath || 'token' } }), 'auth_no_token');\n        return;\n    }\n    var code = (c.status === 401 || c.status === 403) ? ('http_' + c.status) : (c.status >= 500 ? 'http_5xx' : (c.status >= 400 ? 'http_4xx' : 'ECONNREFUSED'));\n    markCatalogueFailure(c.cid, createHarvestError({ stage:'transport', step:'login', code:code, catalogueId:c.cid, catalogueName:c.cname, endpoint:c.ep, assetId:null, retryable:(c.status === 0 || c.status >= 500 || c.status === 429), message:'Login failed (HTTP ' + c.status + ') for catalogue ' + (c.cname || c.cid) + '.', detail:{ httpCode: c.status } }), code);\n    _rd.counters.transportErrors++;\n}\n\n// Returns true when the HTTP exchange itself failed and no body should be parsed.\nfunction handleTransportFailure(c) {\n    if (c.status === 0 || c.status == null || c.status < 200) {\n        var code = 'ECONNREFUSED';\n        if (/ENOTFOUND|getaddrinfo/i.test(c.rawStr)) code = 'ENOTFOUND';\n        else if (/ETIMEDOUT|ESOCKETTIMEDOUT|timed? ?out/i.test(c.rawStr)) code = 'ETIMEDOUT';\n        else if (/ECONNRESET/i.test(c.rawStr)) code = 'ECONNRESET';\n        else if (/self.signed|CERT_|DEPTH_ZERO|unable to verify|SSL|TLS/i.test(c.rawStr)) code = 'TLS_ERROR';\n        else if (/maximum redirect|redirect/i.test(c.rawStr)) code = 'REDIRECT_LOOP';\n        markCatalogueFailure(c.cid, createHarvestError({ stage:'transport', step:'fetch', code:code, catalogueId:c.cid, catalogueName:c.cname, endpoint:c.ep, assetId:null, retryable:true, message:'Could not reach ' + c.ep + ' for catalogue ' + (c.cname || c.cid) + ' (' + code + ').', detail:{ httpCode: c.status, protocol: c.protocol } }), code);\n        _rd.counters.transportErrors++;\n        c.ev.errors.push({ code: code, message: 'transport failure' });\n        return true;\n    }\n    if (c.status >= 400) {\n        var hcode = c.status === 429 ? 'http_429' : (c.status >= 500 ? 'http_5xx' : ('http_' + c.status));\n        markCatalogueFailure(c.cid, createHarvestError({ stage:'transport', step:'fetch', code:hcode, catalogueId:c.cid, catalogueName:c.cname, endpoint:c.ep, assetId:null, retryable:(c.status >= 500 || c.status === 429), message:'HTTP ' + c.status + ' from ' + c.ep + ' for catalogue ' + (c.cname || c.cid) + '.', detail:{ httpCode: c.status, protocol: c.protocol } }), hcode);\n        _rd.counters.transportErrors++;\n        c.ev.errors.push({ code: hcode, message: 'HTTP ' + c.status });\n        return true;\n    }\n    return false;\n}\n\nif (current) {\n    var raw = msg.payload;\n    var contentType = responseContentType(msg.headers, current.requestedMime);\n    var data = raw;\n    var parsedJson = false;\n    if (typeof raw === 'string' && (/json/i.test(contentType) || looksLikeJsonText(raw))) {\n        try { data = JSON.parse(raw); parsedJson = true; } catch (e) { data = raw; }\n    } else if (raw && typeof raw === 'object' && !Buffer.isBuffer(raw)) {\n        parsedJson = true;\n    }\n    var cat = catalogues[current.catIdx] || {};\n    var ctx = {\n        current: current, queue: queue, idx: idx, cat: cat,\n        cid: _catIdOf(current.catIdx),\n        cname: current.catName || cat.catalogName || '',\n        ep: _redact(current.url || ''),\n        status: msg.statusCode || 0,\n        raw: raw,\n        rawStr: (typeof raw === 'string') ? raw : (Buffer.isBuffer(raw) ? raw.toString('utf8') : ''),\n        data: data,\n        parsedJson: parsedJson,\n        contentType: contentType,\n        protocol: current.protocol || 'query-interface',\n        ev: cat.protocolEvidence || (cat.protocolEvidence = { protocol: current.protocol || '', pages: 0, remoteRecords: 0, errors: [] })\n    };\n\n    if (current.step === 'login') {\n        handleLoginStep(ctx);\n    } else if (current.step === 'data' && !handleTransportFailure(ctx)) {\n        ctx.ev.responseContentType = ctx.contentType || ctx.ev.responseContentType || '';\n        if (ctx.protocol === 'oai-pmh') handleOaiPage(ctx);\n        else if (ctx.protocol === 'dcat') handleDcatResult(ctx);\n        else if (ctx.protocol === 'query-interface' && current.query) handleQueryResult(ctx);\n        else handleRestResult(ctx);\n    }\n}\n\nvar nextIdx = idx + 1;\nmsg._fetchIdx = nextIdx;\nif (nextIdx < queue.length) {\n    var next = queue[nextIdx];\n    msg.url = next.url;\n    msg.method = next.method || (next.step === 'login' ? 'POST' : 'GET');\n    msg.headers = next.hdrs;\n    msg.payload = next.body || '';\n    msg.requestTimeout = 15000;\n    node.warn('[harvest] Next fetch: ' + (next.protocol || next.step) + ' ' + next.url);\n    return [msg, null];\n}\ndelete msg._fetchQueue; delete msg._fetchIdx;\nmsg.payload = '';\nreturn [null, msg];\n",
        "outputs": 2,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [
            {
                "var": "N3",
                "module": "n3"
            }
        ],
        "x": 760,
        "y": 240,
        "wires": [
            [
                "2d6d4f7aa11eeeaa"
            ],
            [
                "478e4cbe3927bbd4"
            ]
        ]
    },
    {
        "id": "cf57203f2dee07ff",
        "type": "comment",
        "z": "a4fcd945f4957d68",
        "name": "API Fetch Loop: resolve remote API sources before harvest",
        "info": "When a catalogue has baseEndpoint but no sourceData,\nfetches data via HTTP POST before harvesting.\nSupports auth: none, static-token, api-key, token-login.",
        "x": 880,
        "y": 440,
        "wires": []
    },
    {
        "id": "5b1b65da78e29c95",
        "type": "inject",
        "z": "a4fcd945f4957d68",
        "name": "heartbeat: Harvester",
        "props": [],
        "repeat": "30",
        "crontab": "",
        "once": true,
        "onceDelay": "5",
        "topic": "",
        "x": 200,
        "y": 1900,
        "wires": [
            [
                "69e4adc352eb58f7"
            ]
        ]
    },
    {
        "id": "69e4adc352eb58f7",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "write heartbeat",
        "func": "msg.collection = \"dcm_heartbeats\";\nmsg.operation = \"updateOne\";\nmsg.payload = [\n  { module: \"Harvester\" },\n  { $set: { module: \"Harvester\", at: new Date().toISOString() } },\n  { upsert: true }\n];\nreturn msg;",
        "outputs": 1,
        "x": 420,
        "y": 1900,
        "wires": [
            [
                "b4098bfc2e87cb40"
            ]
        ]
    },
    {
        "id": "b4098bfc2e87cb40",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "name": "upsert heartbeat",
        "x": 640,
        "y": 1900,
        "wires": [
            []
        ]
    },
    {
        "id": "d1b768564d8ad712",
        "type": "comment",
        "z": "a4fcd945f4957d68",
        "name": "── FR-CR-03: Prefetch cr_api_mappings before fetch loop ──",
        "info": "",
        "x": 200,
        "y": 360,
        "wires": []
    },
    {
        "id": "875eaf801e261b41",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Prefetch cr_api_mappings",
        "func": "// FR-CR-03: load active api mappings once per harvest run; downstream stash into msg._apiMappings\nmsg.collection = 'cr_api_mappings';\nmsg.operation  = 'find';\nmsg.payload    = [{ status: 'active' }];\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 540,
        "y": 400,
        "wires": [
            [
                "57ec6bfce79adc96"
            ]
        ]
    },
    {
        "id": "57ec6bfce79adc96",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "DB: Find Api Mappings",
        "x": 800,
        "y": 400,
        "wires": [
            [
                "ea561bd5f015178b"
            ]
        ]
    },
    {
        "id": "ea561bd5f015178b",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Stash _apiMappings → Resolve API sources",
        "func": "msg._apiMappings = Array.isArray(msg.payload) ? msg.payload : [];\ndelete msg.payload;\ndelete msg.collection;\ndelete msg.operation;\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1330,
        "y": 580,
        "wires": [
            [
                "baa6fd19b5caa773"
            ]
        ]
    },
    {
        "id": "559e9de37403789f",
        "type": "comment",
        "z": "a4fcd945f4957d68",
        "name": "── FR-CR-04: Prefetch sr_mappings (JSON Field Mapping only) ──",
        "info": "",
        "x": 220,
        "y": 540,
        "wires": []
    },
    {
        "id": "baa6fd19b5caa773",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Prefetch sr_mappings",
        "func": "msg.collection = 'sr_mappings';\nmsg.operation  = 'find';\nmsg.payload    = [{}];\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 482.5,
        "y": 780,
        "wires": [
            [
                "66b6a46b38dd4994"
            ]
        ]
    },
    {
        "id": "66b6a46b38dd4994",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "DB: Find sr_mappings",
        "x": 677.5,
        "y": 820,
        "wires": [
            [
                "b2b159b14dcb8c8c"
            ]
        ]
    },
    {
        "id": "b2b159b14dcb8c8c",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Stash _fieldMappings",
        "func": "msg._fieldMappings = Array.isArray(msg.payload) ? msg.payload : [];\ndelete msg.payload;\ndelete msg.collection;\ndelete msg.operation;\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 757.5,
        "y": 960,
        "wires": [
            [
                "nf6m3shapeprep1"
            ]
        ]
    },
    {
        "id": "44782244dadc6707",
        "type": "comment",
        "z": "a4fcd945f4957d68",
        "name": "── FR-CR-05: Prefetch sr_prompts + sr_providers (AI-driven only) ──",
        "info": "",
        "x": 240,
        "y": 660,
        "wires": []
    },
    {
        "id": "ecee412b5bd9bd6b",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Prefetch sr_prompts",
        "func": "msg.collection = 'sr_prompts';\nmsg.operation  = 'find';\nmsg.payload    = [{ status: 'active' }];\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 580,
        "y": 700,
        "wires": [
            [
                "050ee524cf23c91e"
            ]
        ]
    },
    {
        "id": "050ee524cf23c91e",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "DB: Find sr_prompts",
        "x": 840,
        "y": 700,
        "wires": [
            [
                "b5649b7432381163"
            ]
        ]
    },
    {
        "id": "b5649b7432381163",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Stash _aiPrompts → fetch providers",
        "func": "msg._aiPrompts = Array.isArray(msg.payload) ? msg.payload : [];\nmsg.collection = 'sr_providers';\nmsg.operation  = 'find';\nmsg.payload    = [{ status: 'active' }, { sort: { precedence: 1 } }];\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1140,
        "y": 700,
        "wires": [
            [
                "7e6153e3991c8ebd"
            ]
        ]
    },
    {
        "id": "7e6153e3991c8ebd",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "DB: Find sr_providers",
        "x": 1380,
        "y": 700,
        "wires": [
            [
                "293597706fe908f2"
            ]
        ]
    },
    {
        "id": "293597706fe908f2",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Stash _aiProviders",
        "func": "msg._aiProviders = Array.isArray(msg.payload) ? msg.payload : [];\ndelete msg.payload;\ndelete msg.collection;\ndelete msg.operation;\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1620,
        "y": 700,
        "wires": [
            [
                "9736dc335563a8e8"
            ]
        ]
    },
    {
        "id": "8622fd3ce8562026",
        "type": "comment",
        "z": "a4fcd945f4957d68",
        "name": "── FR-CR-05: AI / Hybrid AI transform pipeline (auto, post-import) ──",
        "info": "",
        "x": 1880,
        "y": 60,
        "wires": []
    },
    {
        "id": "c16a4c309f28dee4",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "NF-6: Transform queue (RDF / AI / Hybrid)",
        "func": "\nconst rdf = rdfExt.default || rdfExt;\nconst Validator = SHACLValidator.default || SHACLValidator;\nconst JsonLdStreamParser = jsonLdStreaming.JsonLdParser;\nconst RdfXmlStreamParser = rdfXmlStreaming.RdfXmlParser;\nconst CONTENT_TYPES = {\n    turtle: 'text/turtle', ttl: 'text/turtle', 'text/turtle': 'text/turtle',\n    ntriples: 'application/n-triples', nt: 'application/n-triples',\n    'n-triples': 'application/n-triples', 'application/n-triples': 'application/n-triples',\n    nquads: 'application/n-quads', nq: 'application/n-quads',\n    'n-quads': 'application/n-quads', 'application/n-quads': 'application/n-quads',\n    trig: 'application/trig', 'application/trig': 'application/trig',\n    jsonld: 'application/ld+json', 'json-ld': 'application/ld+json',\n    'application/ld+json': 'application/ld+json',\n    rdfxml: 'application/rdf+xml', 'rdf/xml': 'application/rdf+xml',\n    'application/rdf+xml': 'application/rdf+xml'\n};\nfunction contentType(value, input) {\n    const clean = String(value || '').split(';')[0].trim().toLowerCase();\n    if (CONTENT_TYPES[clean]) return CONTENT_TYPES[clean];\n    const text = typeof input === 'string' ? input.trim() : '';\n    if (typeof input === 'object' && input !== null) return 'application/ld+json';\n    if (/^<\\?xml|^<rdf:RDF/i.test(text)) return 'application/rdf+xml';\n    if (/^[\\[{]/.test(text)) return 'application/ld+json';\n    if (/\\{[\\s\\S]*\\}/.test(text) && /@prefix|prefix\\s/i.test(text)) return 'application/trig';\n    return 'text/turtle';\n}\nfunction inputText(input, type) {\n    if (Buffer.isBuffer(input)) return input.toString('utf8');\n    if (typeof input === 'string') return input;\n    if (type === 'application/ld+json' && input && typeof input === 'object') return JSON.stringify(input);\n    throw new Error('RDF input must be text, Buffer, or a JSON-LD object.');\n}\nasync function parseQuads(input, requestedType) {\n    const type = contentType(requestedType, input);\n    const text = inputText(input, type);\n    let quads = [];\n    if (type === 'text/turtle' || type === 'application/n-triples' ||\n        type === 'application/n-quads' || type === 'application/trig') {\n        quads = new N3.Parser({ format: type, baseIRI: 'urn:facis:source:' }).parse(text);\n    } else {\n        const stream = type === 'application/ld+json'\n            ? new JsonLdStreamParser({ baseIRI: 'urn:facis:source:' })\n            : new RdfXmlStreamParser({ baseIRI: 'urn:facis:source:' });\n        streamifyString(text).pipe(stream);\n        for await (const quad of stream) quads.push(quad);\n    }\n    if (quads.length === 0) throw new Error('No RDF statements were parsed.');\n    return { quads, type };\n}\nfunction termToNQ(term) {\n    if (!term || term.termType === 'DefaultGraph') return '';\n    if (term.termType === 'NamedNode') return '<' + term.value.replace(/>/g, '%3E') + '>';\n    if (term.termType === 'BlankNode') return '_:' + term.value;\n    if (term.termType === 'Variable') return '?' + term.value;\n    const escaped = String(term.value).replace(/\\\\/g, '\\\\\\\\').replace(/\"/g, '\\\\\"')\n        .replace(/\\r/g, '\\\\r').replace(/\\n/g, '\\\\n');\n    if (term.language) return '\"' + escaped + '\"@' + term.language;\n    const datatype = term.datatype && term.datatype.value;\n    return '\"' + escaped + '\"' + (datatype && datatype !== 'http://www.w3.org/2001/XMLSchema#string'\n        ? '^^<' + datatype + '>' : '');\n}\nfunction quadsToNQ(quads) {\n    return quads.map(q => termToNQ(q.subject) + ' ' + termToNQ(q.predicate) + ' ' +\n        termToNQ(q.object) + (q.graph && q.graph.termType !== 'DefaultGraph' ? ' ' + termToNQ(q.graph) : '') +\n        ' .').join('\\n');\n}\n// NF-6: statements that carry the graph's structure decide which SHACL shape\n// targets a resource, so they must survive namespace filtering.\nconst STRUCTURAL_PREDICATES = [\n    'http://www.w3.org/1999/02/22-rdf-syntax-ns#type',\n    'http://www.w3.org/2000/01/rdf-schema#subClassOf',\n    'http://www.w3.org/2000/01/rdf-schema#subPropertyOf'\n];\nfunction filterQuads(quads, namespaces) {\n    const allowed = (Array.isArray(namespaces) ? namespaces : []).map(String).map(s => s.trim()).filter(Boolean);\n    if (allowed.length === 0) return { retained: quads.slice(), discarded: [] };\n    const retained = quads.filter(q => {\n        const predicate = String(q.predicate.value || '');\n        if (STRUCTURAL_PREDICATES.indexOf(predicate) >= 0) return true;\n        return allowed.some(ns => predicate.startsWith(ns));\n    });\n    const retainedSet = new Set(retained);\n    return { retained, discarded: quads.filter(q => !retainedSet.has(q)) };\n}\nasync function validateShacl(quads, shape) {\n    if (!shape || !shape.body) return null;\n    const parsedShape = await parseQuads(shape.body, shape.rdfFormat || shape.contentType || shape.format || 'turtle');\n    const dataDataset = rdf.dataset(quads);\n    const shapeDataset = rdf.dataset(parsedShape.quads);\n    const report = await new Validator(shapeDataset).validate(dataDataset);\n    const errors = (report.results || []).map(result => ({\n        message: Array.isArray(result.message) ? result.message.map(x => x.value).join('; ') : String(result.message || 'SHACL violation'),\n        path: result.path && result.path.value ? result.path.value : null,\n        focusNode: result.focusNode && result.focusNode.value ? result.focusNode.value : null,\n        severity: result.severity && result.severity.value ? result.severity.value : null,\n        sourceShape: result.sourceShape && result.sourceShape.value ? result.sourceShape.value : null\n    }));\n    return {\n        configured: true,\n        shapeId: shape.id || shape.uniqueId || null,\n        valid: !!report.conforms,\n        status: report.conforms ? 'pass' : 'fail',\n        message: report.conforms\n            ? 'The graph conforms to the configured SHACL shape.'\n            : (errors.length ? errors[0].message : 'The graph does not conform to the configured SHACL shape.'),\n        errors: errors\n    };\n}\nasync function deterministicRdf(input, requestedType, mapping, shape) {\n    const parsed = await parseQuads(input, requestedType || mapping.rdfFormat || mapping.sourceFormat);\n    // NF-6: SHACL runs against the complete parsed graph. Namespace filtering\n    // shapes the output only and must never decide whether a shape target\n    // matches, otherwise a discarded rdf:type turns a violation into a pass.\n    const validation = await validateShacl(parsed.quads, shape);\n    const filtered = filterQuads(parsed.quads, mapping.namespacesToPreserve || []);\n    if (filtered.retained.length === 0) throw new Error('Namespace filtering removed every RDF statement.');\n    return {\n        format: 'application/n-quads',\n        rdf: quadsToNQ(filtered.retained),\n        sourceFormat: parsed.type,\n        totalStatements: parsed.quads.length,\n        retainedCount: filtered.retained.length,\n        discardedCount: filtered.discarded.length,\n        validatedStatements: parsed.quads.length,\n        shaclResult: validation || { configured: false, valid: null, status: 'not-configured', errors: [] }\n    };\n}\n\nconst lcDocs = Array.isArray(msg._lcDocs) ? msg._lcDocs : [];\nconst mappings = Array.isArray(msg._fieldMappings) ? msg._fieldMappings : [];\nconst prompts = Array.isArray(msg._aiPrompts) ? msg._aiPrompts : [];\nconst providers = Array.isArray(msg._aiProviders) ? msg._aiProviders : [];\nconst schemas = Array.isArray(msg._localSchemas) ? msg._localSchemas : [];\nfunction norm(value) { return value == null ? '' : String(value).trim().toLowerCase(); }\nfunction strategy(mapping) {\n    const value = norm(mapping && (mapping.transformationStrategy || mapping.strategy));\n    if (value.includes('hybrid') || value === 'automatic') return 'hybrid';\n    if (value.includes('ai')) return 'ai';\n    if (value.includes('json field')) return 'json-field';\n    if (value.includes('rdf') || value.includes('deterministic')) return 'rdf';\n    return value || 'none';\n}\nfunction findMapping(doc) {\n    const id = norm(doc.sourceCatalogueId);\n    const name = norm(doc.sourceCatalogue);\n    const hit = mappings.find(m => m && id && norm(m.remoteCatalogueId) === id) ||\n        mappings.find(m => m && name && norm(m.remoteCatalogue) === name) ||\n        mappings.find(m => m && id && norm(m.remoteCatalogue) === id) ||\n        mappings.find(m => m && name && norm(m.remoteCatalogueId) === name) || null;\n    if (hit) return hit;\n    // NF-6: a catalogue can carry its own deterministic RDF rules without a\n    // separate sr_mappings row. Honour that configuration at runtime so the\n    // Catalogue Registry settings and the RDF Rules screen behave the same.\n    const own = doc.catalogueTransform;\n    if (own && norm(own.strategy) && norm(own.strategy) !== 'none') {\n        return {\n            id: null,\n            remoteCatalogueId: doc.sourceCatalogueId || '',\n            remoteCatalogue: doc.sourceCatalogue || '',\n            transformationStrategy: own.strategy,\n            namespacesToPreserve: Array.isArray(own.namespacesToPreserve) ? own.namespacesToPreserve : [],\n            shaclShapeSchemaId: own.shaclShapeSchemaId || ''\n        };\n    }\n    return null;\n}\nfunction findShape(mapping) {\n    if (!mapping || !mapping.shaclShapeSchemaId) return null;\n    return schemas.find(s => s && (s.id === mapping.shaclShapeSchemaId || s.uniqueId === mapping.shaclShapeSchemaId)) || null;\n}\nfunction findPrompt(mapping) {\n    if (!mapping) return null;\n    if (mapping.promptId) {\n        const row = prompts.find(p => p && p.id === mapping.promptId && (!p.status || p.status === 'active'));\n        if (row) return row;\n    }\n    const source = mapping.remoteSchema || mapping.remoteCatalogue;\n    return prompts.find(p => p && p.sourceSchema === source && (!p.status || p.status === 'active')) || null;\n}\nfunction findProvider(prompt, mapping) {\n    const wanted = (prompt && prompt.providerId) || (mapping && mapping.providerId);\n    return (wanted && providers.find(p => p && p.id === wanted)) || providers[0] || null;\n}\nfunction decryptKey(provider) {\n    if (!provider || !provider.apiKey) return '';\n    if (typeof provider.apiKey === 'string') return provider.apiKey;\n    const encrypted = provider.apiKey;\n    if (!encrypted.ct) return '';\n    const kmsKey = env.get('DCM_KMS_KEY') || global.get('DCM_KMS_KEY') || '';\n    if (!kmsKey) return '';\n    try {\n        const decipher = crypto.createDecipheriv('aes-256-gcm', Buffer.from(kmsKey, 'hex'), Buffer.from(encrypted.iv, 'hex'));\n        decipher.setAuthTag(Buffer.from(encrypted.tag, 'hex'));\n        return decipher.update(encrypted.ct, 'hex', 'utf8') + decipher.final('utf8');\n    } catch (_) { return ''; }\n}\nfunction buildRequest(provider, prompt, source) {\n    let template = String((prompt && prompt.template) || '');\n    template = template.replace(/\\{SOURCE_ASSET\\}/g, typeof source === 'string' ? source : JSON.stringify(source, null, 2));\n    template = template.replace(/\\{SOURCE_SCHEMA\\}/g, (prompt && prompt.sourceSchema) || '');\n    template = template.replace(/\\{TARGET_SCHEMA\\}/g, (prompt && prompt.targetSchema) || '');\n    template = template.replace(/\\{EXAMPLES\\}/g, (prompt && prompt.examples) || '');\n    template = template.replace(/\\{CONSTRAINTS\\}/g, (prompt && prompt.constraints) || '');\n    const type = norm(provider.type || 'openai');\n    const model = (provider.models && provider.models[0]) || provider.model || 'gpt-4o';\n    const key = decryptKey(provider);\n    if (type === 'anthropic') {\n        return {\n            url: provider.apiEndpoint || 'https://api.anthropic.com/v1/messages',\n            headers: { 'Content-Type': 'application/json', 'x-api-key': key, 'anthropic-version': '2023-06-01' },\n            body: JSON.stringify({ model, max_tokens: 4000, messages: [{ role: 'user', content: template }] }),\n            kind: 'anthropic'\n        };\n    }\n    return {\n        url: provider.apiEndpoint || 'https://api.openai.com/v1/chat/completions',\n        headers: { 'Content-Type': 'application/json', Authorization: 'Bearer ' + key },\n        body: JSON.stringify({ model, messages: [{ role: 'user', content: template }], temperature: 0 }),\n        kind: 'openai'\n    };\n}\nfunction resultBase(doc, mapping, mode) {\n    return {\n        lcId: doc.id, mappingId: mapping ? mapping.id : null, strategy: mode,\n        promptId: null, providerId: null, status: 'failed', error: null,\n        fallbackTriggered: false, fallbackReason: null, shaclResult: null,\n        namespaces: mapping && Array.isArray(mapping.namespacesToPreserve) ? mapping.namespacesToPreserve : []\n    };\n}\nasync function rdfUpdate(doc, mapping, mode, fallbackReason) {\n    const result = resultBase(doc, mapping, mode);\n    result.fallbackTriggered = !!fallbackReason;\n    result.fallbackReason = fallbackReason || null;\n    try {\n        const shape = findShape(mapping);\n        if (mapping && mapping.shaclShapeSchemaId && !shape) {\n            result.shaclResult = {\n                configured: true, valid: false, status: 'fail',\n                shapeId: mapping.shaclShapeSchemaId,\n                errors: [{ message: 'The configured SHACL shape was not found in the schema registry.', path: null, focusNode: null, severity: null }]\n            };\n            result.error = 'Configured SHACL shape ' + mapping.shaclShapeSchemaId + ' is missing.';\n            return result;\n        }\n        const transformed = await deterministicRdf(doc.originalForm, doc.sourceContentType, mapping, shape);\n        const validation = transformed.shaclResult;\n        result.shaclResult = (validation && validation.configured === false) ? null : validation;\n        if (validation && validation.valid === false) {\n            result.error = 'SHACL validation failed: ' + (validation.message || 'the graph does not conform to the configured shape.');\n            return result;\n        }\n        result.status = 'success';\n        result.transformedForm = transformed;\n        return result;\n    } catch (err) {\n        result.error = err.message;\n        return result;\n    }\n}\nasync function validateAi(value, mapping) {\n    const shape = findShape(mapping);\n    if (!shape) return null;\n    const parsed = await parseQuads(value, 'application/ld+json');\n    return validateShacl(parsed.quads, shape);\n}\nasync function run() {\n    const completed = [];\n    const queue = [];\n    for (const doc of lcDocs) {\n        const mapping = findMapping(doc);\n        const mode = strategy(mapping);\n        if (!mapping) {\n            const row = resultBase(doc, null, 'none');\n            row.status = 'skipped'; row.error = 'No mapping configured for source catalogue.';\n            completed.push(row); continue;\n        }\n        if (mode === 'json-field') {\n            const row = resultBase(doc, mapping, mode);\n            row.status = doc.transformedForm ? 'success' : 'failed';\n            row.transformedForm = doc.transformedForm || undefined;\n            row.error = doc.transformedForm ? null : 'JSON field mapping produced no transformed form.';\n            completed.push(row); continue;\n        }\n        if (mode === 'rdf') {\n            completed.push(await rdfUpdate(doc, mapping, mode, null));\n            continue;\n        }\n        if (mode !== 'ai' && mode !== 'hybrid') {\n            const row = resultBase(doc, mapping, mode);\n            row.status = 'skipped'; row.error = 'Unsupported transformation strategy.';\n            completed.push(row); continue;\n        }\n        const prompt = findPrompt(mapping);\n        let inlineFailure = null;\n        if (prompt && String(prompt.generatedCode || '').trim()) {\n            try {\n                const fn = new Function('input', String(prompt.generatedCode) + '\\nreturn transform(input);');\n                const value = fn(doc.originalForm);\n                if (!value || typeof value !== 'object') throw new Error('generatedCode returned a non-object value.');\n                const validation = await validateAi(value, mapping);\n                if (validation && !validation.valid) throw new Error('AI generatedCode failed SHACL validation.');\n                const row = resultBase(doc, mapping, mode);\n                row.status = 'success'; row.transformedForm = value; row.promptId = prompt.id;\n                row.providerId = '(local-code)'; row.shaclResult = validation;\n                completed.push(row); continue;\n            } catch (err) { inlineFailure = err.message; }\n        }\n        const provider = findProvider(prompt, mapping);\n        if (!prompt || !provider) {\n            if (mode === 'hybrid') {\n                completed.push(await rdfUpdate(doc, mapping, mode, !prompt ? 'no_active_ai_prompt' : 'no_ai_provider'));\n            } else {\n                const row = resultBase(doc, mapping, mode);\n                row.error = !prompt ? 'No active AI prompt.' : 'No active AI provider.';\n                completed.push(row);\n            }\n            continue;\n        }\n        queue.push({\n            lcId: doc.id, doc, mapping, mode, shape: findShape(mapping),\n            promptId: prompt.id, providerId: provider.id,\n            inlineFailure, req: buildRequest(provider, prompt, doc.originalForm)\n        });\n    }\n    msg._aiQueue = queue;\n    msg._aiQueueIdx = 0;\n    msg._transformResults = completed;\n    msg._aiUpdates = completed;\n    if (queue.length === 0) {\n        node.send([null, msg]); node.done(); return;\n    }\n    const first = queue[0];\n    msg.url = first.req.url; msg.method = 'POST'; msg.headers = first.req.headers; msg.payload = first.req.body;\n    node.send([msg, null]); node.done();\n}\nrun().catch(err => {\n    msg._transformResults = [{ lcId: '', strategy: 'system', status: 'failed', error: err.message }];\n    msg._aiUpdates = msg._transformResults;\n    node.send([null, msg]); node.done();\n});\nreturn null;",
        "outputs": 2,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [
            {
                "var": "crypto",
                "module": "crypto"
            },
            {
                "var": "N3",
                "module": "n3"
            },
            {
                "var": "jsonLdStreaming",
                "module": "jsonld-streaming-parser"
            },
            {
                "var": "rdfXmlStreaming",
                "module": "rdfxml-streaming-parser"
            },
            {
                "var": "rdfExt",
                "module": "rdf-ext"
            },
            {
                "var": "SHACLValidator",
                "module": "rdf-validate-shacl"
            },
            {
                "var": "streamifyString",
                "module": "streamify-string"
            }
        ],
        "x": 2000,
        "y": 200,
        "wires": [
            [
                "344fcadc0c1a96fb"
            ],
            [
                "9913709a03113bf8"
            ]
        ]
    },
    {
        "id": "344fcadc0c1a96fb",
        "type": "http request",
        "z": "a4fcd945f4957d68",
        "name": "LLM HTTP call",
        "method": "use",
        "ret": "obj",
        "paytoqs": "ignore",
        "url": "",
        "tls": "",
        "persist": false,
        "proxy": "",
        "insecureHTTPParser": false,
        "authType": "",
        "senderr": false,
        "headers": [],
        "x": 2260,
        "y": 200,
        "wires": [
            [
                "46d1baf391499d8b"
            ]
        ]
    },
    {
        "id": "46d1baf391499d8b",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "NF-6: AI result / deterministic fallback",
        "func": "\nconst rdf = rdfExt.default || rdfExt;\nconst Validator = SHACLValidator.default || SHACLValidator;\nconst JsonLdStreamParser = jsonLdStreaming.JsonLdParser;\nconst RdfXmlStreamParser = rdfXmlStreaming.RdfXmlParser;\nconst CONTENT_TYPES = {\n    turtle: 'text/turtle', ttl: 'text/turtle', 'text/turtle': 'text/turtle',\n    ntriples: 'application/n-triples', nt: 'application/n-triples',\n    'n-triples': 'application/n-triples', 'application/n-triples': 'application/n-triples',\n    nquads: 'application/n-quads', nq: 'application/n-quads',\n    'n-quads': 'application/n-quads', 'application/n-quads': 'application/n-quads',\n    trig: 'application/trig', 'application/trig': 'application/trig',\n    jsonld: 'application/ld+json', 'json-ld': 'application/ld+json',\n    'application/ld+json': 'application/ld+json',\n    rdfxml: 'application/rdf+xml', 'rdf/xml': 'application/rdf+xml',\n    'application/rdf+xml': 'application/rdf+xml'\n};\nfunction contentType(value, input) {\n    const clean = String(value || '').split(';')[0].trim().toLowerCase();\n    if (CONTENT_TYPES[clean]) return CONTENT_TYPES[clean];\n    const text = typeof input === 'string' ? input.trim() : '';\n    if (typeof input === 'object' && input !== null) return 'application/ld+json';\n    if (/^<\\?xml|^<rdf:RDF/i.test(text)) return 'application/rdf+xml';\n    if (/^[\\[{]/.test(text)) return 'application/ld+json';\n    if (/\\{[\\s\\S]*\\}/.test(text) && /@prefix|prefix\\s/i.test(text)) return 'application/trig';\n    return 'text/turtle';\n}\nfunction inputText(input, type) {\n    if (Buffer.isBuffer(input)) return input.toString('utf8');\n    if (typeof input === 'string') return input;\n    if (type === 'application/ld+json' && input && typeof input === 'object') return JSON.stringify(input);\n    throw new Error('RDF input must be text, Buffer, or a JSON-LD object.');\n}\nasync function parseQuads(input, requestedType) {\n    const type = contentType(requestedType, input);\n    const text = inputText(input, type);\n    let quads = [];\n    if (type === 'text/turtle' || type === 'application/n-triples' ||\n        type === 'application/n-quads' || type === 'application/trig') {\n        quads = new N3.Parser({ format: type, baseIRI: 'urn:facis:source:' }).parse(text);\n    } else {\n        const stream = type === 'application/ld+json'\n            ? new JsonLdStreamParser({ baseIRI: 'urn:facis:source:' })\n            : new RdfXmlStreamParser({ baseIRI: 'urn:facis:source:' });\n        streamifyString(text).pipe(stream);\n        for await (const quad of stream) quads.push(quad);\n    }\n    if (quads.length === 0) throw new Error('No RDF statements were parsed.');\n    return { quads, type };\n}\nfunction termToNQ(term) {\n    if (!term || term.termType === 'DefaultGraph') return '';\n    if (term.termType === 'NamedNode') return '<' + term.value.replace(/>/g, '%3E') + '>';\n    if (term.termType === 'BlankNode') return '_:' + term.value;\n    if (term.termType === 'Variable') return '?' + term.value;\n    const escaped = String(term.value).replace(/\\\\/g, '\\\\\\\\').replace(/\"/g, '\\\\\"')\n        .replace(/\\r/g, '\\\\r').replace(/\\n/g, '\\\\n');\n    if (term.language) return '\"' + escaped + '\"@' + term.language;\n    const datatype = term.datatype && term.datatype.value;\n    return '\"' + escaped + '\"' + (datatype && datatype !== 'http://www.w3.org/2001/XMLSchema#string'\n        ? '^^<' + datatype + '>' : '');\n}\nfunction quadsToNQ(quads) {\n    return quads.map(q => termToNQ(q.subject) + ' ' + termToNQ(q.predicate) + ' ' +\n        termToNQ(q.object) + (q.graph && q.graph.termType !== 'DefaultGraph' ? ' ' + termToNQ(q.graph) : '') +\n        ' .').join('\\n');\n}\n// NF-6: statements that carry the graph's structure decide which SHACL shape\n// targets a resource, so they must survive namespace filtering.\nconst STRUCTURAL_PREDICATES = [\n    'http://www.w3.org/1999/02/22-rdf-syntax-ns#type',\n    'http://www.w3.org/2000/01/rdf-schema#subClassOf',\n    'http://www.w3.org/2000/01/rdf-schema#subPropertyOf'\n];\nfunction filterQuads(quads, namespaces) {\n    const allowed = (Array.isArray(namespaces) ? namespaces : []).map(String).map(s => s.trim()).filter(Boolean);\n    if (allowed.length === 0) return { retained: quads.slice(), discarded: [] };\n    const retained = quads.filter(q => {\n        const predicate = String(q.predicate.value || '');\n        if (STRUCTURAL_PREDICATES.indexOf(predicate) >= 0) return true;\n        return allowed.some(ns => predicate.startsWith(ns));\n    });\n    const retainedSet = new Set(retained);\n    return { retained, discarded: quads.filter(q => !retainedSet.has(q)) };\n}\nasync function validateShacl(quads, shape) {\n    if (!shape || !shape.body) return null;\n    const parsedShape = await parseQuads(shape.body, shape.rdfFormat || shape.contentType || shape.format || 'turtle');\n    const dataDataset = rdf.dataset(quads);\n    const shapeDataset = rdf.dataset(parsedShape.quads);\n    const report = await new Validator(shapeDataset).validate(dataDataset);\n    const errors = (report.results || []).map(result => ({\n        message: Array.isArray(result.message) ? result.message.map(x => x.value).join('; ') : String(result.message || 'SHACL violation'),\n        path: result.path && result.path.value ? result.path.value : null,\n        focusNode: result.focusNode && result.focusNode.value ? result.focusNode.value : null,\n        severity: result.severity && result.severity.value ? result.severity.value : null,\n        sourceShape: result.sourceShape && result.sourceShape.value ? result.sourceShape.value : null\n    }));\n    return {\n        configured: true,\n        shapeId: shape.id || shape.uniqueId || null,\n        valid: !!report.conforms,\n        status: report.conforms ? 'pass' : 'fail',\n        message: report.conforms\n            ? 'The graph conforms to the configured SHACL shape.'\n            : (errors.length ? errors[0].message : 'The graph does not conform to the configured SHACL shape.'),\n        errors: errors\n    };\n}\nasync function deterministicRdf(input, requestedType, mapping, shape) {\n    const parsed = await parseQuads(input, requestedType || mapping.rdfFormat || mapping.sourceFormat);\n    // NF-6: SHACL runs against the complete parsed graph. Namespace filtering\n    // shapes the output only and must never decide whether a shape target\n    // matches, otherwise a discarded rdf:type turns a violation into a pass.\n    const validation = await validateShacl(parsed.quads, shape);\n    const filtered = filterQuads(parsed.quads, mapping.namespacesToPreserve || []);\n    if (filtered.retained.length === 0) throw new Error('Namespace filtering removed every RDF statement.');\n    return {\n        format: 'application/n-quads',\n        rdf: quadsToNQ(filtered.retained),\n        sourceFormat: parsed.type,\n        totalStatements: parsed.quads.length,\n        retainedCount: filtered.retained.length,\n        discardedCount: filtered.discarded.length,\n        validatedStatements: parsed.quads.length,\n        shaclResult: validation || { configured: false, valid: null, status: 'not-configured', errors: [] }\n    };\n}\n\nconst queue = Array.isArray(msg._aiQueue) ? msg._aiQueue : [];\nconst idx = Number(msg._aiQueueIdx || 0);\nconst results = Array.isArray(msg._transformResults) ? msg._transformResults : [];\nconst current = queue[idx];\nfunction extractContent(data, kind) {\n    let body = data;\n    if (typeof body === 'string') { try { body = JSON.parse(body); } catch (_) { return ''; } }\n    if (!body || typeof body !== 'object') return '';\n    if (kind === 'anthropic') return Array.isArray(body.content) && body.content[0] ? String(body.content[0].text || '') : '';\n    return Array.isArray(body.choices) && body.choices[0] && body.choices[0].message\n        ? String(body.choices[0].message.content || '') : '';\n}\nfunction parseAiJson(text) {\n    let clean = String(text || '').trim();\n    clean = clean.replace(/^`{3}(?:json)?\\s*/i, '').replace(/`{3}\\s*$/, '').trim();\n    try { return JSON.parse(clean); } catch (_) {\n        const match = clean.match(/\\{[\\s\\S]*\\}/);\n        if (!match) return null;\n        try { return JSON.parse(match[0]); } catch (_) { return null; }\n    }\n}\nasync function fallback(reason) {\n    const row = {\n        lcId: current.lcId, mappingId: current.mapping.id, strategy: current.mode,\n        promptId: current.promptId, providerId: current.providerId, status: 'failed',\n        error: null, fallbackTriggered: true, fallbackReason: reason,\n        shaclResult: null, namespaces: current.mapping.namespacesToPreserve || []\n    };\n    try {\n        const transformed = await deterministicRdf(current.doc.originalForm, current.doc.sourceContentType, current.mapping, current.shape);\n        row.shaclResult = (transformed.shaclResult && transformed.shaclResult.configured === false) ? null : transformed.shaclResult;\n        if (transformed.shaclResult && transformed.shaclResult.valid === false) throw new Error('RDF fallback failed SHACL validation.');\n        row.status = 'success'; row.transformedForm = transformed;\n    } catch (err) { row.error = err.message; }\n    return row;\n}\nasync function run() {\n    if (current) {\n        const status = Number(msg.statusCode || 0);\n        const raw = status >= 200 && status < 300 ? extractContent(msg.payload, current.req.kind) : '';\n        const value = parseAiJson(raw);\n        let aiReason = status < 200 || status >= 300 ? 'ai_http_' + status : (!raw ? 'empty_ai_response' : (!value ? 'unparseable_ai_response' : null));\n        let validation = null;\n        if (value && !aiReason) {\n            try {\n                if (current.shape) {\n                    const parsed = await parseQuads(value, 'application/ld+json');\n                    validation = await validateShacl(parsed.quads, current.shape);\n                    if (validation && !validation.valid) aiReason = 'ai_shacl_validation_failed';\n                }\n            } catch (err) { aiReason = 'ai_validation_error: ' + err.message; }\n        }\n        if (!aiReason) {\n            results.push({\n                lcId: current.lcId, mappingId: current.mapping.id, strategy: current.mode,\n                promptId: current.promptId, providerId: current.providerId, status: 'success',\n                transformedForm: value, error: null, fallbackTriggered: false, fallbackReason: null,\n                shaclResult: validation, namespaces: current.mapping.namespacesToPreserve || []\n            });\n        } else if (current.mode === 'hybrid') {\n            results.push(await fallback(aiReason));\n        } else {\n            results.push({\n                lcId: current.lcId, mappingId: current.mapping.id, strategy: current.mode,\n                promptId: current.promptId, providerId: current.providerId, status: 'failed',\n                error: aiReason, fallbackTriggered: false, fallbackReason: null,\n                shaclResult: validation, namespaces: current.mapping.namespacesToPreserve || []\n            });\n        }\n    }\n    const nextIdx = idx + 1;\n    msg._aiQueueIdx = nextIdx;\n    msg._transformResults = results;\n    msg._aiUpdates = results;\n    if (nextIdx < queue.length) {\n        const next = queue[nextIdx];\n        msg.url = next.req.url; msg.method = 'POST'; msg.headers = next.req.headers; msg.payload = next.req.body;\n        node.send([msg, null]); node.done(); return;\n    }\n    delete msg._aiQueue; delete msg._aiQueueIdx;\n    msg.payload = '';\n    node.send([null, msg]); node.done();\n}\nrun().catch(err => {\n    results.push({ lcId: current ? current.lcId : '', strategy: current ? current.mode : 'ai', status: 'failed', error: err.message });\n    msg._transformResults = results; msg._aiUpdates = results;\n    node.send([null, msg]); node.done();\n});\nreturn null;",
        "outputs": 2,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [
            {
                "var": "N3",
                "module": "n3"
            },
            {
                "var": "jsonLdStreaming",
                "module": "jsonld-streaming-parser"
            },
            {
                "var": "rdfXmlStreaming",
                "module": "rdfxml-streaming-parser"
            },
            {
                "var": "rdfExt",
                "module": "rdf-ext"
            },
            {
                "var": "SHACLValidator",
                "module": "rdf-validate-shacl"
            },
            {
                "var": "streamifyString",
                "module": "streamify-string"
            }
        ],
        "x": 2520,
        "y": 200,
        "wires": [
            [
                "344fcadc0c1a96fb"
            ],
            [
                "9913709a03113bf8"
            ]
        ]
    },
    {
        "id": "9913709a03113bf8",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "NF-6: Persist transformation results",
        "func": "const results = Array.isArray(msg._transformResults) ? msg._transformResults : [];\nconst now = new Date().toISOString();\nconst docs = Array.isArray(msg._lcDocs) ? msg._lcDocs : [];\nfor (const row of results) {\n    const doc = docs.find(d => d && d.id === row.lcId);\n    if (!doc) continue;\n    doc.transformationStrategy = row.strategy;\n    doc.transformationStatus = row.status;\n    doc.transformationError = row.error || null;\n    doc.shaclResult = row.shaclResult || null;\n    doc.fallbackTriggered = !!row.fallbackTriggered;\n    doc.fallbackReason = row.fallbackReason || null;\n    if (row.transformedForm !== undefined) doc.transformedForm = row.transformedForm;\n}\nconst bulk = results.filter(row => row.lcId).map(row => {\n    const set = {\n        transformedAt: now,\n        transformedByMappingId: row.mappingId || null,\n        transformedByPromptId: row.promptId || null,\n        transformedByProviderId: row.providerId || null,\n        transformationStrategy: row.strategy,\n        transformationStatus: row.status,\n        transformationError: row.error || null,\n        shaclResult: row.shaclResult || null,\n        fallbackTriggered: !!row.fallbackTriggered,\n        fallbackReason: row.fallbackReason || null\n    };\n    if (row.transformedForm !== undefined) set.transformedForm = row.transformedForm;\n    return { updateOne: { filter: { id: row.lcId }, update: { $set: set } } };\n});\nif (bulk.length === 0) return [null, msg];\nmsg.collection = 'lc_assets';\nmsg.operation = 'bulkWrite';\nmsg.payload = [bulk];\nreturn [msg, null];",
        "outputs": 2,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 2820,
        "y": 200,
        "wires": [
            [
                "5125b9cd8cd557e4"
            ],
            [
                "f7fe8fc4a0918ee6"
            ]
        ]
    },
    {
        "id": "5125b9cd8cd557e4",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "DB: bulkWrite lc_assets",
        "x": 3100,
        "y": 200,
        "wires": [
            [
                "f7fe8fc4a0918ee6"
            ]
        ]
    },
    {
        "id": "f7fe8fc4a0918ee6",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "AI: pass to audit",
        "func": "delete msg._aiQueue; delete msg._aiQueueIdx; delete msg._aiUpdates;\ndelete msg._aiPrompts; delete msg._aiProviders; delete msg._localSchemas;\ndelete msg.url; delete msg.method; delete msg.headers;\nmsg.payload = '';\n\n// NF-6: a harvest that could not transform its assets is not a success. Failed\n// and required-but-skipped transformations become first-class harvest errors so\n// they show up in the run record, the harvest details and the logs.\nconst run = msg._harvestRun;\nconst results = Array.isArray(msg._transformResults) ? msg._transformResults : [];\nconst docs = Array.isArray(msg._lcDocs) ? msg._lcDocs : [];\nconst nowIso = new Date().toISOString();\nconst transformErrors = [];\n\nfunction docFor(lcId) {\n    return docs.find(function (d) { return d && d.id === lcId; }) || null;\n}\n\nfor (const row of results) {\n    if (!row) continue;\n    const strategy = String(row.strategy || 'none').toLowerCase();\n    const required = strategy !== 'none';\n    const failed = row.status === 'failed';\n    const skipped = row.status === 'skipped';\n    const noForm = row.status === 'success' && (row.transformedForm === null || row.transformedForm === undefined);\n    if (!failed && !noForm && !(skipped && required)) continue;\n\n    const doc = docFor(row.lcId);\n    const code = failed ? 'TRANSFORMATION_FAILED' : (noForm ? 'TRANSFORMATION_EMPTY' : 'TRANSFORMATION_SKIPPED');\n    const reason = row.error || (noForm ? 'The transformation produced no transformed form.' : 'A configured transformation did not run.');\n    transformErrors.push({\n        stage: 'transform',\n        step: strategy,\n        code: code,\n        level: 'error',\n        retryable: false,\n        runId: run ? run.uniqueId : null,\n        catalogueId: doc ? doc.sourceCatalogueId : null,\n        catalogueName: doc ? doc.sourceCatalogue : null,\n        assetId: row.lcId || null,\n        message: 'Transformation (' + strategy + ') for asset ' + (row.lcId || 'unknown') + ' ' + (skipped ? 'was skipped' : 'failed') + ': ' + reason,\n        detail: { shaclResult: row.shaclResult || null },\n        timestamp: nowIso\n    });\n}\n\nlet runUpdate = null;\nif (run && transformErrors.length > 0) {\n    run.errors = Array.isArray(run.errors) ? run.errors.concat(transformErrors) : transformErrors.slice();\n    run.errorCount = (run.errorCount || 0) + transformErrors.length;\n    const transformed = results.filter(function (r) { return r && r.status === 'success' && r.transformedForm; }).length;\n    run.result = transformed > 0 ? 'Partial' : 'Failed';\n    run.status = transformed > 0 ? 'completed_with_errors' : 'failed';\n    run.transformationErrorCount = transformErrors.length;\n    if (!Array.isArray(run.logs)) run.logs = [];\n    run.logs = run.logs.concat(transformErrors.map(function (e) {\n        return { runId: e.runId, assetId: e.assetId, level: 'error', step: 'transform', code: e.code, message: e.message, catalogue: e.catalogueName || '', timestamp: e.timestamp };\n    }));\n    runUpdate = {\n        operation: 'updateOne',\n        collection: 'hv_runs',\n        payload: [\n            { uniqueId: run.uniqueId },\n            { $set: { errors: run.errors, errorCount: run.errorCount, result: run.result, status: run.status, logs: run.logs, transformationErrorCount: run.transformationErrorCount } }\n        ]\n    };\n}\n\nreturn [msg, runUpdate];",
        "outputs": 2,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 3380,
        "y": 200,
        "wires": [
            [
                "b142eeaaaa987f87"
            ],
            [
                "nf6run0000000001"
            ]
        ]
    },
    {
        "id": "nf6run0000000001",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "DB: Update hv_runs (transformation outcome)",
        "x": 1900,
        "y": 340,
        "wires": [
            []
        ]
    },
    {
        "id": "4dff14750bd409a7",
        "type": "comment",
        "z": "a4fcd945f4957d68",
        "name": "── FR-ACM-06: Clear Harvest History (hv_runs + audit) ──",
        "x": 220,
        "y": 940,
        "wires": []
    },
    {
        "id": "14cc0a58c709638f",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Prepare deleteMany hv_runs",
        "func": "msg.collection = 'hv_runs';\nmsg.operation  = 'deleteMany';\nmsg.payload    = {};\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 520,
        "y": 1180,
        "wires": [
            [
                "d5f14a3c906242c9"
            ]
        ]
    },
    {
        "id": "d5f14a3c906242c9",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "DB: deleteMany hv_runs",
        "x": 857.5,
        "y": 1140,
        "wires": [
            [
                "44387564a06dfa1d"
            ]
        ]
    },
    {
        "id": "44387564a06dfa1d",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Stash hv_runs result + delete audit",
        "func": "const r = msg.payload;\nlet n = 0;\nif (r && typeof r === 'object') {\n    if (typeof r.deletedCount === 'number') n = r.deletedCount;\n    else if (Array.isArray(r) && r[0] && typeof r[0].deletedCount === 'number') n = r[0].deletedCount;\n}\nmsg._clearHvDeleted = n;\nmsg.collection = 'dcm_transformation_audit';\nmsg.operation  = 'deleteMany';\nmsg.payload    = {};\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1080,
        "y": 980,
        "wires": [
            [
                "d08b58b16519003f"
            ]
        ]
    },
    {
        "id": "d08b58b16519003f",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "DB: deleteMany audit",
        "x": 1340,
        "y": 980,
        "wires": [
            [
                "18ea5c8aae4fcaa4"
            ]
        ]
    },
    {
        "id": "18ea5c8aae4fcaa4",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "response: clearHarvestHistory",
        "func": "const r = msg.payload;\nlet auditDeleted = 0;\nif (r && typeof r === 'object') {\n    if (typeof r.deletedCount === 'number') auditDeleted = r.deletedCount;\n    else if (Array.isArray(r) && r[0] && typeof r[0].deletedCount === 'number') auditDeleted = r[0].deletedCount;\n}\nmsg.response = {\n    action: 'clearHarvestHistory',\n    status: 'success',\n    runsDeleted: msg._clearHvDeleted || 0,\n    auditDeleted: auditDeleted\n};\ndelete msg._clearHvDeleted;\ndelete msg.payload;\ndelete msg.collection;\ndelete msg.operation;\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1620,
        "y": 980,
        "wires": [
            [
                "dc56327bec3493ee"
            ]
        ]
    },
    {
        "id": "d6e453dd40e806d1",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "DB: Find lc_assets for run",
        "x": 1520,
        "y": 360,
        "wires": [
            [
                "303c1b13f93c3e8d"
            ]
        ]
    },
    {
        "id": "303c1b13f93c3e8d",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "response: getHarvestRunDetail (with assets)",
        "func": "const run = msg._hvRunDoc || null;\nconst assets = Array.isArray(msg.payload) ? msg.payload : [];\n\n// ── 1) Assets table — add Status / Duration / Error so the FE columns render ──\nconst flatAssets = assets.map(a => ({\n    id: a.id || a.uniqueId,\n    uniqueId: a.uniqueId || a.id,\n    title: a.title || a.assets || '(untitled)',\n    type: a.type || '-',\n    sourceCatalogue: a.sourceCatalogue || '-',\n    sourceCatalogueId: a.sourceCatalogueId || '',\n    remoteAssetId: a.remoteAssetId || '',\n    harvestedAt: a.harvestedAt || '',\n    transformed: !!a.transformedForm,\n    transformedByMappingId:  a.transformedByMappingId  || '',\n    transformedByPromptId:   a.transformedByPromptId   || '',\n    transformedByProviderId: a.transformedByProviderId || '',\n    // FE bindings:\n    status: a.transformedForm ? 'Success' : (a.transformedByMappingId ? 'Warning' : 'Success'),\n    duration: '-',\n    error: ''\n}));\n\n// ── 2) Logs table — add Step (derived: 'asset' if assetId, otherwise 'run') ──\nconst rawLogs = run && Array.isArray(run.logs) ? run.logs : [];\nconst logs = rawLogs.map(l => ({\n    uniqueId: l.uniqueId || (l.runId + '-' + (l.assetId || '') + '-' + (l.timestamp || '')),\n    level: l.level || 'info',\n    message: l.message || '',\n    timestamp: l.timestamp || '',\n    catalogue: l.catalogue || '',\n    assetId: l.assetId || '',\n    step: l.assetId ? 'asset' : 'run'\n}));\n\n// ── 3) Provenance — rename fields + aggregate per-catalogue counts ──\nconst rawProv = run && Array.isArray(run.provenance) ? run.provenance : [];\n\n// 3a) per-row mapping (full list, used as basis for aggregation). Rows keep the\n// canonical provenance names and the table aliases, so no view falls back to '-'\n// because of a mismatched property name.\nconst runEvidence = (run && Array.isArray(run.protocolEvidence)) ? run.protocolEvidence : [];\nfunction provProtocol(p, catName, catId) {\n    if (p.protocol) return p.protocol;\n    const ev = runEvidence.find(e => e && ((catId && e.catalogueId === catId) || (catName && e.catalogueName === catName)));\n    return (ev && ev.protocol) || '';\n}\nconst provDetailed = rawProv.map(p => {\n    const catName = p.sourceCatalogueName || p.catalogue || p.remoteCatalogueName || '';\n    const catId = p.sourceCatalogueId || p.catalogueId || p.remoteCatalogueId || '';\n    const runId = p.harvestRunId || p.runId || (run && run.uniqueId) || '';\n    const at = p.harvestedAt || p.timestamp || p.harvestDate || (run && run.startedAt) || '';\n    const strategy = p.transformationStrategy || p.strategy || '';\n    return {\n        uniqueId:               p.uniqueId || (runId + '-' + (p.assetId || '') + '-' + at),\n        runId:                  runId,\n        harvestRunId:           runId,\n        remoteCatalogueName:    catName || '-',\n        sourceCatalogueName:    catName || '-',\n        catalogueName:          catName || '-',\n        catalogue:              catName || '-',\n        remoteCatalogueId:      catId,\n        sourceCatalogueId:      catId,\n        harvestDate:            at,\n        harvestedAt:            at,\n        timestamp:              at,\n        protocol:               provProtocol(p, catName, catId),\n        status:                 p.status === 'success' ? 'completed' : (p.status || 'unknown'),\n        action:                 p.action || '',\n        assetId:                p.assetId || '',\n        title:                  p.title || '',\n        sourceId:               p.sourceId || '',\n        strategy:               strategy,\n        transformationStrategy: strategy\n    };\n});\n\n// 3b) aggregate by catalogue + run for the table the FE shows. Imported asset\n// counts stay separate from protocol errors and warnings.\nconst runDiagnostics = (run && Array.isArray(run.errors)) ? run.errors : [];\nfunction catalogueDiagnostics(catName, catId) {\n    let errors = 0;\n    let warnings = 0;\n    for (const d of runDiagnostics) {\n        const byId = catId ? d.catalogueId === catId : false;\n        const byName = (catName && catName !== '-') ? d.catalogueName === catName : false;\n        if (!byId && !byName) continue;\n        if (d.level === 'warn') warnings += 1;\n        else errors += 1;\n    }\n    return { errors: errors, warnings: warnings };\n}\nconst provByCat = {};\nfor (const r of provDetailed) {\n    const key = r.remoteCatalogueName + '|' + r.remoteCatalogueId;\n    if (!provByCat[key]) {\n        provByCat[key] = {\n            uniqueId: 'agg-' + key,\n            remoteCatalogueName: r.remoteCatalogueName,\n            sourceCatalogueName: r.remoteCatalogueName,\n            remoteCatalogueId:   r.remoteCatalogueId,\n            sourceCatalogueId:   r.remoteCatalogueId,\n            harvestRunId:        r.harvestRunId,\n            protocol:            r.protocol,\n            harvestDate:         r.harvestDate,\n            harvestedAt:         r.harvestDate,\n            status:              'completed',\n            assetsImported:      0,\n            errors:              0,\n            warnings:            0\n        };\n    }\n    const slot = provByCat[key];\n    if (r.action === 'imported' || r.action === 'crawled') slot.assetsImported += 1;\n    // keep latest harvestDate (provenance rows are appended in order, so just overwrite)\n    if (r.harvestDate > (slot.harvestDate || '')) {\n        slot.harvestDate = r.harvestDate;\n        slot.harvestedAt = r.harvestDate;\n    }\n}\nconst provAggregated = Object.values(provByCat);\nfor (const slot of provAggregated) {\n    const diag = catalogueDiagnostics(slot.remoteCatalogueName, slot.remoteCatalogueId);\n    slot.errors = diag.errors;\n    slot.warnings = diag.warnings;\n    if (diag.errors > 0) slot.status = slot.assetsImported > 0 ? 'partial' : 'failed';\n    else if (diag.warnings > 0) slot.status = 'partial';\n}\n\nmsg.response = {\n    action: 'getHarvestRunDetail',\n    status: 'success',\n    run: run ? Object.assign({}, run, { assets: flatAssets }) : null,\n    logs: logs,\n    provenance: provAggregated,\n    provenanceDetailed: provDetailed\n};\n\ndelete msg._hvRunDoc;\ndelete msg.payload;\ndelete msg.collection;\ndelete msg.operation;\nreturn msg;\n",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1570,
        "y": 240,
        "wires": [
            [
                "dc56327bec3493ee"
            ]
        ]
    },
    {
        "id": "ac03000000000001",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "NF-5 Session Resolver",
        "func": "function unwrap(value) {\n  return Array.isArray(value) ? (value[0] || null) : value;\n}\nfunction tokenFromMessage() {\n  let token = (msg.auth && msg.auth.userToken) || msg._token || (msg.data && msg.data.token) || \"\";\n  if (!token && msg.payload && typeof msg.payload === \"object\") {\n    token = msg.payload.token || (msg.payload.auth && msg.payload.auth.userToken) || msg.payload._token || \"\";\n  }\n  if (!token && msg.topic && String(msg.topic).indexOf(\"|\") !== -1) {\n    token = String(msg.topic).split(\"|\")[1] || \"\";\n  }\n  return String(token || \"\");\n}\nfunction reject(code, message) {\n  const action = msg._rbacOriginalType || msg.type || \"unknown\";\n  msg.type = action;\n  msg.response = { action, status: \"error\", ok: false, code, error: code, message };\n  delete msg.payload;\n  delete msg.collection;\n  delete msg.operation;\n  [\"_rbacStep\",\"_rbacOriginalType\",\"_rbacOriginalPayload\",\"_rbacOriginalData\",\"_rbacOriginalTopic\",\"_rbacSessionToken\"].forEach(k => delete msg[k]);\n  return [null, null, msg];\n}\n\nconst step = Number(msg._rbacStep || 0);\nif (step === 0) {\n  const token = tokenFromMessage();\n  msg._rbacOriginalType = msg.type || \"\";\n  msg._rbacOriginalPayload = msg.payload;\n  msg._rbacOriginalData = msg.data;\n  msg._rbacOriginalTopic = msg.topic || \"\";\n  if (!token) return reject(\"not_authenticated\", \"Not authenticated - no session token.\");\n  msg._rbacStep = 1;\n  msg._rbacSessionToken = token;\n  msg.collection = \"auth_sessions\";\n  msg.operation = \"findOne\";\n  msg.payload = { token };\n  return [msg, null, null];\n}\n\nif (step === 1) {\n  const session = unwrap(msg.payload);\n  const now = new Date();\n  if (!session || !session.user || (session.expiresAt && new Date(session.expiresAt) <= now)) {\n    return reject(\"not_authenticated\", \"Session expired or invalid.\");\n  }\n  const user = session.user;\n  const status = String(user.status || \"active\");\n  if (status === \"disabled\" || status === \"deleted\") {\n    return reject(\"account_disabled\", \"The account is disabled.\");\n  }\n  if (user.expiresAt && new Date(user.expiresAt) <= now) {\n    return reject(\"account_expired\", \"The account has expired.\");\n  }\n  msg.auth = {\n    username: user.username || \"\",\n    userId: user.id || session.userId || \"\",\n    userToken: msg._rbacSessionToken,\n    role: user.role || (Array.isArray(user.roles) && user.roles[0]) || \"viewer\",\n    roles: Array.isArray(user.roles) ? user.roles : [],\n    permissions: Array.isArray(user.permissions) ? user.permissions : [],\n    accessAreas: Array.isArray(user.accessAreas) ? user.accessAreas : []\n  };\n  msg.type = msg._rbacOriginalType;\n  msg.payload = msg._rbacOriginalPayload;\n  msg.data = msg._rbacOriginalData;\n  msg.topic = msg._rbacOriginalTopic;\n  msg._callerUsername = user.username || \"unknown\";\n  [\"_rbacStep\",\"_rbacOriginalType\",\"_rbacOriginalPayload\",\"_rbacOriginalData\",\"_rbacOriginalTopic\",\"_rbacSessionToken\",\"collection\",\"operation\"].forEach(k => delete msg[k]);\n  return [null, msg, null];\n}\n\nreturn reject(\"auth_error\", \"Unexpected authorization state.\");",
        "outputs": 3,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 350,
        "y": 180,
        "wires": [
            [
                "ac03000000000002"
            ],
            [
                "ac03000000000003"
            ],
            [
                "dc56327bec3493ee"
            ]
        ]
    },
    {
        "id": "ac03000000000002",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "forEach",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "NF-5 Auth Session Lookup",
        "x": 610,
        "y": 180,
        "wires": [
            [
                "ac03000000000001"
            ]
        ]
    },
    {
        "id": "ac03000000000003",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "NF-5 Permission Gate",
        "func": "const action = String(msg.type || \"\");\nconst auth = msg.auth || {};\nconst permissions = Array.isArray(auth.permissions) ? auth.permissions : [];\nconst roles = Array.isArray(auth.roles) ? auth.roles : [];\nconst requiredPermission = {\n  \"getHarvestCatalogues\": \"harvest.scope.read\",\n  \"startHarvest\": \"harvest.run.execute\",\n  \"listHarvestRuns\": \"harvest.scope.read\",\n  \"getHarvestRunDetail\": \"harvest.scope.read\",\n  \"listHarvestLogs\": \"harvest.scope.read\",\n  \"listHarvestProvenance\": \"harvest.scope.read\",\n  \"pauseHarvest\": \"harvest.lifecycle.configure\",\n  \"resumeHarvest\": \"harvest.lifecycle.configure\",\n  \"cancelHarvest\": \"harvest.lifecycle.configure\",\n  \"clearHarvestHistory\": \"harvest.scope.delete\"\n}[action] || null;\nconst isAdmin = auth.role === \"admin\" || roles.includes(\"admin\");\nconst hasPermission = requiredPermission && (\n  permissions.includes(requiredPermission) ||\n  (isAdmin && permissions.includes(\"*\"))\n);\n\nif (hasPermission) return [msg, null, null];\n\nconst reason = requiredPermission ? \"missing_permission\" : \"unmapped_action\";\nconst message = requiredPermission\n  ? \"Insufficient permissions. Required: \" + requiredPermission\n  : \"Action is not permitted by the backend authorization policy.\";\nconst audit = {\n  collection: \"dcm_audit_log\",\n  operation: \"insertOne\",\n  payload: {\n    timestamp: new Date().toISOString(),\n    actor: auth.username || \"unknown\",\n    actorId: auth.userId || null,\n    area: \"harvest\",\n    action: \"access.denied\",\n    target: action || \"unknown\",\n    after: { requiredPermission, reason },\n    result: \"denied\",\n    source: \"rbac\"\n  }\n};\nmsg.response = {\n  action: action || \"unknown\",\n  status: \"error\",\n  ok: false,\n  code: \"permission_denied\",\n  error: \"permission_denied\",\n  message,\n  requiredPermission\n};\ndelete msg.payload;\nreturn [null, audit, msg];",
        "outputs": 3,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 850,
        "y": 180,
        "wires": [
            [
                "bde976005c102747"
            ],
            [
                "ac03000000000004"
            ],
            [
                "dc56327bec3493ee"
            ]
        ]
    },
    {
        "id": "ac03000000000004",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "NF-5 Denied Audit",
        "x": 1080,
        "y": 180,
        "wires": [
            []
        ]
    },
    {
        "id": "nf6m3shapeprep1",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Prefetch sr_local_schemas for SHACL",
        "func": "msg.collection = 'sr_local_schemas';\nmsg.operation = 'find';\nmsg.payload = [{}];\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1610,
        "y": 400,
        "wires": [
            [
                "nf6m3shapedb001"
            ]
        ]
    },
    {
        "id": "nf6m3shapedb001",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "DB: Find sr_local_schemas",
        "x": 1880,
        "y": 400,
        "wires": [
            [
                "nf6m3shapestash1"
            ]
        ]
    },
    {
        "id": "nf6m3shapestash1",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Stash _localSchemas",
        "func": "msg._localSchemas = Array.isArray(msg.payload) ? msg.payload : [];\ndelete msg.payload; delete msg.collection; delete msg.operation;\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 2120,
        "y": 400,
        "wires": [
            [
                "ecee412b5bd9bd6b"
            ]
        ]
    },
    {
        "id": "a1b2c3d4e5f60001",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Reconcile lc_assets insert (NF-4 RC-7)",
        "func": "const expected = (msg._lcDocs || []).length;\nlet inserted = null;\nconst p = msg.payload;\nif (p && typeof p === 'object') {\n    if (typeof p.insertedCount === 'number') inserted = p.insertedCount;\n    else if (Array.isArray(p.insertedIds)) inserted = p.insertedIds.length;\n    else if (p.insertedIds && typeof p.insertedIds === 'object') inserted = Object.keys(p.insertedIds).length;\n    else if (typeof p.acknowledged === 'boolean' && Array.isArray(p.ops)) inserted = p.ops.length;\n}\nlet updateMsg = null;\nif (inserted != null && inserted !== expected && msg._harvestRun) {\n    const run = msg._harvestRun;\n    if (!msg._runDiag) msg._runDiag = { errors: [], logs: [], counters: {} };\n    const errObj = { stage:'persistence', step:'insert lc_assets', code:'insert_count_mismatch', level:'error', retryable:true, runId: run.uniqueId, message:'Expected to store ' + expected + ' assets but MongoDB reported ' + inserted + '.', detail:{ expected: expected, inserted: inserted }, timestamp:new Date().toISOString() };\n    msg._runDiag.errors.push(errObj);\n    if (msg._runDiag.counters) msg._runDiag.counters.persistenceErrors = (msg._runDiag.counters.persistenceErrors || 0) + 1;\n    run.errorCount = (run.errorCount || 0) + 1;\n    run.result = (inserted > 0) ? 'Partial' : 'Failed';\n    run.status = (inserted > 0) ? 'completed_with_errors' : 'failed';\n    run.assetsAdded = inserted; run.totalAssets = inserted; run.importedCount = inserted;\n    if (Array.isArray(run.errors)) run.errors.push(errObj); else run.errors = [errObj];\n    updateMsg = { operation:'updateOne', collection:'hv_runs', payload:[ { uniqueId: run.uniqueId }, { $set: { errorCount: run.errorCount, result: run.result, status: run.status, assetsAdded: run.assetsAdded, totalAssets: run.totalAssets, importedCount: run.importedCount, errors: run.errors } } ], _msgid: msg._msgid };\n}\nreturn [msg, updateMsg];",
        "outputs": 2,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1600,
        "y": 200,
        "wires": [
            [
                "c16a4c309f28dee4"
            ],
            [
                "a1b2c3d4e5f60002"
            ]
        ]
    },
    {
        "id": "a1b2c3d4e5f60002",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "DB: Update hv_runs (reconcile)",
        "x": 1860,
        "y": 260,
        "wires": [
            []
        ]
    },
    {
        "id": "a1b2c3d4e5f60003",
        "type": "catch",
        "z": "a4fcd945f4957d68",
        "name": "Catch: Fetch Catalogue API",
        "scope": [
            "2d6d4f7aa11eeeaa"
        ],
        "uncaught": false,
        "x": 620,
        "y": 60,
        "wires": [
            [
                "5e5b280d414c2c59"
            ]
        ]
    },
    {
        "id": "a1b2c3d4e5f60004",
        "type": "status",
        "z": "a4fcd945f4957d68",
        "name": "Status: Fetch Catalogue API",
        "scope": [
            "2d6d4f7aa11eeeaa"
        ],
        "x": 620,
        "y": 20,
        "wires": [
            [
                "a1b2c3d4e5f60005"
            ]
        ]
    },
    {
        "id": "a1b2c3d4e5f60005",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "Log fetch status (NF-4)",
        "func": "if (msg.status && msg.status.text) { node.warn('[harvest] fetch node status: ' + msg.status.text); }\nreturn null;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 860,
        "y": 20,
        "wires": [
            []
        ]
    },
    {
        "id": "nf8ch00000000009",
        "type": "comment",
        "z": "a4fcd945f4957d68",
        "name": "── NF-8: TAMPER-EVIDENT AUDIT HASH CHAIN ──",
        "info": "Append-only SHA-256 hash chain over dcm_transformation_audit.\n\nrecordHash = SHA256(previousHash + canonicalPayload)\ncanonicalPayload = JSON of a fixed, sorted key set (chainVersion 1),\nexcluding recordHash and previousHash. Missing optional values are\nnormalised to null so verification is deterministic.\n\nSequence numbers and previousHash are allocated atomically via a\ncompare-and-set on the dcm_audit_chain_state document: the update only\napplies when seq and lastHash still match what this writer observed.\nA losing writer retries (bounded), so two records can never share a\nsequenceNumber or previousHash.\n\nRecords written before this chain existed carry no hash and are\nreported as \"legacy / unverified\" — never as verified.",
        "x": 300,
        "y": 1180,
        "wires": []
    },
    {
        "id": "nf8ch00000000001",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "audit chain: read chain head",
        "func": "msg.collection = 'dcm_audit_chain_state';\nmsg.operation  = 'findOne';\nmsg.payload    = { _id: 'dcm_transformation_audit' };\nreturn msg;",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 340,
        "y": 1230,
        "wires": [
            [
                "nf8ch00000000002"
            ]
        ]
    },
    {
        "id": "nf8ch00000000002",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "DB: Read Audit Chain State",
        "x": 640,
        "y": 1230,
        "wires": [
            [
                "nf8ch00000000003"
            ]
        ]
    },
    {
        "id": "nf8ch00000000003",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "audit chain: hash rows + CAS reserve",
        "func": "// ── NF-8 audit hash chain (chainVersion 1) ─────────────────────────\n// This block MUST stay byte-identical in the M3 writer and the M1 verifier.\n// The Node-RED function sandbox does not expose node:crypto, so SHA-256 is\n// computed here directly (FIPS 180-4) instead of failing at runtime.\nvar AUDIT_CHAIN_VERSION = 1;\nvar AUDIT_GENESIS = 'GENESIS';\nvar AUDIT_CANONICAL_KEYS = [\n    'catalogueId', 'catalogueNameSnapshot', 'chainVersion', 'durationMs', 'errorCode',\n    'errorMessage', 'eventType', 'harvestRunId', 'id', 'localAssetId', 'operationId',\n    'promptVersionId', 'remoteAssetId', 'sequenceNumber', 'status', 'strategy',\n    'timestamp', 'userId', 'validationStatus'\n];\nvar AUDIT_SHA256_K = [\n    0x428a2f98, 0x71374491, 0xb5c0fbcf, 0xe9b5dba5, 0x3956c25b, 0x59f111f1, 0x923f82a4, 0xab1c5ed5,\n    0xd807aa98, 0x12835b01, 0x243185be, 0x550c7dc3, 0x72be5d74, 0x80deb1fe, 0x9bdc06a7, 0xc19bf174,\n    0xe49b69c1, 0xefbe4786, 0x0fc19dc6, 0x240ca1cc, 0x2de92c6f, 0x4a7484aa, 0x5cb0a9dc, 0x76f988da,\n    0x983e5152, 0xa831c66d, 0xb00327c8, 0xbf597fc7, 0xc6e00bf3, 0xd5a79147, 0x06ca6351, 0x14292967,\n    0x27b70a85, 0x2e1b2138, 0x4d2c6dfc, 0x53380d13, 0x650a7354, 0x766a0abb, 0x81c2c92e, 0x92722c85,\n    0xa2bfe8a1, 0xa81a664b, 0xc24b8b70, 0xc76c51a3, 0xd192e819, 0xd6990624, 0xf40e3585, 0x106aa070,\n    0x19a4c116, 0x1e376c08, 0x2748774c, 0x34b0bcb5, 0x391c0cb3, 0x4ed8aa4a, 0x5b9cca4f, 0x682e6ff3,\n    0x748f82ee, 0x78a5636f, 0x84c87814, 0x8cc70208, 0x90befffa, 0xa4506ceb, 0xbef9a3f7, 0xc67178f2\n];\nfunction auditUtf8Bytes(str) {\n    var bytes = [];\n    for (var i = 0; i < str.length; i++) {\n        var c = str.charCodeAt(i);\n        if (c < 0x80) {\n            bytes.push(c);\n        } else if (c < 0x800) {\n            bytes.push(0xc0 | (c >> 6), 0x80 | (c & 0x3f));\n        } else if (c >= 0xd800 && c <= 0xdbff && i + 1 < str.length) {\n            var cp = 0x10000 + ((c - 0xd800) << 10) + (str.charCodeAt(i + 1) - 0xdc00);\n            bytes.push(0xf0 | (cp >> 18), 0x80 | ((cp >> 12) & 0x3f), 0x80 | ((cp >> 6) & 0x3f), 0x80 | (cp & 0x3f));\n            i++;\n        } else {\n            bytes.push(0xe0 | (c >> 12), 0x80 | ((c >> 6) & 0x3f), 0x80 | (c & 0x3f));\n        }\n    }\n    return bytes;\n}\nfunction auditSha256Hex(str) {\n    var H = [0x6a09e667, 0xbb67ae85, 0x3c6ef372, 0xa54ff53a, 0x510e527f, 0x9b05688c, 0x1f83d9ab, 0x5be0cd19];\n    var bytes = auditUtf8Bytes(String(str));\n    var bitLenHi = Math.floor((bytes.length / 536870912)) >>> 0;\n    var bitLenLo = (bytes.length * 8) >>> 0;\n    bytes.push(0x80);\n    while (bytes.length % 64 !== 56) bytes.push(0);\n    bytes.push((bitLenHi >>> 24) & 255, (bitLenHi >>> 16) & 255, (bitLenHi >>> 8) & 255, bitLenHi & 255);\n    bytes.push((bitLenLo >>> 24) & 255, (bitLenLo >>> 16) & 255, (bitLenLo >>> 8) & 255, bitLenLo & 255);\n    function rotr(x, n) { return ((x >>> n) | (x << (32 - n))) >>> 0; }\n    var w = new Array(64);\n    for (var off = 0; off < bytes.length; off += 64) {\n        var i;\n        for (i = 0; i < 16; i++) {\n            w[i] = ((bytes[off + i * 4] << 24) | (bytes[off + i * 4 + 1] << 16) |\n                    (bytes[off + i * 4 + 2] << 8) | bytes[off + i * 4 + 3]) >>> 0;\n        }\n        for (i = 16; i < 64; i++) {\n            var s0 = (rotr(w[i - 15], 7) ^ rotr(w[i - 15], 18) ^ (w[i - 15] >>> 3)) >>> 0;\n            var s1 = (rotr(w[i - 2], 17) ^ rotr(w[i - 2], 19) ^ (w[i - 2] >>> 10)) >>> 0;\n            w[i] = (((w[i - 16] + s0) >>> 0) + ((w[i - 7] + s1) >>> 0)) >>> 0;\n        }\n        var a = H[0], b = H[1], c = H[2], d = H[3], e = H[4], f = H[5], g = H[6], h = H[7];\n        for (i = 0; i < 64; i++) {\n            var S1 = (rotr(e, 6) ^ rotr(e, 11) ^ rotr(e, 25)) >>> 0;\n            var ch = ((e & f) ^ ((~e) & g)) >>> 0;\n            var t1 = (((h + S1) >>> 0) + ((ch + AUDIT_SHA256_K[i]) >>> 0) + w[i]) >>> 0;\n            var S0 = (rotr(a, 2) ^ rotr(a, 13) ^ rotr(a, 22)) >>> 0;\n            var maj = ((a & b) ^ (a & c) ^ (b & c)) >>> 0;\n            var t2 = (S0 + maj) >>> 0;\n            h = g; g = f; f = e; e = (d + t1) >>> 0;\n            d = c; c = b; b = a; a = (t1 + t2) >>> 0;\n        }\n        H[0] = (H[0] + a) >>> 0; H[1] = (H[1] + b) >>> 0; H[2] = (H[2] + c) >>> 0; H[3] = (H[3] + d) >>> 0;\n        H[4] = (H[4] + e) >>> 0; H[5] = (H[5] + f) >>> 0; H[6] = (H[6] + g) >>> 0; H[7] = (H[7] + h) >>> 0;\n    }\n    var hex = '';\n    for (var j = 0; j < 8; j++) hex += ('00000000' + H[j].toString(16)).slice(-8);\n    return hex;\n}\nfunction auditNorm(v) {\n    if (v === undefined || v === null || v === '') return null;\n    if (typeof v === 'number' || typeof v === 'boolean') return v;\n    return String(v);\n}\nfunction auditCanonicalPayload(rec) {\n    var out = {};\n    for (var i = 0; i < AUDIT_CANONICAL_KEYS.length; i++) {\n        var k = AUDIT_CANONICAL_KEYS[i];\n        out[k] = auditNorm(rec[k]);\n    }\n    return JSON.stringify(out);\n}\nfunction auditRecordHash(previousHash, rec) {\n    return auditSha256Hex(String(previousHash || AUDIT_GENESIS) + auditCanonicalPayload(rec));\n}\n\nconst state = (msg.payload && !Array.isArray(msg.payload)) ? msg.payload\n            : (Array.isArray(msg.payload) ? msg.payload[0] : null);\n\nconst rows = Array.isArray(msg._auditPendingRows) ? msg._auditPendingRows : [];\nif (!rows.length) { return [null, msg]; }\n\nconst observedSeq  = state && typeof state.seq === 'number' ? state.seq : 0;\nconst observedHash = (state && state.lastHash) ? state.lastHash : AUDIT_GENESIS;\n\n// Build the chain locally over the range this writer intends to reserve.\ntry {\n    let prev = observedHash;\n    let seq = observedSeq;\n    for (let i = 0; i < rows.length; i++) {\n        seq += 1;\n        rows[i].sequenceNumber = seq;\n        rows[i].chainVersion = AUDIT_CHAIN_VERSION;\n        rows[i].hashAlgorithm = 'SHA-256';\n        rows[i].previousHash = prev;\n        rows[i].recordHash = auditRecordHash(prev, rows[i]);\n        if (!rows[i].recordHash || rows[i].recordHash.length !== 64) {\n            throw new Error('SHA-256 produced an invalid digest.');\n        }\n        prev = rows[i].recordHash;\n    }\n    msg._auditChainFrom = observedSeq;\n    msg._auditChainTo = seq;\n    msg._auditChainLastHash = prev;\n} catch (err) {\n    // Never persist an unhashed audit record and never downgrade it to legacy.\n    node.error('NF-8 audit chain: hashing failed — ' + err.message);\n    delete msg._auditPendingRows;\n    msg._auditWriteError = {\n        code: 'AUDIT_WRITE_FAILED',\n        message: 'Audit records could not be hashed; no audit evidence was written for this harvest.'\n    };\n    delete msg.payload;\n    return [null, msg];\n}\n\n// Compare-and-set: only advance when nobody else moved the head meanwhile.\nconst filter = state\n    ? { _id: 'dcm_transformation_audit', seq: observedSeq, lastHash: state.lastHash === undefined ? null : state.lastHash }\n    : { _id: 'dcm_transformation_audit', seq: { $exists: false } };\n\nmsg.collection = 'dcm_audit_chain_state';\nmsg.operation  = 'updateOne';\nmsg.payload = [\n    filter,\n    { $set: { seq: msg._auditChainTo, lastHash: msg._auditChainLastHash, chainVersion: AUDIT_CHAIN_VERSION, updatedAt: new Date().toISOString() },\n      $setOnInsert: { genesis: AUDIT_GENESIS, startedAt: new Date().toISOString() } },\n    { upsert: !state }\n];\nreturn [msg, null];\n",
        "outputs": 2,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 960,
        "y": 1230,
        "wires": [
            [
                "nf8ch00000000004"
            ],
            [
                "9d3da29eadaf1d1e"
            ]
        ]
    },
    {
        "id": "nf8ch00000000004",
        "type": "mongodb4",
        "z": "a4fcd945f4957d68",
        "clientNode": "b991ffcd8f6c360a",
        "mode": "collection",
        "collection": "",
        "operation": "",
        "output": "toArray",
        "maxTimeMS": "0",
        "handleDocId": false,
        "name": "DB: CAS Advance Chain Head",
        "x": 1300,
        "y": 1230,
        "wires": [
            [
                "nf8ch00000000005"
            ]
        ]
    },
    {
        "id": "nf8ch00000000005",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "audit chain: CAS result → insert or retry",
        "func": "const res = msg.payload || {};\nfunction num(v) { return typeof v === 'number' ? v : 0; }\nconst won = num(res.matchedCount) + num(res.modifiedCount) + num(res.upsertedCount) +\n            num(res.nMatched) + num(res.nModified) + num(res.nUpserted) > 0;\n\nif (won) {\n    const rows = msg._auditPendingRows || [];\n    delete msg._auditPendingRows;\n    delete msg._auditCasAttempt;\n    msg._auditExpectedCount = rows.length;\n    msg.collection = 'dcm_transformation_audit';\n    msg.operation  = 'insertMany';\n    msg.payload    = [ rows ];\n    return [msg, null, null];\n}\n\n// Another writer advanced the head — re-read and rebuild the chain.\nmsg._auditCasAttempt = (msg._auditCasAttempt || 0) + 1;\nif (msg._auditCasAttempt <= 5) {\n    node.warn('NF-8 audit chain: CAS contention, retry ' + msg._auditCasAttempt);\n    delete msg.payload;\n    return [null, msg, null];\n}\n\n// Exhausted: fail the harvest response loudly instead of dropping the message.\nnode.error('NF-8 audit chain: could not reserve a sequence range after 5 attempts; audit rows were NOT written.');\ndelete msg._auditPendingRows;\ndelete msg._auditCasAttempt;\ndelete msg.payload;\nmsg._auditWriteError = {\n    code: 'AUDIT_WRITE_FAILED',\n    message: 'Audit chain is contended; the audit records for this harvest could not be reserved.'\n};\nreturn [null, null, msg];\n",
        "outputs": 3,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1640,
        "y": 1230,
        "wires": [
            [
                "6b85ed9cbde579a9"
            ],
            [
                "nf8ch00000000001"
            ],
            [
                "9d3da29eadaf1d1e"
            ]
        ]
    },
    {
        "id": "nf8ch00000000006",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "audit chain: insert result",
        "func": "const res = msg.payload || {};\nconst expected = msg._auditExpectedCount || 0;\nfunction num(v) { return typeof v === 'number' ? v : 0; }\nconst inserted = num(res.insertedCount) || (res.insertedIds ? Object.keys(res.insertedIds).length : 0);\n\ndelete msg._auditExpectedCount;\ndelete msg.payload;\ndelete msg.collection;\ndelete msg.operation;\n\nif (expected > 0 && inserted < expected) {\n    node.error('NF-8 audit chain: expected ' + expected + ' audit record(s), inserted ' + inserted + '.');\n    msg._auditWriteError = {\n        code: 'AUDIT_WRITE_FAILED',\n        message: 'Only ' + inserted + ' of ' + expected + ' audit record(s) were persisted.'\n    };\n    return msg;\n}\n\nmsg._auditRowsWritten = inserted;\nreturn msg;\n",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 1640,
        "y": 1300,
        "wires": [
            [
                "9d3da29eadaf1d1e"
            ]
        ]
    },
    {
        "id": "nf8ch00000000007",
        "type": "catch",
        "z": "a4fcd945f4957d68",
        "name": "Catch: audit chain",
        "scope": [
            "b142eeaaaa987f87",
            "nf8ch00000000001",
            "nf8ch00000000002",
            "nf8ch00000000003",
            "nf8ch00000000004",
            "nf8ch00000000005",
            "6b85ed9cbde579a9",
            "nf8ch00000000006"
        ],
        "uncaught": false,
        "x": 340,
        "y": 1370,
        "wires": [
            [
                "nf8ch00000000008"
            ]
        ]
    },
    {
        "id": "nf8ch00000000008",
        "type": "function",
        "z": "a4fcd945f4957d68",
        "name": "audit chain: failure → harvest response",
        "func": "// The harvest must never silently succeed when its audit evidence failed.\nconst reason = (msg.error && msg.error.message) ? String(msg.error.message) : 'Unknown audit pipeline error.';\nnode.error('NF-8 audit chain failure: ' + reason);\ndelete msg._auditPendingRows;\ndelete msg._auditCasAttempt;\ndelete msg._auditExpectedCount;\ndelete msg.payload;\ndelete msg.collection;\ndelete msg.operation;\ndelete msg.error;\nmsg._auditWriteError = {\n    code: 'AUDIT_WRITE_FAILED',\n    message: 'The tamper-evident audit records for this harvest could not be written.'\n};\nreturn msg;\n",
        "outputs": 1,
        "timeout": 0,
        "noerr": 0,
        "initialize": "",
        "finalize": "",
        "libs": [],
        "x": 700,
        "y": 1370,
        "wires": [
            [
                "9d3da29eadaf1d1e"
            ]
        ]
    }
]
