{{- if .Values.quota.enabled }}
apiVersion: v1
kind: ResourceQuota
metadata:
  name: {{ include "dcm.fullname" . }}-quota
  namespace: {{ include "dcm.namespace" . }}
  labels:
    {{- include "dcm.labels" . | nindent 4 }}
spec:
  hard:
    {{- toYaml .Values.quota.hard | nindent 4 }}
{{- end }}
